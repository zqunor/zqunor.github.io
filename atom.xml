<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>程序小工</title>
  
  <subtitle>Just do it!</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://blog.i2arch.tech/"/>
  <updated>2018-12-11T13:04:01.179Z</updated>
  <id>https://blog.i2arch.tech/</id>
  
  <author>
    <name>程序小工</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>系统学习 Docker 践行 DevOps 理念</title>
    <link href="https://blog.i2arch.tech/2018/12/11/docker-devops/"/>
    <id>https://blog.i2arch.tech/2018/12/11/docker-devops/</id>
    <published>2018-12-11T13:02:12.000Z</published>
    <updated>2018-12-11T13:04:01.179Z</updated>
    
    <content type="html"><![CDATA[<h1 id="系统学习-docker-践行-devops-理念"><a class="markdownIt-Anchor" href="#系统学习-docker-践行-devops-理念"></a> 系统学习 Docker 践行 DevOps 理念</h1><h2 id="vagrant"><a class="markdownIt-Anchor" href="#vagrant"></a> vagrant</h2><p><code>vagrant --help</code></p><p>指定虚拟机的目录：<br><code>cd ~/Vagrant/centos7/</code><br><code>vagrant init centos/7</code><br>创建一个 centos 7 的虚拟机，在当前目录初始化一个 Vagrantfile 文件，<br>描述了需要创建的虚拟机<br>config.vm.box = “centos/7”</p><p><code>vagrant up</code><br>启动 centos 7 的 VM，如果有的话直接加载，如果没有的话要去网上下载<br>，启动完成之后，就可以在 virtualbox 中看到成功创建了一个 centos7 的虚拟机</p><p>然后在 centos7 目录下执行：<br><code>vagrant ssh</code><br>进入到 centos7 的 VM 中</p><p>查看当前已经有的虚拟机：<br><code>vagrant status</code></p><p>停掉运行的 VM<br><code>vagrant halt</code></p><p>删除创建的 VM<br><code>vagrant destroy</code></p><p>通过 Vagrantfile 可以创建各种虚拟机，<br>可以从 vagrant cloud 网上找相应的 Vagrantfile 来创建相应环境的虚拟机</p><p>在 Vagrantfile 中配置 provision,使得启动虚拟机时可以自动运行相关命令</p><p>示例，配置 Vagrantfile 文件，使得启动虚拟机时自动安装配置好环境：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">config.vm.provision <span class="string">"shell"</span>, inline: &lt;&lt;-SHELL</span><br><span class="line">    sudo yum update</span><br><span class="line">    sudo yum remove docker \</span><br><span class="line">                docker-client \</span><br><span class="line">                docker-client-latest \</span><br><span class="line">                docker-common \</span><br><span class="line">                docker-latest \</span><br><span class="line">                docker-latest-logrotate \</span><br><span class="line">                docker-logrotate \</span><br><span class="line">                docker-selinux \</span><br><span class="line">                docker-engine-selinux \</span><br><span class="line">                docker-engine</span><br><span class="line">    sudo yum install -y yum-utils \</span><br><span class="line">                device-mapper-persistent-data \</span><br><span class="line">                lvm2</span><br><span class="line">    sudo yum-config-manager \</span><br><span class="line">                --add-repo \</span><br><span class="line">                https://download.docker.com/linux/centos/docker-ce.repo</span><br><span class="line">    sudo yum install -y docker-ce</span><br><span class="line">    sudo groupadd docker</span><br><span class="line">    sudo gpasswd -a docker vagrant</span><br><span class="line">    sudo service docker restart</span><br><span class="line">SHELL</span><br></pre></td></tr></table></figure><h2 id="docker-machine"><a class="markdownIt-Anchor" href="#docker-machine"></a> Docker Machine</h2><h3 id="1-在虚拟机中自动安装-docker-engine-的工具"><a class="markdownIt-Anchor" href="#1-在虚拟机中自动安装-docker-engine-的工具"></a> 1、在虚拟机中自动安装 Docker Engine 的工具</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#创建一个安装好docker的linux虚拟机，命名为demo</span></span><br><span class="line">docker-machine create &#123;demo&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">#查看当前创建的虚拟机和相应的状态</span></span><br><span class="line">docker-machine ls</span><br><span class="line"></span><br><span class="line"><span class="comment">#进入到相应的虚拟机</span></span><br><span class="line">docker-machine ssh &#123;demo&#125;</span><br></pre></td></tr></table></figure><h3 id="2-docker-machine-在-mac-中的应用"><a class="markdownIt-Anchor" href="#2-docker-machine-在-mac-中的应用"></a> 2、docker-machine 在 mac 中的应用：</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#查看mac上的docker信息</span></span><br><span class="line">docker version</span><br><span class="line"></span><br><span class="line"><span class="comment">#将mac的服务器端连上demo的里的docker服务器端,实现远程管理docker machine</span></span><br><span class="line">docker-machine env &#123;demo&#125;</span><br><span class="line"><span class="built_in">eval</span> $(docker-machine env &#123;demo&#125;)</span><br><span class="line"><span class="comment">#此时在mac上运行docker version查看的server端的信息就是demo这台虚拟机中的docker server信息</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#</span></span><br><span class="line">docker-machine env --<span class="built_in">unset</span></span><br><span class="line"><span class="built_in">eval</span> $(docker-machine env --<span class="built_in">unset</span>)</span><br></pre></td></tr></table></figure><h3 id="3-docker-machine-在阿里云上的使用"><a class="markdownIt-Anchor" href="#3-docker-machine-在阿里云上的使用"></a> 3、docker machine 在阿里云上的使用</h3><p>docker machine 可以通过这种方式实现本地 docker 命令远程管理 docker machine</p><p>在本地运行 docker 命令创建远程服务器上的 docker 容器：<br>使用 driver（在官网上找第三方 driver: 阿里云<code>docker-machine-driver-aliyunecs</code>）</p><p>安装目录：<code>/usr/local/bin/docker-machine-driver-aliyunecs</code></p><p>验证阿里云 ecs driver 安装成功<br><code>docker-machine create -d aliyunecs --help</code></p><h4 id="使用"><a class="markdownIt-Anchor" href="#使用"></a> 使用</h4><ul><li><p>前提：账户余额大于 100</p></li><li><p>步骤：</p></li></ul><p>（1）阿里云控制台： 创建用户的 access key(访问控制=》用户管理=》创建用户=》创建 AccessKey)</p><p>（2）本地命令行，创建阿里云的 docker host：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#执行的命令：</span></span><br><span class="line">`docker-machine create -d aliyunecs --aliyunecs-io-optimized=optimized --aliyunecs-instance-type=ecs.c5.large --aliyunecs-access-key-id=L&#123;access-key-id&#125; --aliyunecs-access-key-secret=&#123;access-key-secret&#125; --aliyunecs-region=&#123;region&#125; &#123;name&#125;</span><br></pre></td></tr></table></figure><p>参数说明：</p><p><code>--aliyunecs-io-optimized=optimized</code> io 优化的参数，必须要添加<br><code>--aliyunecs-instance-type=ecs.c5.large</code> 实例的类型<br><code>--aliyunecs-region={region}</code> 创建的实例所属地域<br><code>{imooc}</code> 创建的实例名称</p><p>（3）本地命令行，连接阿里云的 docker server</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker-machine env &#123;imooc&#125;</span><br><span class="line"><span class="built_in">eval</span> $(docker-machine env &#123;imooc&#125;)</span><br></pre></td></tr></table></figure><p>创建完成之后就可以在阿里云的控制台中看到刚刚创建的实例。但是不用的时候及时删除，以免产生不必要的收费。</p><h2 id="docker-架构和底层技术简介"><a class="markdownIt-Anchor" href="#docker-架构和底层技术简介"></a> Docker 架构和底层技术简介</h2><h2 id="镜像-image"><a class="markdownIt-Anchor" href="#镜像-image"></a> 镜像 Image</h2><h3 id="1什么是-image"><a class="markdownIt-Anchor" href="#1什么是-image"></a> 1.什么是 Image</h3><ul><li>文件和 meta data 的集合（root filesystem）</li><li>分层的，并且每一层都可以添加、改变、删除文件，成为一个新的 image</li><li>不同的 image 可以共享相同的 layer</li><li>Image 本身是 read-only 的</li></ul><h3 id="2image-的获取"><a class="markdownIt-Anchor" href="#2image-的获取"></a> 2.Image 的获取</h3><p>（1）自行编译创建</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#1.创建Dockerfile</span></span><br><span class="line">FROM ubuntu:14.04</span><br><span class="line">LABEL maintainer=<span class="string">"zqunor@gmail.com"</span></span><br><span class="line">RUN apt-get update &amp;&amp; aput-get install -y redis-server</span><br><span class="line">EXPOSE 6379</span><br><span class="line"><span class="comment">#启动redis-server</span></span><br><span class="line">ENTRYPOINT [ <span class="string">"/usr/bin/redis-server"</span> ]</span><br><span class="line"></span><br><span class="line"><span class="comment">#2.build镜像 .表示根据当前目录下的Dockerfile进行build</span></span><br><span class="line">docker built -t zqunor/redis:latest .</span><br><span class="line"><span class="comment">#5行Dockerfile，对应5步build过程，5步对应5层</span></span><br></pre></td></tr></table></figure><p>（2）从 Docker 仓库中拉取</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker pull ubuntu:18.04</span><br></pre></td></tr></table></figure><h3 id="3创建-image"><a class="markdownIt-Anchor" href="#3创建-image"></a> 3.创建 Image</h3><p>每一个 base image 都是一个可执行文件，执行 image 就是创建一个容器</p><p>（1）创建 docker 用户组和用户，避免每次使用 docker 命令都需要加 sudo</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#创建docker用户组</span></span><br><span class="line">sudo groupadd docker</span><br><span class="line"></span><br><span class="line"><span class="comment">#将当前用户家到docker的用户组中，使当前用户有使用docker的权限</span></span><br><span class="line">sudo gpasswd -a vagrant docker</span><br><span class="line"></span><br><span class="line"><span class="comment">#创建完成之后，重启docker服务</span></span><br><span class="line">sudo service docker restart</span><br></pre></td></tr></table></figure><p>（2）Image 体验: hello-world</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#获取镜像</span></span><br><span class="line">docker pull hello-world</span><br><span class="line"><span class="comment">#查看镜像的基本信息</span></span><br><span class="line">docker image ls</span><br><span class="line"><span class="comment">#执行image,创建容器</span></span><br><span class="line">docker hello-world</span><br></pre></td></tr></table></figure><p>（3）制作 Image</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#准备工作：创建一个单独的目录，编写.c文件，并执行生成可执行文件，将这个可执行文件</span></span><br><span class="line">vim hello.c</span><br><span class="line"><span class="comment">#include&lt;stdio.h&gt;</span></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"hello docker\n"</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">#配置c语言执行环境</span></span><br><span class="line">sudo yum install gcc glibc-static</span><br><span class="line"><span class="comment">#执行生成可执行程序hello</span></span><br><span class="line">gcc -static hello.c -o hello</span><br><span class="line"><span class="comment">#生成可执行文件以后，就可以在当前目录下创建Dockerfile文件</span></span><br><span class="line">FROM scratch</span><br><span class="line">ADD hello /</span><br><span class="line">CMD [<span class="string">"/hello"</span>]</span><br><span class="line"><span class="comment">#build成image</span></span><br><span class="line">docker build -t zqunor/hello-world .</span><br><span class="line"><span class="comment">#查看image层级</span></span><br><span class="line">docker <span class="built_in">history</span> &#123;image_id&#125;</span><br><span class="line"><span class="comment">#执行image</span></span><br><span class="line">docker run zqunor/hello-world</span><br></pre></td></tr></table></figure><h2 id="容器-container"><a class="markdownIt-Anchor" href="#容器-container"></a> 容器 Container</h2><h3 id="1什么是-container"><a class="markdownIt-Anchor" href="#1什么是-container"></a> 1.什么是 Container</h3><ul><li>通过 Image 创建（copy）</li><li>在 Image layer 之上建立一个 container layer(可读写)</li><li>类比面向对象：类和实例</li><li>Image 负责 app 的存储和分发，Container 负责运行 app</li></ul><h3 id="2相关命令"><a class="markdownIt-Anchor" href="#2相关命令"></a> 2.相关命令</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看当前所有的容器，运行中和退出的</span></span><br><span class="line">docker container ls -a = docker ps -a</span><br><span class="line"><span class="comment">#查看正在运行的所有容器</span></span><br><span class="line">docker containrt ls = docker ps</span><br><span class="line"><span class="comment">#删除一个容器</span></span><br><span class="line">docker container rm &#123;container_id&#125; = docker rm &#123;container_id&#125;</span><br><span class="line"><span class="comment">#删除一个镜像</span></span><br><span class="line">docker image rm &#123;image_id&#125; = docker rmi &#123;image_id&#125;</span><br><span class="line"><span class="comment">#列出所有的container id</span></span><br><span class="line">docker container ls -aq = docke container ls -a | awk &#123;<span class="string">'print$1'</span>&#125;</span><br><span class="line"><span class="comment">#删除所有的container</span></span><br><span class="line">docker rm $(docker container ls -aq)</span><br><span class="line"><span class="comment">#删除所有退出的container</span></span><br><span class="line">docker rm $(docker container ls -f <span class="string">"status=existed"</span> -q) = docker container prune</span><br></pre></td></tr></table></figure><h2 id="构建-docker-镜像"><a class="markdownIt-Anchor" href="#构建-docker-镜像"></a> 构建 Docker 镜像</h2><p>(1)建立在已有的 image 上，创建 container, 然后在 container 中执行操作，操作完后，创建一个新的 image【不推荐，不安全】</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker commit &#123;container_name&#125; &#123;repository:tag&#125;</span><br></pre></td></tr></table></figure><p>这样生成的 image 通过<code>docker history {container_id}</code>可以看到有相同的历史版本信息</p><p>(2)通过 Dockerfile 中的配置命令进行构建【推荐，安全】</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">&gt; <span class="built_in">cd</span> docker-centos-vim</span><br><span class="line">&gt; vim Dockerfile</span><br><span class="line"></span><br><span class="line">FROM centos</span><br><span class="line">RUN yum install vim</span><br><span class="line"></span><br><span class="line">&gt; docker build -t zqunor/centos-vim-new .</span><br></pre></td></tr></table></figure><p>分析：</p><p>Dockerfile 构建镜像过程中，执行安装操作时对 image 的写操作，而 image 本身是只读的，所以在执行安装 vim 之前，先创建了临时的 container, 然后在 container 中执行安装 vim 的命令，之后再删除临时的 container，安装完成后再 commit 一个新的镜像。</p><h2 id="dockerfile-语法梳理"><a class="markdownIt-Anchor" href="#dockerfile-语法梳理"></a> Dockerfile 语法梳理</h2><h3 id="关键字"><a class="markdownIt-Anchor" href="#关键字"></a> 关键字</h3><p>(1) FROM image 来源</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">FROM scratch  <span class="comment"># 制作base image</span></span><br><span class="line"></span><br><span class="line">FROM centos <span class="comment"># 使用base image</span></span><br></pre></td></tr></table></figure><p>尽量使用官方的 image 作为 base image，安全</p><p>(2) LABEL 标签备注记录信息</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">LABEL maintaner=<span class="string">"xiaoquwl@gmail.com"</span></span><br><span class="line">LABEL verion=<span class="string">"1.0"</span></span><br><span class="line">LABEL description=<span class="string">"This is description"</span></span><br></pre></td></tr></table></figure><p>备注 image 的基本信息。</p><p>(3) RUN 执行命令,并创建新的 Image Layer</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">RUN yum update &amp;&amp; yum install -y vim \</span><br><span class="line">    python-dev <span class="comment"># 反斜线换行</span></span><br></pre></td></tr></table></figure><p>执行命令，每 run 一次，就会产生一个分层，为了避免产生无用分层，应该合并成一个 run，负责的 RUN 使用反斜线&quot;&quot;换行</p><p>(4) WORKDIR 跳转或创建目录</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">WORKDIE /<span class="built_in">test</span></span><br><span class="line">WORKDIR tmp</span><br><span class="line">RUN <span class="built_in">pwd</span></span><br><span class="line"><span class="comment"># 输出/test/tmp</span></span><br></pre></td></tr></table></figure><p>进入到相应目录下，没有的话就创建该目录。</p><p>尽量使用绝对路径，并且不要用<code>RUN cd</code>跳转目录</p><p>(5) ADD and COPY 拷贝</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">WORKDIR /root</span><br><span class="line">ADD hello <span class="built_in">test</span>/</span><br><span class="line"><span class="comment"># /root/test/hello</span></span><br><span class="line"></span><br><span class="line">WORKDIR /root</span><br><span class="line">COPY hello <span class="built_in">test</span>/</span><br><span class="line"><span class="comment"># ?? /root/test/hello</span></span><br></pre></td></tr></table></figure><ul><li><p>ADD|COPY：把本地的文件添加到 docker 中</p></li><li><p>ADD：</p><ul><li>如果是压缩文件，会添加到 docker 并解压；</li><li>如果添加的目录不存在，会创建该目录</li></ul></li><li><p>COPY</p><ul><li>大部分情况，COPY 由于 ADD</li></ul></li><li><p>添加远程文件/目录要使用 curl 或者 wget</p></li></ul><p>(6) ENV 设置常量，环境变量</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">ENV MYSQL_VERSION 5.6</span><br><span class="line">RUN apt-get install -y mysql-server= <span class="string">"<span class="variable">$&#123;MYSQL_VERSION&#125;</span>"</span> \</span><br><span class="line">    &amp;&amp; rm -rf /var/lib/apt/lists/*</span><br><span class="line"><span class="comment"># 引用常量，在RUN中可以直接使用</span></span><br></pre></td></tr></table></figure><p>(7) VOLUME and EXPOSE 存储和网络</p><p>(8) CMD and ENTRYPOINT</p><ul><li><p>CMD：设置容器启动后默认执行的命令和参数</p><ul><li>容器启动时默认执行的命令</li><li>如果 docker run 指定了其他命令，CMD 命令被忽视</li><li>如果定义了多个 CMD，只有最后一个会执行</li></ul></li><li><p>ENTRYPOINT：设置容器启动时运行的命令</p><ul><li>让容器以应用程序或者服务的形式运行</li><li>不会被忽略，一定会执行</li></ul></li></ul><h3 id="dockerfile-格式-shell-格式和-exec-格式"><a class="markdownIt-Anchor" href="#dockerfile-格式-shell-格式和-exec-格式"></a> Dockerfile 格式： Shell 格式和 Exec 格式</h3><p>(1) 基本样式</p><ul><li>Shell 格式</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">RUN apt-get install -y vim</span><br><span class="line">CMD <span class="built_in">echo</span> <span class="string">"hello docker"</span></span><br><span class="line">ENTRYPOINT <span class="built_in">echo</span> <span class="string">"hello docker"</span></span><br><span class="line"><span class="comment">#输出 hello docker</span></span><br></pre></td></tr></table></figure><ul><li>Exec 格式</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">RUN [ <span class="string">"apt-get"</span>, <span class="string">"install"</span>, <span class="string">"-y"</span>, <span class="string">"vim"</span> ]</span><br><span class="line">CMD [ <span class="string">"/bin/echo"</span>, <span class="string">"hello docker"</span> ]</span><br><span class="line">ENTRYPOINT [ <span class="string">"/bin/bash"</span>, <span class="string">"-c"</span>, <span class="string">"echo hello <span class="variable">$name</span>"</span> ]</span><br><span class="line"><span class="comment">#输出 hello docker</span></span><br><span class="line"></span><br><span class="line">ENTRYPOINT [<span class="string">"/bin/echo"</span>, <span class="string">"hello <span class="variable">$name</span>"</span>]</span><br><span class="line"><span class="comment">#输出hello $name [echo指令未被识别]</span></span><br></pre></td></tr></table></figure><p>(2) shell 格式示例</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">FROM centos</span><br><span class="line">ENV name Docker</span><br><span class="line">ENTRYPOINT <span class="built_in">echo</span> <span class="string">"hello <span class="variable">$&#123;name&#125;</span>"</span></span><br></pre></td></tr></table></figure><h2 id="遇到的问题"><a class="markdownIt-Anchor" href="#遇到的问题"></a> 遇到的问题</h2><h3 id="1编辑-vagrantfile设置启动虚拟机时自动执行的命令但是进入到虚拟机中后docker-没有启动手动启动-docker-后有报错"><a class="markdownIt-Anchor" href="#1编辑-vagrantfile设置启动虚拟机时自动执行的命令但是进入到虚拟机中后docker-没有启动手动启动-docker-后有报错"></a> 1.编辑 Vagrantfile，设置启动虚拟机时自动执行的命令，但是进入到虚拟机中后，docker 没有启动，手动启动 docker 后，有报错:</h3><p><code>Got permission denied while trying to connect to the Docker daemon socket at unix:///var/run/docker.sock: Get http://%2Fvar%2Frun%2Fdocker.sock/v1.39/version: dial unix /var/run/docker.sock: connect: permission denied</code></p><p>=&gt; 需要 执行 <code>sudo docker version</code> 查看 docker 服务端信息</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;系统学习-docker-践行-devops-理念&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#系统学习-docker-践行-devops-理念&quot;&gt;&lt;/a&gt; 系统学习 Docker 践行 DevOps 理念&lt;/h1&gt;
&lt;h2 id=&quot;v
      
    
    </summary>
    
      <category term="Docker" scheme="https://blog.i2arch.tech/categories/Docker/"/>
    
      <category term="linux" scheme="https://blog.i2arch.tech/categories/Docker/linux/"/>
    
    
      <category term="Docker" scheme="https://blog.i2arch.tech/tags/Docker/"/>
    
      <category term="DevOps" scheme="https://blog.i2arch.tech/tags/DevOps/"/>
    
  </entry>
  
  <entry>
    <title>【HTTP】原理结合实践</title>
    <link href="https://blog.i2arch.tech/2018/08/30/http/"/>
    <id>https://blog.i2arch.tech/2018/08/30/http/</id>
    <published>2018-08-30T04:00:00.000Z</published>
    <updated>2018-11-16T12:14:51.912Z</updated>
    
    <content type="html"><![CDATA[<p>理解网络请求的原理，对 http 请求头、请求过程、请求方式和数据传输原理进行学习。HTTP 网络请求是交互式 web 应用的根基。在能够使用的基础上，进一步理解实现原理，从而强化对 HTTP 协议的理解，和基于该协议上的网络实现。</p><a id="more"></a><h3 id="http-请求头信息"><a class="markdownIt-Anchor" href="#http-请求头信息"></a> HTTP 请求头信息</h3><p>Cache-Control</p><ul><li>public、private：设置缓存仅在客户端缓存还是可以在代理服务器中缓存</li><li>must-revalidate：客户端的缓存获取之后必须到服务端验证之后才能继续使用缓存</li><li>no-cache、no-store：设置是否使用缓存</li></ul><p>缓存验证</p><ul><li><p>last-modified 配合 if-modified-since</p></li><li><p>etag 配合 if-none-match</p></li><li><p>Contetn-Type 、Content-Encoding：等用来约束数据类型</p></li><li><p>Cookie：保持会话信息</p></li><li><p>CORS：实现跨域并保持安全性限制</p></li></ul><h2 id="深入到-tcp"><a class="markdownIt-Anchor" href="#深入到-tcp"></a> 深入到 TCP</h2><p>1.什么是三次握手</p><p>2.HTTPS 链接的创建过程，以及为什么 HTTPS 是安全的</p><p>3.什么是长链接，为什么需要长链接</p><p>4.HTTP2 的信道复用为什么能提高性能</p><h2 id="http-请求完整过程"><a class="markdownIt-Anchor" href="#http-请求完整过程"></a> HTTP 请求完整过程</h2><p><img src="https://ws1.sinaimg.cn/large/005EgYNMgy1fuqog8z9uij318n0f30xa.jpg" alt=""></p><p>1.Redirect 浏览器要对 url 判断是否需要重定向到新的 url 地址</p><p>2.缓存 判断是否需要到浏览器缓存中获取 url 对应的网页资源</p><p>3.DNS 解析 将域名解析成 ip 才能访问到资源所在的服务器</p><p>4.TCP 连接 拿到 ip 后就要进行 TCP 连接[ http: 经过 TCP 的三次握手;https: 不同于三次握手，需要另外的保证数据安全传输的过程]</p><p>5.Request 发送请求的数据包</p><p>6.Response 接收到请求数据包，并返回响应结果</p><h2 id="网络协议分层"><a class="markdownIt-Anchor" href="#网络协议分层"></a> 网络协议分层</h2><p><img src="https://ws1.sinaimg.cn/large/005EgYNMgy1fuqoss50zpj30zv0pogpe.jpg" alt=""></p><ul><li>物理层：定义物理设备如何传输数据</li><li>数据链路层：在通信的实体间建立数据链路连接</li><li>网络层：为数据在结点之间传输创建逻辑链路</li><li>传输层：<ul><li>向用户传输可靠的端到端(End-to-End)服务</li><li>传输层向高层屏蔽了下层数据通信的细节</li></ul></li><li>应用层<ul><li>为应用软件提供了很多服务</li><li>构建于 TCP 协议之上</li><li>屏蔽网络传输相关细节</li></ul></li></ul><h2 id="http-发展历史"><a class="markdownIt-Anchor" href="#http-发展历史"></a> HTTP 发展历史</h2><h3 id="几个概念"><a class="markdownIt-Anchor" href="#几个概念"></a> 几个概念</h3><p>1.http 并发数</p><p>2.http 请求</p><p>3.tcp 连接 [1 个连接可以有多个 http 请求]</p><h3 id="http09"><a class="markdownIt-Anchor" href="#http09"></a> HTTP/0.9</h3><ul><li>只有一个命令(方法)GET</li><li>没有 HEADER 等描述数据的信息</li><li>服务器发送完毕，就关闭 TCP 连接</li></ul><h3 id="http10"><a class="markdownIt-Anchor" href="#http10"></a> HTTP/1.0</h3><ul><li>增加了很多命令</li><li>增加 status code(服务端处理完某个请求后的状态)和 header(发送和请求相关的数据 以及对这部分数据的操作方法))</li><li>多字符支持、多部分发送、权限、缓存等</li></ul><p>[一个 http 请求就要建立一个 tcp 连接，返回完内容后就关闭，但是三次握手建立的消耗和延迟比较高]</p><h3 id="http11"><a class="markdownIt-Anchor" href="#http11"></a> HTTP/1.1</h3><ul><li>增加持久连接 [建立完一个 tcp 连接后，在返回完数据后不关闭，让后续的所有请求都使用这个连接]</li><li>pipeline [可以在同一个连接中发送多个请求，但是多个请求在服务器端需要顺序处理（串行），这样如果前面的请求处理的时间需要很长，而后面的请求处理的时间只需要很短，导致性能不太好]</li><li>增加 host(可以在同一台(物理)服务器跑多个 web 服务[node、java]，使用 host 进行判断，提高物理服务的使用效率)和其他一些命令</li></ul><h3 id="http20"><a class="markdownIt-Anchor" href="#http20"></a> HTTP/2.0</h3><ul><li>分帧传输：所有数据以二进制(帧)传输[以前是以字符串形式传输]</li><li>信道复用：同一个连接里面发送多个请求不再需要需要按照顺序来（并行）</li><li>头信息压缩以及推送等提高效率的功能</li></ul><h2 id="http-三次握手"><a class="markdownIt-Anchor" href="#http-三次握手"></a> HTTP 三次握手</h2><h3 id="http-请求和-tcp-连接关系"><a class="markdownIt-Anchor" href="#http-请求和-tcp-连接关系"></a> HTTP 请求和 TCP 连接关系</h3><p><img src="https://ws1.sinaimg.cn/large/005EgYNMgy1fuqq6thfa2j30yy0eyjs6.jpg" alt=""></p><p>1.客户端和服务器之间的 http 请求的发送和返回的过程需要创建 TCP 连接</p><p>2.http 只有请求和响应的概念，没有连接的概念</p><p>3.请求和响应都是数据包，需要传输通道，TCP 连接就是数据包的传输通道（TCP 连接: 客户端发送，服务端接收）</p><h3 id="三次握手时序图"><a class="markdownIt-Anchor" href="#三次握手时序图"></a> 三次握手时序图</h3><p><img src="https://ws1.sinaimg.cn/large/005EgYNMgy1fuqq70zeilj30tw0p4zmm.jpg" alt=""></p><p>1.第一次握手（SYN=1,Seq=X）</p><p>2.第二次握手（SYN=1,ACK=X+1,Seq=Y）</p><p>3.第三次握手（ACK=Y+1,Seq=Z）</p><p>网络抓包工具：wireshark</p><h2 id="uri-url-urn"><a class="markdownIt-Anchor" href="#uri-url-urn"></a> URI URL URN</h2><h3 id="uri"><a class="markdownIt-Anchor" href="#uri"></a> URI</h3><p>【定义】： Uniform Resource Identifier 统一资源标识符</p><ul><li>用来唯一标识互联网上的信息资源</li><li>包括 URL 和 URN</li></ul><h3 id="url"><a class="markdownIt-Anchor" href="#url"></a> URL</h3><p>【定义】：Uniform Resource Locator 统一资源定位器</p><p>示例： <a href="http://user:pass@host.com:80/path?query=string#hash" target="_blank" rel="noopener">http://user:pass@host.com:80/path?query=string#hash</a></p><ul><li><p><strong>[ http ]</strong>：传送协议。<code>Data URI scheme</code> [http ftp mailto]</p></li><li><p><strong>[ // ]</strong>：层级 URL 标记符号(为[//],固定不变)</p></li><li><p><strong>[ user:pass ]</strong>： 访问资源需要的凭证信息（可省略，不安全）</p></li><li><p><strong>[ <a href="http://host.com" target="_blank" rel="noopener">host.com</a> ]</strong>：服务器。（通常为域名，有时为 IP 地址）</p></li><li><p><strong>[ 80 ]</strong>：端口号。（以数字方式表示，若为 HTTP 的默认值“:80”可省略）</p></li><li><p><strong>[ /path ]</strong>：路径。（以“/”字符区别路径中的每一个目录名称）</p></li><li><p><strong>[ ?query=string ]</strong>：查询。（GET 模式的窗体参数，以“?”字符为起点，每个参数以“&amp;”隔开，再以“=”分开参数名称与数据，通常以 UTF8 的 URL 编码，避开字符冲突的问题）</p></li><li><p><strong>[ #hash ]</strong>：片段。以“#”字符为起点。（锚点应用）</p></li></ul><h3 id="urn"><a class="markdownIt-Anchor" href="#urn"></a> URN</h3><p>【定义】：永久统一资源定位符</p><p>资源移动之后还能被找到</p><p>目前还没有成熟的使用方案</p><h2 id="http-报文"><a class="markdownIt-Anchor" href="#http-报文"></a> HTTP 报文</h2><ul><li><p>请求行（request line）</p><ul><li>请求报文：请求方式 请求参数 请求的网络协议</li><li>响应报文：请求的网络协议 状态码 状态信息</li></ul></li><li><p><a href="https://zh.wikipedia.org/wiki/HTTP%E5%A4%B4%E5%AD%97%E6%AE%B5" target="_blank" rel="noopener">请求头部</a></p></li><li><p>空行</p></li><li><p>主体数据[可选]</p></li></ul><h3 id="请求报文"><a class="markdownIt-Anchor" href="#请求报文"></a> 请求报文</h3><h3 id="响应报文"><a class="markdownIt-Anchor" href="#响应报文"></a> 响应报文</h3><h2 id="cors-跨域请求的限制与解决"><a class="markdownIt-Anchor" href="#cors-跨域请求的限制与解决"></a> CORS 跨域请求的限制与解决</h2><h3 id="基本介绍"><a class="markdownIt-Anchor" href="#基本介绍"></a> 基本介绍</h3><p>CORS (<code>Cross-origin resource sharing</code>):<br>一个 web 服务器程序(域名)请求另一个 web 服务器程序(域名).</p><p><code>images, stylesheets, scripts, iframes, and videos</code>这几个资源的请求是不涉及跨域的概念的，都可以直接访问。</p><p>跨域请求时，请求发送的依然是正常的，并且服务器仍然会正常响应，但是浏览器接收到数据后看到头信息没有跨域处理，就会忽略掉请求的数据，并在控制命令行报错</p><h3 id="限制"><a class="markdownIt-Anchor" href="#限制"></a> 限制</h3><p>1.跨域请求只允许的方法：GET HEAD POST</p><p>2.允许 Content-Type 的值</p><p>(1) text/plain</p><p>(2) multipart/form-data</p><p>(3) application/x-www-form-urlencoded</p><p>3.请求头限制</p><p>(1) Accept</p><p>(2) Accept-Language</p><p>(3) Content-Language</p><p>4.XMLHttpRequestUpload 对象均没有注册任何事件监听器</p><p>5.请求中没有使用 ReadableStream 对象</p><h3 id="解决"><a class="markdownIt-Anchor" href="#解决"></a> 解决</h3><p>1.设置 header。</p><p>对<strong>被请求</strong>的资源设置<code>'Access-Control-Allow-Origin': '*'</code>头信息（不安全）[但是值只能是字符串，所以涉及到多个 url 时，可以通过对请求 url 的判断来确定是否需要设置该头信息]</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// server2.js</span></span><br><span class="line">response.writeHead(<span class="number">200</span>, &#123;</span><br><span class="line">  <span class="string">"Access-Control-Allow-Origin"</span>: <span class="string">"*"</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>2.<code>jsonp</code> 实现方法：在 script 标签设置 src 的值为需要请求的跨域 url</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// test.html</span></span><br><span class="line">&lt;script src=<span class="string">"http://127.0.0.1:8887/"</span> /&gt;</span><br></pre></td></tr></table></figure><h3 id="cors-预请求"><a class="markdownIt-Anchor" href="#cors-预请求"></a> CORS 预请求</h3><p>对跨域限制允许外的请求参数都需要进行验证，才能有效请求，这个过程就是预请求</p><p>验证方法: 多发送一个请求的方式为 OPTIONS 的请求验证</p><p>1、<code>Access-Control-Allow-Headers</code></p><p>使用 fetch 请求服务器时设置了头信息，相应的服务器端需要对请求的头信息进行配置</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// test.html</span></span><br><span class="line">fetch(<span class="string">"http://127.0.0.1:8887/"</span>, &#123;</span><br><span class="line">  method: <span class="string">"POST"</span>,</span><br><span class="line">  headers: &#123;</span><br><span class="line">    <span class="string">"X-Test-Cors"</span>: <span class="string">"123"</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// server2.js</span></span><br><span class="line"><span class="string">'Access-Control-Allow-Headers'</span>: <span class="string">'X-Test-Cors'</span></span><br></pre></td></tr></table></figure><p>2、 <code>Access-Control-Allow-Methods</code></p><p>默认支持的请求方式只允许 <code>GET HEAD POST</code>，如果需要使用其他方式请求，则需要在服务器端的头信息中设置相关参数。设置允许用户可以请求的请求方式:</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">'Access-Control-Allow-Methods'</span>: <span class="string">'POST, PUT, DELETE'</span></span><br></pre></td></tr></table></figure><p>3、<code>Access-Control-Max-Age</code></p><p>设置同一个跨域请求的有效期, 有效期内不需要再发送预请求进行验证。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">'Access-Control-Max-Age'</span>: <span class="string">'1000'</span></span><br></pre></td></tr></table></figure><h2 id="缓存头-cache-control"><a class="markdownIt-Anchor" href="#缓存头-cache-control"></a> 缓存头 Cache-Control</h2><h3 id="可缓存性"><a class="markdownIt-Anchor" href="#可缓存性"></a> 可缓存性</h3><ul><li>public [代理服务器和客户端都可以缓存返回内容]</li><li>private [只有发起请求的客户端才可以缓存数据]</li><li>no-cache [所有的节点都不可以缓存数据]</li></ul><h3 id="到期"><a class="markdownIt-Anchor" href="#到期"></a> 到期</h3><ul><li>max-age=[seconds][单位：秒]</li><li>s-maxage=[seconds][代理服务器中才生效]</li><li>max-stale=[seconds][浏览器设置的，可以使用过期的缓存，而不用再到服务器请求]</li></ul><h3 id="重新验证"><a class="markdownIt-Anchor" href="#重新验证"></a> 重新验证</h3><ul><li>must-revalidate [缓存过期后拿到缓存不能直接用，必须到服务器端验证后才能用]</li><li>proxy-revalidate [指定<strong>缓存服务器</strong>失效后必须验证]</li></ul><h3 id="其他"><a class="markdownIt-Anchor" href="#其他"></a> 其他</h3><ul><li>no-store [浏览器和代理服务器都不可以缓存数据，必须要到服务器端验证]</li><li>no-transform [不允许代理服务器修改服务器返回的内容]</li></ul><h3 id="缓存应用"><a class="markdownIt-Anchor" href="#缓存应用"></a> 缓存应用</h3><h2 id="资源验证"><a class="markdownIt-Anchor" href="#资源验证"></a> 资源验证</h2><p>http 请求使用缓存的流程：</p><p><img src="https://ws1.sinaimg.cn/large/005EgYNMgy1fuw35pe2jrj30xd0fb0ub.jpg" alt=""></p><h3 id="验证头"><a class="markdownIt-Anchor" href="#验证头"></a> 验证头</h3><ul><li>Last-Modified 上次修改时间</li></ul><p>配合 If-Modified-Since[第二次请求时携带，值为第一次请求后响应的 Last-Modified 的值]或者 If-Unmodified-Since 使用</p><p>对比上次修改时间以验证资源是否需要更新</p><ul><li>Etag</li></ul><p>通过数据签名[资源对内容的唯一签名]</p><p>If-None-Match 第二次请求时携带，值为第一次请求后响应的 Etag 的值</p><h2 id="cookie"><a class="markdownIt-Anchor" href="#cookie"></a> Cookie</h2><p>Set-Cookie 设置<br>下次请求会自动带上<br>键值对，可以设置多个</p><p>Cookie 属性</p><ul><li>max-age 和 expires 设置过期时间</li><li>Secure 只在 https 的时候发送</li><li>HttpOnly 无法通过 document.cookie 访问</li></ul><h2 id="redirect"><a class="markdownIt-Anchor" href="#redirect"></a> Redirect</h2><p>301永久重定向，访问一次重定向后，以后即使将重定向的代码删除，也依然进行重定向跳转（清理浏览器缓存后重定向才失效）</p><p>302重定向是每次都要判断然后重定向到相应的url</p><h2 id="参考地址"><a class="markdownIt-Anchor" href="#参考地址"></a> 参考地址：</h2><ol><li><a href="https://zh.wikipedia.org/wiki/%E7%BB%9F%E4%B8%80%E8%B5%84%E6%BA%90%E5%AE%9A%E4%BD%8D%E7%AC%A6" target="_blank" rel="noopener">wikipedia-统一资源定位符</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;理解网络请求的原理，对 http 请求头、请求过程、请求方式和数据传输原理进行学习。HTTP 网络请求是交互式 web 应用的根基。在能够使用的基础上，进一步理解实现原理，从而强化对 HTTP 协议的理解，和基于该协议上的网络实现。&lt;/p&gt;
    
    </summary>
    
      <category term="HTTP" scheme="https://blog.i2arch.tech/categories/HTTP/"/>
    
    
      <category term="http" scheme="https://blog.i2arch.tech/tags/http/"/>
    
  </entry>
  
  <entry>
    <title>【Git】修改已经提交的commit内容</title>
    <link href="https://blog.i2arch.tech/2018/08/13/git-modify-commit-message/"/>
    <id>https://blog.i2arch.tech/2018/08/13/git-modify-commit-message/</id>
    <published>2018-08-13T09:39:15.000Z</published>
    <updated>2018-11-16T12:14:51.883Z</updated>
    
    <content type="html"><![CDATA[<p>通过 Git 进行版本管理时，对于已经提交但没有 push 的 message 信息，发现提交信息填写错误后，如何进行修改？<br>对于已经 push 的 message 信息如何修改？通过<code>git rebase -i</code>进行分支管理，以及重新操作已经提交的分支信息[reword,edit,squash 等]。此次用到的主要是<code>reword</code>修改已经提交的<code>message</code>信息。</p><a id="more"></a><h2 id="修改已经-commit-但没有-push-的-commit-message"><a class="markdownIt-Anchor" href="#修改已经-commit-但没有-push-的-commit-message"></a> 修改已经 commit 但没有 push 的 commit message</h2><h3 id="查看提交历史"><a class="markdownIt-Anchor" href="#查看提交历史"></a> 查看提交历史</h3><p><code>git log --oneline -10</code></p><blockquote><p><code>--onlien</code>的方式能够显示精简的日志信息</p></blockquote><p>显示的信息[当前分支为<code>zzz</code>]：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">15af769 (HEAD -&gt; zzz) 10-15 通过模型自动写入时间戳[补充order模型隐藏字段的设置]</span><br><span class="line">197fcdd 10-13 测试下14 测试下单接口, 修改程序错误</span><br><span class="line">fdeb6af 10-13 一对多关系的新增操作[完成下单接口方法]</span><br><span class="line">da0bd4e 10-13 订单创建[添加订单信息到order order_product表]</span><br><span class="line">5ab5068 10-11 订单快照的实现</span><br><span class="line">09c2116 10-10 订单快照的业务分析</span><br><span class="line">8493571 10-9 下单接口说明文档补充注释</span><br><span class="line">6edda7e (origin/develop) 下单接口业务模型</span><br><span class="line">60b8f01 10-7 编写一个复杂的验证器</span><br><span class="line">3e8375c 10-4|5|6 下单与支付流程 + 重构权限控制前置方法</span><br></pre></td></tr></table></figure><p>发现提交的信息中：</p><ul><li><p><code>6edda7e</code>的信息中没有加标题序号</p></li><li><p><code>da0bd4e</code>的信息中标题序号错误</p></li><li><p><code>197fcdd</code>的信息中标题序号和内容有误</p></li></ul><h3 id="通过git-rebase-i编辑提交的历史"><a class="markdownIt-Anchor" href="#通过git-rebase-i编辑提交的历史"></a> 通过<code>git rebase -i</code>编辑提交的历史</h3><blockquote><p><code>git-rebase - Reapply commits on top of another base tip</code> [重新应用提交到另一个基础提示之上]</p></blockquote><p>在上面的日志中可以看到<code>6edda7e</code>为已经 push 的分支了，暂时不介绍这个，现在需要修改<code>da0bd4e</code>和 <code>197fcdd</code>两个提交的分支上的<code>message</code>内容。</p><p>（1）编辑最久远的需要修改的分支的前一个分支上</p><p><code>git rebase -i 60b8f01</code></p><p>显示的内容：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">pick 6edda7e 下单接口业务模型</span><br><span class="line">pick 8493571 10-9 下单接口说明文档补充注释</span><br><span class="line">pick 09c2116 10-10 订单快照的业务分析</span><br><span class="line">pick 5ab5068 10-11 订单快照的实现</span><br><span class="line">pick da0bd4e 10-13 订单创建[添加订单信息到order order_product表]</span><br><span class="line">pick fdeb6af 10-13 一对多关系的新增操作[完成下单接口方法]</span><br><span class="line">pick 197fcdd 10-13 测试下14 测试下单接口, 修改程序错误</span><br><span class="line">pick 15af769 10-15 通过模型自动写入时间戳[补充order模型隐藏字段的设置]</span><br></pre></td></tr></table></figure><p>并且下方会提示修改建议：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Commands:</span></span><br><span class="line"><span class="comment"># p, pick &lt;commit&gt; = use commit</span></span><br><span class="line"><span class="comment"># r, reword &lt;commit&gt; = use commit, but edit the commit message</span></span><br><span class="line"><span class="comment"># e, edit &lt;commit&gt; = use commit, but stop for amending</span></span><br><span class="line"><span class="comment"># s, squash &lt;commit&gt; = use commit, but meld into previous commit</span></span><br><span class="line"><span class="comment"># f, fixup &lt;commit&gt; = like "squash", but discard this commit's log message</span></span><br><span class="line"><span class="comment"># x, exec &lt;command&gt; = run command (the rest of the line) using shell</span></span><br><span class="line"><span class="comment"># d, drop &lt;commit&gt; = remove commit</span></span><br><span class="line"><span class="comment"># l, label &lt;label&gt; = label current HEAD with a name</span></span><br><span class="line"><span class="comment"># t, reset &lt;label&gt; = reset HEAD to a label</span></span><br><span class="line"><span class="comment"># m, merge [-C &lt;commit&gt; | -c &lt;commit&gt;] &lt;label&gt; [# &lt;oneline&gt;]</span></span><br></pre></td></tr></table></figure><p>我们需要操作的是重新编辑已经提交的分支记录的 message 信息，所以对应的应该是<code>reword</code>,简写为<code>r</code>。</p><p>（2）修改显示的内容，将<code>pick</code>修改为<code>reword</code> [保留提交的分支记录，但是编辑提交的信息]</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">r 6edda7e 下单接口业务模型</span><br><span class="line">pick 8493571 10-9 下单接口说明文档补充注释</span><br><span class="line">pick 09c2116 10-10 订单快照的业务分析</span><br><span class="line">pick 5ab5068 10-11 订单快照的实现</span><br><span class="line">r da0bd4e 10-13 订单创建[添加订单信息到order order_product表]</span><br><span class="line">pick fdeb6af 10-13 一对多关系的新增操作[完成下单接口方法]</span><br><span class="line">r 197fcdd 10-13 测试下14 测试下单接口, 修改程序错误</span><br><span class="line">pick 15af769 10-15 通过模型自动写入时间戳[补充order模型隐藏字段的设置]</span><br></pre></td></tr></table></figure><p>将需要修改的记录前的 <code>pick</code> 改为 <code>r</code>，然后<code>:wq</code>保存退出后，会<strong>按顺序</strong>自动进入需要编辑的提交信息框</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">下单接口业务模型</span><br><span class="line"></span><br><span class="line"><span class="comment"># Please enter the commit message for your changes. Lines starting</span></span><br><span class="line"><span class="comment"># with '#' will be ignored, and an empty message aborts the commit.</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Date:      Wed Aug 8 20:08:03 2018 +0800</span></span><br></pre></td></tr></table></figure><p>然后将第一行的提交信息修改为需要设置的信息，然后<code>:wq</code>保存退出，进入下一条需要编辑的提交记录。将全部需要修改的分支信息依次修改完成后，保存退出后会出现下面的信息，表示提交成功。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">[detached HEAD 91d973f] 10-8|9 下单接口业务模型</span><br><span class="line">Date: Wed Aug 8 20:08:03 2018 +0800</span><br><span class="line">3 files changed, 252 insertions(+), 1 deletion(-)</span><br><span class="line">create mode 100644 application/api/service/Order.php</span><br><span class="line">[detached HEAD b007935] 10-12 订单创建[添加订单信息到order order_product表]</span><br><span class="line">4 files changed, 179 insertions(+), 23 deletions(-)</span><br><span class="line">create mode 100644 application/api/model/Order.php</span><br><span class="line">create mode 100644 application/api/model/OrderProduct.php</span><br><span class="line">[detached HEAD a5449fc] 10-14 测试下单接口, 修改程序错误</span><br><span class="line">4 files changed, 99 insertions(+), 7 deletions(-)</span><br><span class="line">Successfully rebased and updated refs/heads/develop.</span><br></pre></td></tr></table></figure><p>再次执行<code>git log --oneline -10</code>命令后，即可看到分支的信息为修改后的提交信息</p><h2 id="修改已经-push-的-commmit-message"><a class="markdownIt-Anchor" href="#修改已经-push-的-commmit-message"></a> 修改已经 push 的 commmit message</h2><p>对于已经提交的信息的分支信息操作步骤同上，只是在推送<code>push</code>的时候需要加<code>--force</code>，强制覆盖远程分支上的提交信息。</p><p>【声明】</p><p>我的博客即将搬运同步至腾讯云+社区，邀请大家一同入驻：<a href="https://cloud.tencent.com/developer/support-plan?invite_code=89fda9dsh3d0" target="_blank" rel="noopener">https://cloud.tencent.com/developer/support-plan?invite_code=89fda9dsh3d0</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;通过 Git 进行版本管理时，对于已经提交但没有 push 的 message 信息，发现提交信息填写错误后，如何进行修改？&lt;br&gt;
对于已经 push 的 message 信息如何修改？通过&lt;code&gt;git rebase -i&lt;/code&gt;进行分支管理，以及重新操作已经提交的分支信息[reword,edit,squash 等]。此次用到的主要是&lt;code&gt;reword&lt;/code&gt;修改已经提交的&lt;code&gt;message&lt;/code&gt;信息。&lt;/p&gt;
    
    </summary>
    
      <category term="Git" scheme="https://blog.i2arch.tech/categories/Git/"/>
    
    
      <category term="git" scheme="https://blog.i2arch.tech/tags/git/"/>
    
  </entry>
  
  <entry>
    <title>【React入门】实现todolist功能</title>
    <link href="https://blog.i2arch.tech/2018/08/08/js-react/"/>
    <id>https://blog.i2arch.tech/2018/08/08/js-react/</id>
    <published>2018-08-08T14:07:21.000Z</published>
    <updated>2018-11-16T12:14:51.915Z</updated>
    
    <content type="html"><![CDATA[<p>作为一名 PHP 初级的程序员，目前尚且处于学习 PHP 业务逻辑实现到日常工作中的阶段，但是由于现在想要搭建一个满意的个人博客，并且尝试过很多 hexo 主题后总是会对主题的某些或某个部分不太满意，<br>所以为了以后可以自己实现相应页面的开发，所以还是想着能够学点前端框架的知识，为以后成为全栈工程师做准备。目前比较流行的前端框架主要有<code>React.js</code>和<code>Vue.js</code>，因为当前公司使用的是<code>React.js</code>开发的，所以也选择<code>React</code>作为学习对象。</p><a id="more"></a><h2 id="开发环境准备"><a class="markdownIt-Anchor" href="#开发环境准备"></a> 开发环境准备</h2><h3 id="安装-nodejs"><a class="markdownIt-Anchor" href="#安装-nodejs"></a> 安装 node.js</h3><p>建议在 React 中使用 CommonJS 模块系统，比如 browserify 或 webpack(推荐使用)。</p><p>使用淘宝定制的 cpm 命令行工具代替默认的 npm</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">npm install -g cnpm --registry=https://registry.npm.taobao.org</span><br><span class="line">npm config <span class="built_in">set</span> registry https://registry.npm.taobao.org</span><br></pre></td></tr></table></figure><p>这样就可以使用 cnpm 命令来代替 npm 进行安装模块了：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cnpm install [name]</span><br></pre></td></tr></table></figure><h3 id="创建-react-应用"><a class="markdownIt-Anchor" href="#创建-react-应用"></a> 创建 react 应用</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">cnpm install -g create-react-app</span><br><span class="line"><span class="built_in">cd</span> todolist-react/</span><br><span class="line">npm start</span><br></pre></td></tr></table></figure><h3 id="目录结构"><a class="markdownIt-Anchor" href="#目录结构"></a> 目录结构</h3><ul><li>原始结构</li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">todolist-react/</span><br><span class="line">    node_modules/</span><br><span class="line">    public/</span><br><span class="line">        favicon.ico</span><br><span class="line">        index.html</span><br><span class="line">        manifest.json</span><br><span class="line">    src/</span><br><span class="line">        App.css</span><br><span class="line">        App.js</span><br><span class="line">        App.test.js</span><br><span class="line">        index.css</span><br><span class="line">        index.js</span><br><span class="line">        logo.svg</span><br><span class="line">    README.md</span><br><span class="line">    package-lock.json</span><br><span class="line">    package.json</span><br><span class="line">    .gitignore</span><br></pre></td></tr></table></figure><h2 id="代码实现"><a class="markdownIt-Anchor" href="#代码实现"></a> 代码实现</h2><h3 id="准备工作"><a class="markdownIt-Anchor" href="#准备工作"></a> 准备工作</h3><p>（1）编写 todolist 功能时，对默认项目的文件进行删减，只需要保留主要的文件即可。</p><p>将<code>App.js</code>重命名为<code>TodoList.js</code>，创建文件<code>TodoItem.js</code>。</p><ul><li>精简后的文件结构</li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">todolist-react/</span><br><span class="line">    node_modules/</span><br><span class="line">    public/</span><br><span class="line">        favicon.ico</span><br><span class="line">        index.html</span><br><span class="line">        manifest.json</span><br><span class="line">    src/</span><br><span class="line">        TodoList.js</span><br><span class="line">        TodoItem.js</span><br><span class="line">        index.js</span><br><span class="line">    README.md</span><br><span class="line">    package-lock.json</span><br><span class="line">    package.json</span><br><span class="line">    .gitignore</span><br></pre></td></tr></table></figure><p>（2）组件拆分</p><p><code>React</code> 的组件开发意思是将页面上每个部分作为一个组件，然后通过每个组件之间的通信，进行数据交互，实现完整页面的渲染。</p><p><code>TodoList</code> 功能包括用户输入部分和 <code>List</code> 清单部分，所以将整个功能拆分为两部分。用户输入部分为<code>TodoList</code>; <code>List</code>清单部分为<code>TodoItem</code>。</p><p>（3）<code>index.js</code>代码实现</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> React <span class="keyword">from</span> <span class="string">"react"</span>;</span><br><span class="line"><span class="keyword">import</span> ReactDOM <span class="keyword">from</span> <span class="string">"react-dom"</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 引入TodoList组件</span></span><br><span class="line"><span class="keyword">import</span> TodoList <span class="keyword">from</span> <span class="string">"./TodoList"</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取TodoList返回的数据，并渲染到id为root的元素节点中</span></span><br><span class="line">ReactDOM.render(<span class="xml"><span class="tag">&lt;<span class="name">TodoList</span> /&gt;</span>, document.getElementById("root"));</span></span><br></pre></td></tr></table></figure><p>（4）<code>TodoList.js</code>代码实现</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 引入React类，引入React.Component组件类</span></span><br><span class="line"><span class="keyword">import</span> React, &#123; Component&#125; <span class="keyword">from</span> <span class="string">'react'</span>;</span><br><span class="line"><span class="comment">// 从当前目录下的TodoItem.js中引入TodoItem子组件</span></span><br><span class="line"><span class="keyword">import</span> TodoItem <span class="keyword">from</span> <span class="string">'./TodoItem'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 组件定义和实现</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TodoList</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 通过render() 方法渲染页面数据</span></span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">      &lt;div&gt;</span><br><span class="line">        &lt;div&gt;</span><br><span class="line">          &lt;input value=&#123;<span class="keyword">this</span>.state.inputValue&#125; onChange=&#123;<span class="keyword">this</span>.handleInputChange&#125;/&gt;</span><br><span class="line">          &lt;button onClick=&#123;<span class="keyword">this</span>.handleBtnClick&#125;&gt;add&lt;<span class="regexp">/button&gt;</span></span><br><span class="line"><span class="regexp">        &lt;/</span>div&gt;</span><br><span class="line">        &lt;ul&gt;</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> (</span><br><span class="line">                <span class="keyword">this</span>.state.list.map(<span class="function">(<span class="params">item, index</span>) =&gt;</span> &#123;</span><br><span class="line">                    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">TodoItem</span> <span class="attr">key</span>=<span class="string">&#123;index&#125;</span> <span class="attr">content</span>=<span class="string">&#123;item&#125;</span> <span class="attr">index</span>=<span class="string">&#123;index&#125;</span> <span class="attr">delete</span>=<span class="string">&#123;this.handleDelete&#125;/</span>&gt;</span></span></span><br><span class="line"><span class="xml">                &#125;)</span></span><br><span class="line"><span class="xml">            )</span></span><br><span class="line"><span class="xml">        &#125;</span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span></span><br><span class="line">      &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">    );</span></span><br><span class="line"><span class="regexp">  &#125;</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br></pre></td></tr></table></figure><h3 id="未完待续"><a class="markdownIt-Anchor" href="#未完待续"></a> 未完待续</h3>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;作为一名 PHP 初级的程序员，目前尚且处于学习 PHP 业务逻辑实现到日常工作中的阶段，但是由于现在想要搭建一个满意的个人博客，并且尝试过很多 hexo 主题后总是会对主题的某些或某个部分不太满意，&lt;br&gt;
所以为了以后可以自己实现相应页面的开发，所以还是想着能够学点前端框架的知识，为以后成为全栈工程师做准备。目前比较流行的前端框架主要有&lt;code&gt;React.js&lt;/code&gt;和&lt;code&gt;Vue.js&lt;/code&gt;，因为当前公司使用的是&lt;code&gt;React.js&lt;/code&gt;开发的，所以也选择&lt;code&gt;React&lt;/code&gt;作为学习对象。&lt;/p&gt;
    
    </summary>
    
      <category term="React" scheme="https://blog.i2arch.tech/categories/React/"/>
    
    
      <category term="web" scheme="https://blog.i2arch.tech/tags/web/"/>
    
  </entry>
  
  <entry>
    <title>【TP5深入理解】控制器(三)--前置操作</title>
    <link href="https://blog.i2arch.tech/2018/08/07/tp5-controller3/"/>
    <id>https://blog.i2arch.tech/2018/08/07/tp5-controller3/</id>
    <published>2018-08-07T12:26:33.000Z</published>
    <updated>2018-11-16T12:14:52.126Z</updated>
    
    <content type="html"><![CDATA[<p>tp5 框架的前置操作可以用于对某些方法进行通用的预处理，比如登录状态的判断[session 处理]、用户权限的卡控[cache/session 的处理]，通过控制器的前置操作，将公用代码进行封装，简化了调用流程[直接设定前置关系即可实现前置方法的自动调用]。</p><a id="more"></a><h2 id="基本用法"><a class="markdownIt-Anchor" href="#基本用法"></a> 基本用法</h2><h3 id="使用示例"><a class="markdownIt-Anchor" href="#使用示例"></a> 使用示例：</h3><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">protected</span> $beforeActionList = [</span><br><span class="line">    <span class="string">'userBeforeAction1'</span> =&gt; [<span class="string">'only'</span> =&gt; <span class="string">'function1NeedDoBeforeAction1, function2DoNeedBeforeAction1'</span>],</span><br><span class="line">    <span class="string">'userBeforeAction2'</span> =&gt; [<span class="string">'except'</span> =&gt; <span class="string">'function1NotNeedBeforeAction2, function2NotNeedBeforeAction2'</span>],</span><br><span class="line">    <span class="string">'userBeforeAction3'</span></span><br><span class="line">];</span><br></pre></td></tr></table></figure><h3 id="使用说明"><a class="markdownIt-Anchor" href="#使用说明"></a> 使用说明：</h3><p>(1) <code>only</code> =&gt; 当调用 api 接口方法<code>function1NeedDoBeforeAction1()</code>和<code>function1NeedDoBeforeAction1</code>时，都会自动调用不公开[<code>protected</code>]的前置方法<code>userBeforeAction1()</code>，并且该前置方法只在访问这两个 api 方法时执行。</p><p>(2) <code>except</code> =&gt; api 接口方法<code>function1NotNeedBeforeAction2()</code>和<code>function1NotNeedBeforeAction2()</code>时，不会执行前置方法<code>userBeforeAction2()</code></p><h2 id="使用介绍"><a class="markdownIt-Anchor" href="#使用介绍"></a> 使用介绍</h2><blockquote><p>可以为某个或者某些操作指定前置执行的操作方法，设置 beforeActionList 属性可以指定某个方法为其他方法的前置操作，数组键名为需要调用的前置方法名，无值的话为当前控制器下所有方法的前置方法。</p></blockquote><h3 id="控制器类属性-beforeactionlist"><a class="markdownIt-Anchor" href="#控制器类属性-beforeactionlist"></a> 控制器类属性: $beforeActionList</h3><h3 id="属性值-键值对"><a class="markdownIt-Anchor" href="#属性值-键值对"></a> 属性值： 键值对</h3><ul><li><p>键：前置方法名</p></li><li><p>值：前置方法作用域[键值对|无]</p><ul><li><p>无值时： 对当前控制器所有 api 方法都执行前置</p></li><li><p>有值[键值对]：</p><ul><li>键：<ul><li>except：除某些 api 方法执行前置</li><li>only：只对某些 api 方法执行前置</li></ul></li><li>值：需要进行前置操作的 api 方法 [当该项值为多个时，用半角,进行间隔]</li></ul></li></ul></li></ul><h3 id="注意点"><a class="markdownIt-Anchor" href="#注意点"></a> 【注意点】：</h3><p>（1）由于 TP5 框架对 url 的处理是全部转化为小写，并且执行前置操作时，也是通过 url 中参数获取当前调用的 api 方法，并判断是否需要进行前置操作的，所以定义需要执行前置方法的 api 方法时，都需要使用小写[针对 TP5.0 版本]</p><p>（2）前置方法的访问方式为<code>private</code>时，则无法调用。</p><h2 id="实现原理源码阅读"><a class="markdownIt-Anchor" href="#实现原理源码阅读"></a> 实现原理[源码阅读]</h2><h3 id="框架类库"><a class="markdownIt-Anchor" href="#框架类库"></a> 框架类库</h3><p>基类控制器<code>thinkphp\library\think\Controller.php</code></p><h3 id="相关介绍"><a class="markdownIt-Anchor" href="#相关介绍"></a> 相关介绍</h3><p>(1) 属性名：<code>$beforeActionList</code></p><p>前置方法列表：</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">protected</span> $beforeActionList = [];</span><br></pre></td></tr></table></figure><p>(2) 构造方法：<code>__construc()</code></p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 遍历前置方法列表，并对每个前置方法进行前置处理</span></span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">$this</span>-&gt;beforeActionList) &#123;</span><br><span class="line">    <span class="keyword">foreach</span> (<span class="keyword">$this</span>-&gt;beforeActionList <span class="keyword">as</span> $method =&gt; $options) &#123;</span><br><span class="line">        is_numeric($method) ?</span><br><span class="line">        <span class="keyword">$this</span>-&gt;beforeAction($options) :</span><br><span class="line">        <span class="keyword">$this</span>-&gt;beforeAction($method, $options);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>【逻辑分析】：</p><ul><li><p>判断前置数组的键是否为数值</p></li><li><p>如果是数值，直接对前置方法键值对的值进行处理</p></li><li><p>如果不是数值，则对每组前置关系进行处理</p></li></ul><h3 id="实现方法beforeaction"><a class="markdownIt-Anchor" href="#实现方法beforeaction"></a> 实现方法：<code>beforeAction()</code></h3><p>(1) 当<strong>前置方法</strong>[每组前置关系的键]是数值时 【键为数值的情况即 <strong>该组前置关系</strong> 没有指定前置方法的作用域，是对所有方法执行该前置方法】</p><ul><li><p>调用 – <code>$this-&gt;beforeAction($options)</code></p></li><li><p>实际执行过程为</p></li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">protected</span> <span class="function"><span class="keyword">function</span> <span class="title">beforeAction</span><span class="params">($options)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    call_user_func([<span class="keyword">$this</span>, $options]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>(2) 当<strong>前置方法</strong>[每组前置关系的键]不是数值时</p><ul><li><p>调用 – <code>$this-&gt;beforeAction($method, $options)</code></p></li><li><p>完整执行过程为</p></li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">protected</span> <span class="function"><span class="keyword">function</span> <span class="title">beforeAction</span><span class="params">($method, $options = [])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">isset</span>($options[<span class="string">'only'</span>])) &#123;</span><br><span class="line">        <span class="keyword">if</span> (is_string($options[<span class="string">'only'</span>])) &#123;</span><br><span class="line">            $options[<span class="string">'only'</span>] = explode(<span class="string">','</span>, $options[<span class="string">'only'</span>]);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!in_array(<span class="keyword">$this</span>-&gt;request-&gt;action(), $options[<span class="string">'only'</span>])) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">elseif</span> (<span class="keyword">isset</span>($options[<span class="string">'except'</span>])) &#123;</span><br><span class="line">        <span class="keyword">if</span> (is_string($options[<span class="string">'except'</span>])) &#123;</span><br><span class="line">            $options[<span class="string">'except'</span>] = explode(<span class="string">','</span>, $options[<span class="string">'except'</span>]);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (in_array(<span class="keyword">$this</span>-&gt;request-&gt;action(), $options[<span class="string">'except'</span>])) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    call_user_func([<span class="keyword">$this</span>, $method]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>（3）函数 [<a href="http://php.net/manual/en/function.call-user-func.php" target="_blank" rel="noopener">call_user_func</a>]功能：把第一个参数作为回调函数调用</p><ul><li><p>参数说明：</p><p>当传入的参数为一个数组时，则将数组的</p><ul><li>第一个元素作为 <strong>类名</strong> 或 <strong>类的实例化对象</strong></li><li>第二个元素作为类的方法名 [一般方法和静态方法均可以]</li></ul></li><li><p>调用结果：</p><p>就是直接执行该类下的相应方法的结果</p></li></ul><p>[全程学习+笔记时长：100min]</p><p>【声明】</p><p>我的博客即将搬运同步至腾讯云+社区，邀请大家一同入驻：<a href="https://cloud.tencent.com/developer/support-plan?invite_code=89fda9dsh3d0" target="_blank" rel="noopener">https://cloud.tencent.com/developer/support-plan?invite_code=89fda9dsh3d0</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;tp5 框架的前置操作可以用于对某些方法进行通用的预处理，比如登录状态的判断[session 处理]、用户权限的卡控[cache/session 的处理]，通过控制器的前置操作，将公用代码进行封装，简化了调用流程[直接设定前置关系即可实现前置方法的自动调用]。&lt;/p&gt;
    
    </summary>
    
      <category term="PHP" scheme="https://blog.i2arch.tech/categories/PHP/"/>
    
    
      <category term="thinkphp5" scheme="https://blog.i2arch.tech/tags/thinkphp5/"/>
    
  </entry>
  
  <entry>
    <title>【Docker】--入门使用</title>
    <link href="https://blog.i2arch.tech/2018/07/31/docker-base/"/>
    <id>https://blog.i2arch.tech/2018/07/31/docker-base/</id>
    <published>2018-07-31T05:00:00.000Z</published>
    <updated>2018-11-16T12:14:51.856Z</updated>
    
    <content type="html"><![CDATA[<p>在学习 Docker 的镜像源设置和安装配置后，开始学习 Docker 的基本使用，包括服务进程的管理，容器、镜像的基本使用和管理。</p><a id="more"></a><h2 id="docker-使用命令"><a class="markdownIt-Anchor" href="#docker-使用命令"></a> Docker 使用命令</h2><h3 id="系统管理相关"><a class="markdownIt-Anchor" href="#系统管理相关"></a> 系统管理相关</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看docker运行状态</span></span><br><span class="line">sudo service docker status</span><br><span class="line">sudo systemctl docker status</span><br><span class="line"></span><br><span class="line"><span class="comment"># 启动和停止docker</span></span><br><span class="line">sudo service docker start</span><br><span class="line">sudo systemctl docekr start</span><br><span class="line"></span><br><span class="line">sudo service sdocker stop</span><br><span class="line">sudo systemctl docekr stop</span><br></pre></td></tr></table></figure><h3 id="docker-运行"><a class="markdownIt-Anchor" href="#docker-运行"></a> Docker 运行</h3><p>(1)下载 Docker Hub 上的&quot;hello-world&quot;镜像[大小只有 1.85k]</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo docker run hello-world</span><br></pre></td></tr></table></figure><p>(2) 查看 docker 镜像</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker images</span><br></pre></td></tr></table></figure><p>(3) 查看 docker 容器</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看运行状态的container</span></span><br><span class="line">docker ps</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看所有的container</span></span><br><span class="line">docker ps -a</span><br></pre></td></tr></table></figure><p>(4)下载<code>ubuntu</code>镜像，创建容器，保持可写入操作，并以 bash 状态进入容器</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">sudo docker run -i -t ubuntu /bin/bash</span><br><span class="line"><span class="comment"># [-i]: 保证容器中STDIN是开启的，持久的标准写入</span></span><br><span class="line"><span class="comment"># [-t]: 为要创建的容器分配一个伪tty终端，使新创建的容器能够提供一个交互式shell</span></span><br><span class="line"><span class="comment"># [ubuntu]: 使用的镜像名，如果不存在则下载Docker Hub上的镜像</span></span><br><span class="line"><span class="comment"># [/bin/bash]: 让新建的容器运行/bin/bash命令</span></span><br></pre></td></tr></table></figure><p>只有指定的/bin/bash 命令处于运行状态的时候，容器才处于运行状态，一旦 exit 退出容器，/bin/bash 命令也就结束了，这时容器也随之停止运行。<br>但容器仍然是存在的。</p><p>Docker 在文件系统内部用这个镜像创建了一个新容器，该容器拥有自己的网络、IP 地址，以及一个用来和宿主机进行通信的桥接网络接口。</p><h3 id="容器的基本使用"><a class="markdownIt-Anchor" href="#容器的基本使用"></a> 容器的基本使用</h3><p>(1)查看容器的主机名[即该容器的 ID]</p><p><code>hostname</code></p><p>未完待续！</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在学习 Docker 的镜像源设置和安装配置后，开始学习 Docker 的基本使用，包括服务进程的管理，容器、镜像的基本使用和管理。&lt;/p&gt;
    
    </summary>
    
      <category term="Docker" scheme="https://blog.i2arch.tech/categories/Docker/"/>
    
    
      <category term="docker" scheme="https://blog.i2arch.tech/tags/docker/"/>
    
  </entry>
  
  <entry>
    <title>【Docker】--安装与配置</title>
    <link href="https://blog.i2arch.tech/2018/07/31/docker-install/"/>
    <id>https://blog.i2arch.tech/2018/07/31/docker-install/</id>
    <published>2018-07-31T03:00:00.000Z</published>
    <updated>2018-11-16T12:14:51.863Z</updated>
    
    <content type="html"><![CDATA[<p>docker 是 linux 虚拟化技术，能够一键式搭建开发环境，并且能保证运维、开发、上线部署的环境完全一致，避免了运行环境差异性带来的问题。<br>具有简单、轻量、快速、高效的特性。掌握 Docker 的安装和相关配置也是提升开发技能的重要途径。</p><a id="more"></a><h2 id="linux-安装ubuntucentos"><a class="markdownIt-Anchor" href="#linux-安装ubuntucentos"></a> Linux 安装[ubuntu/centos]</h2><h3 id="安装前检查"><a class="markdownIt-Anchor" href="#安装前检查"></a> 安装前检查</h3><p><strong>Ubuntu &gt; 12</strong></p><p>(1).内核版本：[&gt;=Linux 3.8]</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uname -a</span><br></pre></td></tr></table></figure><p>(2).存储驱动 Device Mapper[文件存在即已安装]</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">ls -l /sys/class/misc/device-mapper</span><br><span class="line"></span><br><span class="line"><span class="comment"># 如果不存在，加载dm_mod模块</span></span><br><span class="line">sudo modprobe dm_mod</span><br></pre></td></tr></table></figure><p>(3).开启<a href="http://en.wikipedia.org/wiki/Cgroups" target="_blank" rel="noopener">cgroup</a>和命名空间(namespace)功能</p><p>2.6.38 以后的内核都已经提供了良好的支持。</p><p>(4).系统为 64 位[x86_64 和 amd64] 【目前不支持 32 位 CPU】</p><h3 id="安装方式"><a class="markdownIt-Anchor" href="#安装方式"></a> 安装方式</h3><p>（1）安装 Ubuntu/CentOS 维护的版本</p><p>[ubuntu]</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 安装docker[ubuntu]</span></span><br><span class="line">sudo apt-get install -y docker.io</span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置docker</span></span><br><span class="line"><span class="built_in">source</span> /etc/bash_completion.d/docker.io</span><br></pre></td></tr></table></figure><p>[centos]</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 安装docker[centos]</span></span><br><span class="line">sudo yum install -y docker</span><br></pre></td></tr></table></figure><p>（2）安装 Docker 维护的版本</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1.检查APT的HTTPS支持，查看/usr/lib/apt/methods/https文件是否存在，如果不存在，运行安装命令</span></span><br><span class="line">apt-get update</span><br><span class="line">apt-get install -y apt-transport-https</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2.添加Docker的apt仓库</span></span><br><span class="line"><span class="built_in">echo</span> deb https://get.docker.com/ubuntu docker main &gt; /etc/apt/sources.list.d/docker.list</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3.添加仓库的key</span></span><br><span class="line">apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys &#123;你的key&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4.安装</span></span><br><span class="line">apt-get update</span><br><span class="line">apt-get install -y lxc-docker</span><br></pre></td></tr></table></figure><p>（3）脚本安装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看系统是否安装curl</span></span><br><span class="line">whereis curl</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装curl命令</span></span><br><span class="line">sudo apt-get install -y curl   [ubuntu]</span><br><span class="line">sudo yum install -y curl    [centos]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 运行脚本 [获得当前最新稳定版docker]</span></span><br><span class="line">curl -sSL https://get.docker.com/ubuntu/ | sudo sh</span><br></pre></td></tr></table></figure><p>（4）查看 docker 版本信息</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看docker版本</span></span><br><span class="line">sudo docker version</span><br></pre></td></tr></table></figure><p>当前版本信息：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">Client:</span><br><span class="line"> Version:         1.13.1</span><br><span class="line"> API version:     1.26</span><br><span class="line"> Package version: docker-1.13.1-68.gitdded712.el7.centos.x86_64</span><br><span class="line"> Go version:      go1.9.4</span><br><span class="line"> Git commit:      dded712/1.13.1</span><br><span class="line"> Built:           Tue Jul 17 18:34:48 2018</span><br><span class="line"> OS/Arch:         linux/amd64</span><br><span class="line"></span><br><span class="line">Server:</span><br><span class="line"> Version:         1.13.1</span><br><span class="line"> API version:     1.26 (minimum version 1.12)</span><br><span class="line"> Package version: docker-1.13.1-68.gitdded712.el7.centos.x86_64</span><br><span class="line"> Go version:      go1.9.4</span><br><span class="line"> Git commit:      dded712/1.13.1</span><br><span class="line"> Built:           Tue Jul 17 18:34:48 2018</span><br><span class="line"> OS/Arch:         linux/amd64</span><br><span class="line"> Experimental:    <span class="literal">false</span></span><br></pre></td></tr></table></figure><h3 id="查看-docker-运行效果"><a class="markdownIt-Anchor" href="#查看-docker-运行效果"></a> 查看 docker 运行效果</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo docker run ubuntu <span class="built_in">echo</span> <span class="string">'Hello World'</span></span><br></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">Hello from Docker!</span><br><span class="line">This message shows that your installation appears to be working correctly.</span><br><span class="line"></span><br><span class="line">To generate this message, Docker took the following steps:</span><br><span class="line"> 1. The Docker client contacted the Docker daemon.</span><br><span class="line"> 2. The Docker daemon pulled the &quot;hello-world&quot; image from the Docker Hub.</span><br><span class="line">    (amd64)</span><br><span class="line"> 3. The Docker daemon created a new container from that image which runs the</span><br><span class="line">    executable that produces the output you are currently reading.</span><br><span class="line"> 4. The Docker daemon streamed that output to the Docker client, which sent it</span><br><span class="line">    to your terminal.</span><br><span class="line"></span><br><span class="line">To try something more ambitious, you can run an Ubuntu container with:</span><br><span class="line"> $ docker run -it ubuntu bash</span><br><span class="line"></span><br><span class="line">Share images, automate workflows, and more with a free Docker ID:</span><br><span class="line"> https://hub.docker.com/</span><br><span class="line"></span><br><span class="line">For more examples and ideas, visit:</span><br><span class="line"> https://docs.docker.com/engine/userguide/</span><br></pre></td></tr></table></figure><h3 id="使用非-root-用户为当前用户添加-docker-用户组"><a class="markdownIt-Anchor" href="#使用非-root-用户为当前用户添加-docker-用户组"></a> 使用非 root 用户[为当前用户添加 docker 用户组]</h3><p>默认使用 docker 相关命令时，必须使用 root 权限，实际上可以创建 docker 用户组，从而简化 docker 使用操作，不用在所有的 docker 命令前使用<code>sudo</code>命名。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1.添加docker用户组</span></span><br><span class="line">sudo groupadd docker</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2.将当前用户添加到docker用户组</span></span><br><span class="line">sudo gpasswd -a <span class="variable">$&#123;USER&#125;</span> docker</span><br><span class="line"><span class="comment"># 或</span></span><br><span class="line">sudo usermod -aG docker <span class="variable">$USER</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3.重启docker服务</span></span><br><span class="line">sudo service docker restart</span><br></pre></td></tr></table></figure><p>[注意，执行 gpasswd 命令之后需要注销后重新再登录才能生效]</p><blockquote><p>添加到 docker 用户组存在安全隐患，docker 用户组对 Docker 具有与 root 用户相同的权限，<br>所以 docker 用户组中应该只能添加确实需要使用 Docker 的用户和程序</p></blockquote><h3 id="docker-升级"><a class="markdownIt-Anchor" href="#docker-升级"></a> Docker 升级</h3><p>[ubuntu]</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sudo apt-get update</span><br><span class="line">sudo apt-get install docker</span><br></pre></td></tr></table></figure><p>[centos]</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sudo yum update</span><br><span class="line">sudo yum install docker</span><br></pre></td></tr></table></figure><h2 id="安装-docker-compose"><a class="markdownIt-Anchor" href="#安装-docker-compose"></a> 安装 docker-compose</h2><h3 id="pip-安装"><a class="markdownIt-Anchor" href="#pip-安装"></a> pip 安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo pip install docker-compose</span><br></pre></td></tr></table></figure><h3 id="下载二进制文件安装"><a class="markdownIt-Anchor" href="#下载二进制文件安装"></a> 下载二进制文件安装</h3><p>【查看最新版版本号】:<a href="https://github.com/docker/compose/releases" target="_blank" rel="noopener">docker/compose–github</a></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 下载最新版的docker-compose[版本号需要前往github确认是否为最新，1.22.0是目前的最新版2018/7/31]</span></span><br><span class="line">sudo curl -L https://github.com/docker/compose/releases/download/1.22.0/docker-compose-`uname -s`-`uname -m` -o /usr/<span class="built_in">local</span>/bin/docker-compose</span><br><span class="line"></span><br><span class="line"><span class="comment"># 对docker-compose添加可执行权限</span></span><br><span class="line">sudo chmod +x /usr/<span class="built_in">local</span>/bin/docker-compose</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看版本信息</span></span><br><span class="line">sudo docker-compose -v</span><br></pre></td></tr></table></figure><h2 id="windows-安装"><a class="markdownIt-Anchor" href="#windows-安装"></a> Windows 安装</h2><h3 id="docker-基本介绍"><a class="markdownIt-Anchor" href="#docker-基本介绍"></a> Docker 基本介绍</h3><ul><li>Linux 容器技术</li><li>操作系统级别的虚拟化</li><li>依赖于 Linux 内核的 Namespace 和 Cgroups</li></ul><h3 id="windows-下的-dockerboot2docker-for-windowsdocker-toolbox"><a class="markdownIt-Anchor" href="#windows-下的-dockerboot2docker-for-windowsdocker-toolbox"></a> windows 下的 docker[Boot2Docker for windows|Docker ToolBox]</h3><ul><li>Boot2Docker Linux ISO | Docker ToolBox [为 docker 定制的虚拟机镜像，包含 docker 的运行环境]</li><li>Virtualbox [提供虚拟机服务的软件]</li><li>MSYS-git [提供 shell 运行环境]</li><li>管理工具</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;docker 是 linux 虚拟化技术，能够一键式搭建开发环境，并且能保证运维、开发、上线部署的环境完全一致，避免了运行环境差异性带来的问题。&lt;br&gt;
具有简单、轻量、快速、高效的特性。掌握 Docker 的安装和相关配置也是提升开发技能的重要途径。&lt;/p&gt;
    
    </summary>
    
      <category term="Docker" scheme="https://blog.i2arch.tech/categories/Docker/"/>
    
    
      <category term="docker" scheme="https://blog.i2arch.tech/tags/docker/"/>
    
  </entry>
  
  <entry>
    <title>【Docker】更新docker镜像源</title>
    <link href="https://blog.i2arch.tech/2018/07/31/docker-mirrors/"/>
    <id>https://blog.i2arch.tech/2018/07/31/docker-mirrors/</id>
    <published>2018-07-31T02:00:00.000Z</published>
    <updated>2018-11-16T12:14:51.871Z</updated>
    
    <content type="html"><![CDATA[<p>使用 docker 拉取 Docker Hub 上镜像时，可能会由于网络限制，导致下载失败。可以将 docker 的镜像源设置为国内的镜像，<br>目前支持的镜像源有<a href="https://cr.console.aliyun.com/cn-hangzhou/mirrors" target="_blank" rel="noopener">阿里云</a>和<a href="https://www.docker-cn.com" target="_blank" rel="noopener">docker 中文站</a></p><a id="more"></a><p>1.获取个人加速地址：</p><p><a href="https://cr.console.aliyun.com/cn-hangzhou/mirrors" target="_blank" rel="noopener">阿里云镜像加速器</a></p><p>2.配置镜像加速器</p><blockquote><p>针对 Docker 客户端版本大于 1.10.0 的用户</p></blockquote><p>修改 daemon 配置文件<code>/etc/docker/daemon.json</code>来使用加速器</p><p>(1)创建<code>/etc/docker</code>目录(如果不存在的话)</p><p><code>sudo mkdir -p /etc/docker</code></p><p>(2)修改配置文件<code>/etc/docker/daemon.json</code></p><p>[阿里云]：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">// 文件位置: /etc/docker/daemon.json</span><br><span class="line">&#123;</span><br><span class="line">  <span class="attr">"registry-mirrors"</span>: [<span class="string">"https://y0qd3iq.mirror.aliyuncs.com"</span>]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>[docker 中文站]：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">// 文件位置: /etc/docker/daemon.json</span><br><span class="line">&#123;</span><br><span class="line">  <span class="attr">"registry-mirrors"</span>: [<span class="string">"https://registry.docker-cn.com"</span>]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>3.重载守护进程文件，重启 docker</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sudo systemctl daemon-reload</span><br><span class="line">sudo systemctl restart docker</span><br></pre></td></tr></table></figure><p>4.查看加速器是否生效</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">[root@txyun ~]<span class="comment"># docker info</span></span><br><span class="line"></span><br><span class="line">...</span><br><span class="line">Registry Mirrors:</span><br><span class="line"> https://y0qd36iq.mirror.aliyuncs.com</span><br><span class="line">...</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;使用 docker 拉取 Docker Hub 上镜像时，可能会由于网络限制，导致下载失败。可以将 docker 的镜像源设置为国内的镜像，&lt;br&gt;
目前支持的镜像源有&lt;a href=&quot;https://cr.console.aliyun.com/cn-hangzhou/mirrors&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;阿里云&lt;/a&gt;和&lt;a href=&quot;https://www.docker-cn.com&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;docker 中文站&lt;/a&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="Docker" scheme="https://blog.i2arch.tech/categories/Docker/"/>
    
    
      <category term="镜像" scheme="https://blog.i2arch.tech/tags/%E9%95%9C%E5%83%8F/"/>
    
  </entry>
  
  <entry>
    <title>【CentOS】更新yum软件源</title>
    <link href="https://blog.i2arch.tech/2018/07/31/linux-yum%E6%BA%90/"/>
    <id>https://blog.i2arch.tech/2018/07/31/linux-yum源/</id>
    <published>2018-07-31T01:00:00.000Z</published>
    <updated>2018-11-16T12:14:51.936Z</updated>
    
    <content type="html"><![CDATA[<p>当使用 linux 安装包命令安装相关软件时，会提示<code>Connection timed out</code>,表示连接超时，即访问国外网址受限，可以切换软件源地址为国内的。</p><a id="more"></a><p>目前有网易和阿里云：</p><ul><li><p><a href="mirrors.163.com">网易软件源</a></p></li><li><p><a href="https://opsx.alibaba.com/mirror" target="_blank" rel="noopener">阿里云软件源</a></p></li></ul><p>【以阿里云为例(CentOS)】：</p><p>(1) 备份</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mv /etc/yum.repos.d/CentOS-Base.repo /etc/yum.repos.d/CentOS-Base.repo.backup</span><br></pre></td></tr></table></figure><p>(2) 下载新的 CentOS-Base.repo 到<code>/etc/yum.repos.d/</code></p><p>[CentOS 5]</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">wget -O /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-5.repo</span><br><span class="line"><span class="comment"># 或</span></span><br><span class="line">curl -o /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-5.repo</span><br></pre></td></tr></table></figure><p>[CentOS 6]</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">wget -O /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-6.repo</span><br><span class="line"><span class="comment"># 或</span></span><br><span class="line">curl -o /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-6.repo</span><br></pre></td></tr></table></figure><p>[CentOS 7]</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">wget -O /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-7.repo</span><br><span class="line"><span class="comment"># 或</span></span><br><span class="line">curl -o /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-7.repo</span><br></pre></td></tr></table></figure><p>(3) 生成缓存，是 yum 源生效</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">yum clean all</span><br><span class="line">yum makecache</span><br></pre></td></tr></table></figure><p>(4)更新 yum,验证 yum 软件源</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">[root@centos ~]<span class="comment"># yum -y update</span></span><br><span class="line">Loaded plugins: fastestmirror, langpacks</span><br><span class="line">Loading mirror speeds from cached hostfile</span><br><span class="line"> * base: mirrors.aliyun.com</span><br><span class="line"> * extras: mirrors.aliyun.com</span><br><span class="line"> * updates: mirrors.aliyun.com</span><br><span class="line">No packages marked <span class="keyword">for</span> update</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;当使用 linux 安装包命令安装相关软件时，会提示&lt;code&gt;Connection timed out&lt;/code&gt;,表示连接超时，即访问国外网址受限，可以切换软件源地址为国内的。&lt;/p&gt;
    
    </summary>
    
      <category term="Linux" scheme="https://blog.i2arch.tech/categories/Linux/"/>
    
    
      <category term="镜像" scheme="https://blog.i2arch.tech/tags/%E9%95%9C%E5%83%8F/"/>
    
  </entry>
  
  <entry>
    <title>【Python】安装与配置</title>
    <link href="https://blog.i2arch.tech/2018/07/30/python-base/"/>
    <id>https://blog.i2arch.tech/2018/07/30/python-base/</id>
    <published>2018-07-30T12:02:53.000Z</published>
    <updated>2018-11-16T12:14:52.064Z</updated>
    
    <content type="html"><![CDATA[<p>作为学习数据分析的工具，前期准备。</p><a id="more"></a><h2 id="环境搭建"><a class="markdownIt-Anchor" href="#环境搭建"></a> 环境搭建</h2><h3 id="一-windows-平台"><a class="markdownIt-Anchor" href="#一-windows-平台"></a> 一、Windows 平台</h3><p>占位</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;作为学习数据分析的工具，前期准备。&lt;/p&gt;
    
    </summary>
    
      <category term="Python" scheme="https://blog.i2arch.tech/categories/Python/"/>
    
    
      <category term="python" scheme="https://blog.i2arch.tech/tags/python/"/>
    
  </entry>
  
  <entry>
    <title>【SSH】使用SSH登录远程主机，并禁用密码登录</title>
    <link href="https://blog.i2arch.tech/2018/07/30/linux-ssh/"/>
    <id>https://blog.i2arch.tech/2018/07/30/linux-ssh/</id>
    <published>2018-07-30T08:54:08.000Z</published>
    <updated>2018-11-16T12:14:51.930Z</updated>
    
    <content type="html"><![CDATA[<p>对远程主机进行登录管理，一方面可以简化日常频繁登录的密码和 ip 输入步骤，另一方面，也可以提高远程主机的安全性，避免远程主机被“黑客”轻易攻击。<br>也借此加强对 Linux 文件权限的认识和理解。</p><a id="more"></a><h2 id="本地生成-ssh-秘钥"><a class="markdownIt-Anchor" href="#本地生成-ssh-秘钥"></a> 本地生成 SSH 秘钥</h2><h3 id="生成本机系统的-ssh-公钥"><a class="markdownIt-Anchor" href="#生成本机系统的-ssh-公钥"></a> 生成本机系统的 ssh 公钥</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-keygen</span><br></pre></td></tr></table></figure><p>默认保存路径为 <code>~/.ssh/</code></p><p>[windows 对应为<code>C:\Users\Administrator\.ssh\</code>][mobaxterm对应为<code>/home/mobaxterm/.ssh/</code>]</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 公钥路径</span></span><br><span class="line">~/.ssh/id_rsa.pub</span><br><span class="line"></span><br><span class="line"><span class="comment"># 私钥路径</span></span><br><span class="line">~/.ssh/id_rsa</span><br></pre></td></tr></table></figure><h3 id="复制公钥"><a class="markdownIt-Anchor" href="#复制公钥"></a> 复制公钥</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cat ~/.ssh/id_rsa.pub</span><br></pre></td></tr></table></figure><h2 id="远程主机配置-ssh"><a class="markdownIt-Anchor" href="#远程主机配置-ssh"></a> 远程主机配置 ssh</h2><h3 id="使用密码登录到远程主机"><a class="markdownIt-Anchor" href="#使用密码登录到远程主机"></a> 使用密码登录到远程主机</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ssh &#123;登录用户&#125;@&#123;ip地址&#125;</span><br><span class="line"></span><br><span class="line">输入密码后进入远程主机系统</span><br></pre></td></tr></table></figure><h3 id="查看远程主机的-ssh-配置"><a class="markdownIt-Anchor" href="#查看远程主机的-ssh-配置"></a> 查看远程主机的 ssh 配置</h3><p>配置文件目录<code>/etc/ssh/</code></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/etc/ssh/sshd_config</span><br></pre></td></tr></table></figure><p>对以下参数进行设置：</p><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 默认的认证公钥文件</span></span><br><span class="line">AuthorizedKeysFile .ssh/authorized_keys</span><br></pre></td></tr></table></figure><p>将<code>本地的公钥</code>复制到<code>远程的公钥认证文件</code> [~/.ssh/authorized_keys]中保存</p><h3 id="设置文件和目录权限"><a class="markdownIt-Anchor" href="#设置文件和目录权限"></a> 设置文件和目录权限</h3><p>【理论说明】：</p><p><strong>一栏有十个字符</strong>：</p><ul><li><p>第一个字符用于标识是<code>文件</code>还是<code>目录</code></p></li><li><p>后面九个字符为三组：</p><ul><li>第一组为『文件拥有者的权限』；</li><li>第二组为『同群组的权限』；</li><li>第三组为『其他非本群组的权限』。</li></ul></li></ul><p>其中：</p><p>[ r ]代表可读(read)，值为 4 [二进制：100][ w ]代表可写(write)，值为 2 [二进制：010][ x ]代表可执行(execute)，值为 1 [二进制: 001][ - ]代表没有操作权限，值为 0 [二进制： 000]</p><p>参考来源: <a href="http://cn.linux.vbird.org/linux_basic/0210filepermission.php" target="_blank" rel="noopener">http://cn.linux.vbird.org/linux_basic/0210filepermission.php</a></p><p>（1）设置<code>.ssh</code>目录权限</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 文件拥有者拥有读、写、执行权限,其他组无权限</span></span><br><span class="line">chmod 700 ~/.ssh/</span><br></pre></td></tr></table></figure><p>（2）设置<code>authorized_keys</code>文件权限</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 文件拥有者拥有读、写权限，同群组和其他群组成员拥有读权限</span></span><br><span class="line">chmod 644 ~/.ssh/authorized_keys</span><br></pre></td></tr></table></figure><h3 id="对-ssh-进行配置"><a class="markdownIt-Anchor" href="#对-ssh-进行配置"></a> 对 ssh 进行配置</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 位置：/etc/ssh/sshd_config</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 允许root用户通过ssh登录</span></span><br><span class="line">PermitRootLogin yes</span><br><span class="line"></span><br><span class="line"><span class="comment"># 允许使用ssh权限登录</span></span><br><span class="line">RSAAuthentication yes</span><br><span class="line">PubkeyAuthentication yes</span><br></pre></td></tr></table></figure><p>使用秘钥方式登陆后,禁用密码登录[之前密码登录的 session 将失效]</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 禁用密码登录</span></span><br><span class="line">PasswordAuthentication no</span><br></pre></td></tr></table></figure><p>重启 ssh</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">service sshd restart</span><br></pre></td></tr></table></figure><p>参考来源: <a href="https://hyjk2000.github.io/2012/03/16/how-to-set-up-ssh-keys/" target="_blank" rel="noopener">https://hyjk2000.github.io/2012/03/16/how-to-set-up-ssh-keys/</a></p><h2 id="本地-ssh-配置进行快捷登录"><a class="markdownIt-Anchor" href="#本地-ssh-配置进行快捷登录"></a> 本地 ssh 配置进行快捷登录</h2><h3 id="配置-ssh"><a class="markdownIt-Anchor" href="#配置-ssh"></a> 配置 ssh</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># ~/.ssh/config</span></span><br><span class="line"><span class="comment"># 定义登录远程主机的ssh连接名</span></span><br><span class="line">Host txyun</span><br><span class="line"></span><br><span class="line"><span class="comment"># 定义远程主机ip地址</span></span><br><span class="line">HostName &#123;ip地址&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 定义远程主机的ssh端口号[默认情况下ssh端口号为22]</span></span><br><span class="line">Port 22</span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置登录用户名, root用户拥有所有权限</span></span><br><span class="line">User root</span><br></pre></td></tr></table></figure><h3 id="进行-ssh-方式登录远程主机"><a class="markdownIt-Anchor" href="#进行-ssh-方式登录远程主机"></a> 进行 ssh 方式登录远程主机</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh txyun</span><br></pre></td></tr></table></figure><p>即可直接登录。</p><h2 id="总结"><a class="markdownIt-Anchor" href="#总结"></a> 总结</h2><p>简化了可信访客的登录步骤，并且也只有和远程服务器 ssh 认证文件中公钥相匹配的主机才能成功登录，提高了服务器的安全性。</p><p>完。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;对远程主机进行登录管理，一方面可以简化日常频繁登录的密码和 ip 输入步骤，另一方面，也可以提高远程主机的安全性，避免远程主机被“黑客”轻易攻击。&lt;br&gt;
也借此加强对 Linux 文件权限的认识和理解。&lt;/p&gt;
    
    </summary>
    
      <category term="Linux" scheme="https://blog.i2arch.tech/categories/Linux/"/>
    
    
      <category term="ssh" scheme="https://blog.i2arch.tech/tags/ssh/"/>
    
  </entry>
  
  <entry>
    <title>【总结】四个月的工作任务总结</title>
    <link href="https://blog.i2arch.tech/2018/07/28/summary-monthly4/"/>
    <id>https://blog.i2arch.tech/2018/07/28/summary-monthly4/</id>
    <published>2018-07-28T01:41:33.000Z</published>
    <updated>2018-11-16T12:14:52.099Z</updated>
    
    <content type="html"><![CDATA[<p>这个阶段主要开始完成 Facebook 的项目，基于第三方 api 进行开发，完成和 Facebook 平台的对接，实现 Facebook 业务迁移。需要强化英文文档阅读能力和业务理解和构思业务实现流程的能力，并提高代码质量，高效完成任务。养成知识迁移能力和相关知识点强关联能力。</p><a id="more"></a><h1 id="功能模块介绍"><a class="markdownIt-Anchor" href="#功能模块介绍"></a> 功能模块介绍</h1><h2 id="1文案图片管理"><a class="markdownIt-Anchor" href="#1文案图片管理"></a> 1.文案图片管理</h2><h3 id="需求分析"><a class="markdownIt-Anchor" href="#需求分析"></a> 需求分析</h3><p>对每个设计人员设计的文案图片进行统计分析，包括设计人员的总数据和每个文案图片对应的数据</p><h3 id="数据表设计"><a class="markdownIt-Anchor" href="#数据表设计"></a> 数据表设计</h3><p>(1) 设计人员表 [关联用户表和文案表和记录统计数据]</p><p>(2) 文案统计表 [文案图片和文案的 adset 和 campaign]</p><h2 id="2同步-facebook-数据-campaign-adset-ad"><a class="markdownIt-Anchor" href="#2同步-facebook-数据-campaign-adset-ad"></a> 2.同步 facebook 数据 [campaign adset ad]</h2><p>实现和 facebook 的对接，拿到 ad_account 下对应的 campaign 信息，adset 信息和 ad 信息。[access_token]</p><p><a href="https://developers.facebook.com/docs/marketing-api/reference/v3.1" target="_blank" rel="noopener">facebook 开发者文档</a></p><h3 id="数据表设计-主要依据facebook的数据结构设计表结构"><a class="markdownIt-Anchor" href="#数据表设计-主要依据facebook的数据结构设计表结构"></a> 数据表设计 [主要依据<a href="https://developers.facebook.com/docs/marketing-api/reference/adgroup" target="_blank" rel="noopener">facebook的数据结构</a>设计表结构]</h3><h2 id="3规则模块"><a class="markdownIt-Anchor" href="#3规则模块"></a> 3.规则模块</h2><p><img src="https://ws1.sinaimg.cn/large/005EgYNMgy1furhyuby3zj30l50ihaao.jpg" alt=""></p><h3 id="创建规则及设置规则触发条件"><a class="markdownIt-Anchor" href="#创建规则及设置规则触发条件"></a> 创建规则及设置规则触发条件</h3><p>1）应用对象 [campaign|adset active|paused]</p><p>2）触发规则后的处理方式 [开启|关闭|发送通知|修改名称|拷贝|…]</p><p>3）触发条件 [广告花费|点击量|购买量|单项收益|加入购物车量|…]</p><p>4）生效期 [整个|today|yesterday|last 7 day|…]</p><p>5）额外的条件限制 [限定 id 范围|限定拷贝数量范围]</p><p>6）规则执行周期<code>schedule</code> [手动执行|定点执行|5 分钟一次|…]</p><p>7）规则名称和所属规则分类</p><h3 id="执行规则查看执行结果"><a class="markdownIt-Anchor" href="#执行规则查看执行结果"></a> 执行规则，查看执行结果</h3><p>根据创建规则时设置的执行周期手动或自动执行相应规则，获得效益比较好的广告相关信息, 进而优化广告投放策略。</p><h2 id="4文案模块"><a class="markdownIt-Anchor" href="#4文案模块"></a> 4.文案模块</h2><p><img src="https://ws1.sinaimg.cn/large/005EgYNMgy1furi6njmqaj30d90chjy9.jpg" alt=""></p><h3 id="需求分析-2"><a class="markdownIt-Anchor" href="#需求分析-2"></a> 需求分析</h3><p>同步 Facebook 文案信息，获取所有文案信息并展示，创建文案并同步应用到 facebook 平台。</p><h3 id="组成结构"><a class="markdownIt-Anchor" href="#组成结构"></a> 组成结构</h3><ul><li><p>文本部分 [广告宣言 优惠信息 标题 CTA]</p></li><li><p>图片部分</p></li></ul><h2 id="5关键词功能评论模块下"><a class="markdownIt-Anchor" href="#5关键词功能评论模块下"></a> 5.关键词功能（评论模块下）</h2><p>5.1 应用场景</p><p>对 Facebook 文案下的评论进行关键词屏蔽。含有相应关键词的评论则隐藏。</p><p>5.2 实现功能</p><ul><li>对关键词设置 redis 缓存</li><li>设置新增和更新时</li></ul><h1 id="总结整理"><a class="markdownIt-Anchor" href="#总结整理"></a> 总结整理</h1><h2 id="日常业务实现总结"><a class="markdownIt-Anchor" href="#日常业务实现总结"></a> 日常业务实现总结</h2><h3 id="查询"><a class="markdownIt-Anchor" href="#查询"></a> 查询</h3><p>1.关联查询时字段名相同导致数据覆盖</p><p>2.排序方式</p><p>3.适当使用column()和value()[单纯的值或某列的数组]取代select()和find()[数据集对象]</p><h3 id="查询和编辑"><a class="markdownIt-Anchor" href="#查询和编辑"></a> 查询和编辑</h3><p>3.where 条件是否混杂[多处表查询的$where 没有区分导致不同表使用相同的查询条件导致字段不存在或多余的查询限制）</p><h3 id="新增和编辑"><a class="markdownIt-Anchor" href="#新增和编辑"></a> 新增和编辑</h3><p>4.用户提交数据验证 [post]</p><p>4.1 提交的数据是否是数组</p><p>4.2 编辑时提交的 id 是否存在</p><p>4.3 名称已存在 [新增和编辑的验证不同]</p><h2 id="基于第三方-api-开发"><a class="markdownIt-Anchor" href="#基于第三方-api-开发"></a> 基于第三方 api 开发</h2><h3 id="1阅读第三方开发者文档"><a class="markdownIt-Anchor" href="#1阅读第三方开发者文档"></a> 1.阅读第三方开发者文档</h3><ul><li>有哪些接口？可以实现哪些功能？</li><li>需要用到哪些功能(哪些接口)？</li><li>接口需要的参数（准确理解）</li><li>调用接口后返回的参数</li></ul><h3 id="2下载-sdk-封装成框架的第三方扩展类库概览可以实现的功能"><a class="markdownIt-Anchor" href="#2下载-sdk-封装成框架的第三方扩展类库概览可以实现的功能"></a> 2.下载 SDK, 封装成框架的第三方扩展类库，概览可以实现的功能</h3><ul><li>有哪些文件？有哪些类？有哪些方法？</li><li>方法对应的功能？需要传入的参数？返回的结果形式？</li></ul><h3 id="3封装成通用方法后期直接调用"><a class="markdownIt-Anchor" href="#3封装成通用方法后期直接调用"></a> 3.封装成通用方法，后期直接调用</h3><h2 id="知识体系的建立"><a class="markdownIt-Anchor" href="#知识体系的建立"></a> 知识体系的建立</h2><h3 id="知识体系"><a class="markdownIt-Anchor" href="#知识体系"></a> 知识体系</h3><p>知识体系是对一个概念进行发散性学习，对概念本身和与其具有相关性的概念进行联系，进行全方位的学习和理解。</p><h3 id="如何建立知识体系"><a class="markdownIt-Anchor" href="#如何建立知识体系"></a> 如何建立知识体系</h3><p>1.具体的东西 or 泛泛的概念？</p><p>2.怎么来的(发展历史和发展过程)？</p><p>3.处于知识体系的什么位置？[是由什么扩散开的知识点或者可以扩展出什么知识点]</p><p>4.知识（概念）本身[外部关联 和 内部关联]</p><ul><li>外部：为什么它好？同类的其他概念为什么不好？</li><li>内部：它究竟在讲什么东西？解决什么问题？怎么解决的？</li></ul><p>5.建立知识体系的优势</p><ul><li>系统、完整的理解某个概念</li><li>知识点串联，提升知识迁移能力</li><li>提升学习能力，和学习效率，扩展知识面</li></ul><h1 id="项目展示"><a class="markdownIt-Anchor" href="#项目展示"></a> 项目展示</h1><h2 id="1后台概览图"><a class="markdownIt-Anchor" href="#1后台概览图"></a> 1.后台概览图</h2><p><img src="https://ws1.sinaimg.cn/large/005EgYNMgy1fup8sr9hxhj31g30hwwfz.jpg" alt=""></p><h2 id="2规则列表"><a class="markdownIt-Anchor" href="#2规则列表"></a> 2.规则列表</h2><p><img src="https://ws1.sinaimg.cn/large/005EgYNMgy1furkvqigj0j31hc0jyq5o.jpg" alt=""></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;这个阶段主要开始完成 Facebook 的项目，基于第三方 api 进行开发，完成和 Facebook 平台的对接，实现 Facebook 业务迁移。需要强化英文文档阅读能力和业务理解和构思业务实现流程的能力，并提高代码质量，高效完成任务。养成知识迁移能力和相关知识点强关联能力。&lt;/p&gt;
    
    </summary>
    
      <category term="工作总结" scheme="https://blog.i2arch.tech/categories/%E5%B7%A5%E4%BD%9C%E6%80%BB%E7%BB%93/"/>
    
    
      <category term="工作总结" scheme="https://blog.i2arch.tech/tags/%E5%B7%A5%E4%BD%9C%E6%80%BB%E7%BB%93/"/>
    
  </entry>
  
  <entry>
    <title>【实战】Tp5+小程序(三)--微信登录与令牌</title>
    <link href="https://blog.i2arch.tech/2018/07/11/php-practice-wx-tp5-3/"/>
    <id>https://blog.i2arch.tech/2018/07/11/php-practice-wx-tp5-3/</id>
    <published>2018-07-11T08:26:58.000Z</published>
    <updated>2018-11-16T12:14:52.038Z</updated>
    
    <content type="html"><![CDATA[<p>ThinkPHP5 从入门到深入学习，结合实战项目深入理解 ThinkPHP5 的特性和使用方法。深入学习 api 开发，学习微信登录和令牌的相关知识，并理解微信登录流程，完成与微信开放 api 之间的数据交互，完善项目的相应功能。<br>理解第三方登录授权的 code 和 token 交互过程。</p><a id="more"></a><h3 id="9-1-初识-token-意义与作用"><a class="markdownIt-Anchor" href="#9-1-初识-token-意义与作用"></a> 9-1 初识 Token - 意义与作用</h3><p>说明：目前这种情况下，用户只要知道了系统的接口的形式，就可以直接访问，并获取数据，而大多数情况下，我们需要对用户身份进行验证，如：需要用户登录后才能访问的接口，以及需要管理员才能访问的接口等。</p><p>1.获取令牌</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">客户端=》(账号、密码)=》getToken 《==》 账号、密码、Token、Auth</span><br></pre></td></tr></table></figure><p>描述：客户端携带账号和密码信息，调用<code>getToken</code>接口，经过处理验证后，返回账号、密码、Token、Auth 等信息。</p><p>2.访问接口</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">客户端=》(Token)=》下单接口 《==》 账号、密码、Token、Auth</span><br></pre></td></tr></table></figure><p>验证：1.是否合法 2.是否有效 3.是否有操作权限</p><p>3.上面两个过程的 getToken 接口和下单接口就是被保护的接口，需要验证通过才能让用户访问。</p><h3 id="9-2-微信登录流程"><a class="markdownIt-Anchor" href="#9-2-微信登录流程"></a> 9-2 微信登录流程</h3><p>1.微信身份登录体系</p><p>![微信登录流程](<img src="https://ws1.sinaimg.cn/large/005EgYNMgy1fuchef1j72j30n10b9mza.jpg" alt=""></p><p>2.Token 在接口验证时的使用流程</p><p><img src="https://ws1.sinaimg.cn/large/005EgYNMgy1fuchef3nc9j30gx058aao.jpg" alt="Token访问下单接口"></p><h3 id="9-3-实现-token-身份权限体系"><a class="markdownIt-Anchor" href="#9-3-实现-token-身份权限体系"></a> 9-3 实现 Token 身份权限体系</h3><p>1.获取 token 的请求使用 post 方法[安全性方面考虑]</p><p>2.将复杂的业务分层到<code>service</code>层[实现分层思想]</p><p>使用模型处理数据库 CRUD 相关的操作，对于不操作数据库的复杂业务，将其封装到 Service 目录下，实现分层处理的思想，Service 层是在 Model 层之上的业务层。</p><p>3.基础实现</p><p>1）控制器的定义</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/controller/v1/Token [注意命名空间]</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">getToken</span><span class="params">($code = <span class="string">''</span>)</span> </span>&#123;&#125;</span><br></pre></td></tr></table></figure><p>2）路由定义</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// route.php</span></span><br><span class="line">Route::post(<span class="string">'api/:version/token/user'</span>, <span class="string">'api/:version.Token/getToken'</span>);</span><br></pre></td></tr></table></figure><p>3）验证器校验</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/controller/v1/Token</span></span><br><span class="line">(<span class="keyword">new</span> TokenGet())-&gt;goCheck();</span><br></pre></td></tr></table></figure><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/validate/TokenGet</span></span><br><span class="line"><span class="keyword">protected</span> $rule = [</span><br><span class="line">    <span class="comment">// 在验证器基类中定义isNotEmpty()方法</span></span><br><span class="line">    <span class="string">'code'</span> =&gt; <span class="string">'require|isNotEmpty'</span></span><br><span class="line">];</span><br><span class="line"></span><br><span class="line"><span class="keyword">protected</span> $message = [</span><br><span class="line">    <span class="string">'code'</span> =&gt; <span class="string">'code必填！'</span></span><br><span class="line">];</span><br></pre></td></tr></table></figure><h3 id="9-4567-实现-token-身份权限体系"><a class="markdownIt-Anchor" href="#9-4567-实现-token-身份权限体系"></a> 9-4/5/6/7 实现 Token 身份权限体系</h3><p>1.获取微信生成的 code 码，并将其作为参数，传递给微信接口来获得 openid 和 access_token 等相关信息[openid/session_key]</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/controller/v1/Token</span></span><br><span class="line">$userToken = <span class="keyword">new</span> UserToken($code);</span><br><span class="line">$token = $userToken-&gt;get();</span><br></pre></td></tr></table></figure><p><strong>2.封装 Service 层，实现 Token 令牌的获取</strong>[重点]</p><p>1） 配置微信小程序相关参数[app_id app_secret login_url]</p><p>2.1.1 在配置文件中设置微信小程序的相关参数</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// config/extra/wx.php</span></span><br><span class="line"><span class="keyword">return</span> [</span><br><span class="line">    <span class="string">'app_id'</span> =&gt; <span class="string">'XXXXXXXXX'</span>,</span><br><span class="line">    <span class="string">'app_secret'</span> =&gt; <span class="string">'XXXXXXXXX'</span>,</span><br><span class="line">    <span class="string">'login_url'</span> =&gt; <span class="string">"https://api.weixin.qq.com/sns/jscode2session?"</span> . <span class="string">"appid=%s&amp;secret=%s&amp;js_code=%s&amp;grant_type=authorization_code"</span></span><br><span class="line">];</span><br></pre></td></tr></table></figure><p>2.1.2 创建 Service 层的 UserToken 处理类，定义参数为私有属性</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/service/UserToken.php</span></span><br><span class="line"><span class="keyword">namespace</span> <span class="title">app</span>\<span class="title">api</span>\<span class="title">service</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> <span class="title">app</span>\<span class="title">lib</span>\<span class="title">exception</span>\<span class="title">WechatException</span>;</span><br><span class="line"><span class="keyword">use</span> <span class="title">app</span>\<span class="title">lib</span>\<span class="title">exception</span>\<span class="title">TokenException</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">UserToken</span> <span class="keyword">extends</span> <span class="title">Token</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">protected</span> $code;</span><br><span class="line">    <span class="keyword">protected</span> $appid;</span><br><span class="line">    <span class="keyword">protected</span> $appSecret;</span><br><span class="line">    <span class="keyword">protected</span> $loginUrl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>2） 拼接参数，并使用 curl 模拟 http 请求微信服务器，并获取返回结果</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/service/UserToken.php</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">($code)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">$this</span>-&gt;code      = $code;</span><br><span class="line">    <span class="keyword">$this</span>-&gt;appid     = config(<span class="string">'wx.app_id'</span>);</span><br><span class="line">    <span class="keyword">$this</span>-&gt;appSecret = config(<span class="string">'wx.app_secret'</span>);</span><br><span class="line">    <span class="keyword">$this</span>-&gt;loginUrl  = sprintf(</span><br><span class="line">        config(<span class="string">'wx.login_url'</span>),</span><br><span class="line">        <span class="keyword">$this</span>-&gt;appid, <span class="keyword">$this</span>-&gt;appSecret, <span class="keyword">$this</span>-&gt;code</span><br><span class="line">    );</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">get</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $result = curl_get(<span class="keyword">$this</span>-&gt;loginUrl);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在公共方法文件中定义 curl 模拟 http 请求的方法：</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// application/common.php</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">curl_get</span><span class="params">($url, &amp;$httpCode = <span class="number">0</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//1、初始化curl</span></span><br><span class="line">    $curl = curl_init();</span><br><span class="line"></span><br><span class="line">    <span class="comment">//2、告诉curl,请求的地址</span></span><br><span class="line">    curl_setopt($curl, CURLOPT_URL, $url);</span><br><span class="line">    <span class="comment">//3、将请求的数据返回，而不是直接输出</span></span><br><span class="line">    curl_setopt($curl, CURLOPT_RETURNTRANSFER, <span class="keyword">true</span>);</span><br><span class="line"></span><br><span class="line">    curl_setopt($curl, CURLOPT_SSL_VERIFYPEER, <span class="number">0</span>);</span><br><span class="line">    curl_setopt($curl, CURLOPT_CONNECTTIMEOUT, <span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">    $fileContents = curl_exec($curl); <span class="comment">// 执行操作</span></span><br><span class="line">    curl_close($curl); <span class="comment">// 关键CURL会话</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> $fileContents; <span class="comment">// 返回数据</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>3） 请求微信接口失败[微信内部错误/程序编写出错]的异常处理</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/service/UserToken.php get()</span></span><br><span class="line">$wxResult = json_decode($result, <span class="keyword">true</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">empty</span>($wxResult)) &#123;</span><br><span class="line">    <span class="comment">// 经验总结得：如果返回的结果为空[没有返回错误信息和错误代码]，则是微信服务器接口的问题，直接抛出异常一颗</span></span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> \<span class="keyword">Exception</span>(<span class="string">'获取session_key及openID异常，微信内部错误'</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    $loginFail = <span class="keyword">isset</span>($wxResult[<span class="string">'errcode'</span>]);</span><br><span class="line">    <span class="comment">// 程序传递的参数出错时，微信服务器会返回错误码和错误提示信息</span></span><br><span class="line">    <span class="keyword">if</span> ($loginFail) &#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;processLoginErr($wxResult);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>调用微信 Token 请求接口调用出错时的处理：</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/service/UserToken.php</span></span><br><span class="line"><span class="keyword">private</span> <span class="function"><span class="keyword">function</span> <span class="title">processLoginErr</span><span class="params">($wxResult)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> WechatException(</span><br><span class="line">        [</span><br><span class="line">            <span class="string">'msg'</span>       =&gt; $wxResult[<span class="string">'errmsg'</span>],</span><br><span class="line">            <span class="string">'errorCode'</span> =&gt; $wxResult[<span class="string">'errcode'</span>],</span><br><span class="line">        ]</span><br><span class="line">    );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>4） <strong>成功获取微信接口返回数据后的操作[存储 openid、生成令牌、写入缓存、返回令牌]</strong></p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/service/UserToken.php get()</span></span><br><span class="line"><span class="keyword">return</span> <span class="keyword">$this</span>-&gt;grantToken($wxResult);</span><br></pre></td></tr></table></figure><p>2.4.1 存储 openid</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/service/UserToken.php</span></span><br><span class="line"><span class="keyword">private</span> <span class="function"><span class="keyword">function</span> <span class="title">grantToken</span><span class="params">($wxResult)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $now = time();</span><br><span class="line">    <span class="comment">// 1.拿到openid</span></span><br><span class="line">    $openid     = $wxResult[<span class="string">'openid'</span>];</span><br><span class="line">    <span class="comment">// $sessionKey = $wxResult['session_key'];</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2.查看数据库中该openid的记录是否已经存在[同一个用户的openid始终保持不变]</span></span><br><span class="line">    $user = model(<span class="string">'user'</span>)-&gt;getByOpenId($openid);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3.如果存在，则不处理； 如果不存在，那么新增一条user记录</span></span><br><span class="line">    <span class="keyword">if</span> ($user) &#123;</span><br><span class="line">        $uid = $user-&gt;id;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        $uid = <span class="keyword">$this</span>-&gt;newUser($openid);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>根据 openid 查询是否已经存在该用户</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/model/User.php</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="function"><span class="keyword">function</span> <span class="title">getByOpenId</span><span class="params">($openid)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $user = <span class="keyword">self</span>::where(<span class="string">'openid'</span>, <span class="string">'='</span>, $openid)-&gt;find();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> $user;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>创建用户</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/service/UserToken.php</span></span><br><span class="line"><span class="keyword">private</span> <span class="function"><span class="keyword">function</span> <span class="title">newUser</span><span class="params">($openid)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $user = model(<span class="string">'user'</span>)-&gt;create([</span><br><span class="line">       <span class="string">'openid'</span> =&gt; $openid</span><br><span class="line">    ]);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> $user-&gt;id;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>2.4.2 准备缓存数据(缓存的值)</p><p>[微信返回数据(openid|session_key) + uid(用户服务器中保存的用户记录 id) + scope(用户权限，值越大，权限越高) ]</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/service/UserToken.php  grantToken()</span></span><br><span class="line"><span class="comment">// 4.生成令牌，准备缓存数据，写入缓存 [获取用户的相关信息]</span></span><br><span class="line"><span class="comment">// 4.1 准备缓存数据</span></span><br><span class="line">$cachedValue = <span class="keyword">$this</span>-&gt;prepareCachedValue($wxResult, $uid);</span><br></pre></td></tr></table></figure><p>准备缓存数据值的方法[缓存的值]</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/service/UserToken.php</span></span><br><span class="line"><span class="keyword">private</span> <span class="function"><span class="keyword">function</span> <span class="title">prepareCachedValue</span><span class="params">($wxResult, $uid)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $cachedValue = $wxResult;</span><br><span class="line">    $cachedValue[<span class="string">'uid'</span>] = $uid;</span><br><span class="line">    $cachedValue[<span class="string">'scope'</span>] = <span class="number">16</span>; <span class="comment">// 数值越大，权限越多</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> $cachedValue;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>2.4.3 写入缓存[令牌+微信返回数据+有效期]</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/service/UserToken.php  grantToken()</span></span><br><span class="line"><span class="comment">// 4.2 写入缓存，并返回令牌</span></span><br><span class="line">$token = <span class="keyword">$this</span>-&gt;saveToCache($cachedValue);</span><br></pre></td></tr></table></figure><p>2.4.3.1 生成令牌(缓存的键) [随机字符串+时间戳+盐]</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 令牌是用户程序生成的随机字符串，与微信服务器无关</span></span><br><span class="line"><span class="comment">// api/service/UserToken.php  saveToCache()</span></span><br><span class="line">$key = <span class="keyword">self</span>::generateToken();</span><br></pre></td></tr></table></figure><p>在服务器层构建 Token 基类，处理用户登录 Token 和后续的其他 Token 信息[service 下 UserToken 继承该基类]</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/service/Token.php</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="function"><span class="keyword">function</span> <span class="title">generateToken</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 用三组字符串，进行md5加密 [加强安全性]</span></span><br><span class="line">    <span class="comment">// 1.32个字符组成一组随机字符串</span></span><br><span class="line">    $randChars = getRandChar(<span class="number">32</span>);</span><br><span class="line">    <span class="comment">// 2.时间戳</span></span><br><span class="line">    $timestamp = $_SERVER[<span class="string">'REQUEST_TIME_FLOAT'</span>];</span><br><span class="line">    <span class="comment">// 3.盐</span></span><br><span class="line">    $salt = config(<span class="string">'secure.token_salt'</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> md5($randChars.$timestamp.$salt);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>公共方法中定义生成指定长度的随机字符串</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// application/common.php</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getRandChar</span><span class="params">($length)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $str    = <span class="keyword">null</span>;</span><br><span class="line">    $strPol = <span class="string">"ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz"</span>;</span><br><span class="line">    $max    = strlen($strPol) - <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> ($i = <span class="number">0</span>; $i &lt; $length; $i++) &#123;</span><br><span class="line">        $str .= $strPol[rand(<span class="number">0</span>, $max)];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> $str;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>创建安全配置文件[盐：随机字符串]</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// extra/secure.php</span></span><br><span class="line"><span class="keyword">return</span> [</span><br><span class="line">    <span class="string">'token_salt'</span> =&gt; <span class="string">'E7epHZhrTfgQ'</span></span><br><span class="line">];</span><br></pre></td></tr></table></figure><p>2.4.3.2 配置文件中设置 cache 缓存的有效期</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">创建安全配置文件[盐：随机字符串]</span><br><span class="line"></span><br><span class="line">```php</span><br><span class="line"><span class="comment">// extra/setting.php</span></span><br><span class="line"><span class="string">'token_expire_in'</span> =&gt; <span class="number">7200</span></span><br></pre></td></tr></table></figure><p>2.4.3.3 创建缓存文件</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="function"><span class="keyword">function</span> <span class="title">saveToCache</span><span class="params">($cachedValue)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $key = <span class="keyword">self</span>::generateToken();</span><br><span class="line">    $value = json_encode($cachedValue);</span><br><span class="line">    <span class="comment">// 设置缓存失效时间</span></span><br><span class="line">    $expire_in = config(<span class="string">'setting.token_expire_in'</span>);</span><br><span class="line"></span><br><span class="line">    $request = cache($key, $value, $expire_in);</span><br><span class="line">    <span class="keyword">if</span> (!$request) &#123;</span><br><span class="line">        <span class="comment">// 令牌缓存出错</span></span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> TokenException([</span><br><span class="line">            <span class="string">'msg'</span> =&gt; <span class="string">'服务器缓存异常'</span>,</span><br><span class="line">            <span class="string">'errorCode'</span> =&gt; <span class="number">10005</span></span><br><span class="line">        ]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> $key;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>2.4.4 返回令牌</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/service/UserToken.php  grantToken()</span></span><br><span class="line"><span class="comment">// 4.3 写入缓存，并返回令牌</span></span><br><span class="line"><span class="keyword">return</span> $token;</span><br></pre></td></tr></table></figure><p>3.异常处理类</p><p>3.1 微信内部错误[直接抛出异常]</p><p>3.2 微信接口调用出错[微信相关异常处理类 WechatException]</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">WechatException</span> <span class="keyword">extends</span> <span class="title">BaseException</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $code = <span class="number">404</span>;</span><br><span class="line">    <span class="keyword">public</span> $msg = <span class="string">'微信服务器接口调用失败'</span>;</span><br><span class="line">    <span class="keyword">public</span> $errorCode = <span class="number">999</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>3.3 缓存 Token 出错[Token 异常处理类 TokenException]</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TokenException</span> <span class="keyword">extends</span> <span class="title">BaseException</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $code = <span class="number">401</span>;</span><br><span class="line">    <span class="keyword">public</span> $msg = <span class="string">'Token已过期或无效Token'</span>;</span><br><span class="line">    <span class="keyword">public</span> $errorCode = <span class="number">10001</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="9-8-获取请求参数-code-并调用-php-接口借助微信开发工具"><a class="markdownIt-Anchor" href="#9-8-获取请求参数-code-并调用-php-接口借助微信开发工具"></a> 9-8 获取请求参数 code 并调用 PHP 接口[借助微信开发工具]</h3><h4 id="1微信开发者工具中配置"><a class="markdownIt-Anchor" href="#1微信开发者工具中配置"></a> 1.微信开发者工具中配置：</h4><blockquote><p>设置好 app_key 后，需要将 “详情” 中的 “不校验合法域名、web-view(业务域名)、TLS 版本以及 HTTPS 证书” 勾选上（在本地测试，没有远程访问的服务器或远程服务器访问的域名没有 https 证书）</p></blockquote><h4 id="2小程序代码"><a class="markdownIt-Anchor" href="#2小程序代码"></a> 2.小程序代码：</h4><p>(1) 在 config 中定义 restUrl</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Protoss/utils/config.js [设置本地测试的域名基地址]</span></span><br><span class="line">Config.restUrl = <span class="string">"http://mypro.com/api/v1/"</span>;</span><br></pre></td></tr></table></figure><p>(2)在登录方法中获取 code</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 在小程序登录调用wx.login()方法中输出code，然后使用接口请求工具将code作为post请求的参数，进行调用</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Protoss/utils/token.js getTokenFromServer()</span></span><br><span class="line">wx.login(&#123;</span><br><span class="line">  success: <span class="function"><span class="keyword">function</span>(<span class="params">res</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"code: "</span> + res.code);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><h4 id="3请求-php-接口获取-token"><a class="markdownIt-Anchor" href="#3请求-php-接口获取-token"></a> 3.请求 PHP 接口获取 Token</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 引用使用es6的module引入和定义</span></span><br><span class="line"><span class="comment">// 全局变量以g_开头</span></span><br><span class="line"><span class="comment">// 私有函数以_开头</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> &#123; Config &#125; <span class="keyword">from</span> <span class="string">"config.js"</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Token</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">this</span>.tokenUrl = Config.restUrl + <span class="string">"token/user"</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  verify() &#123;</span><br><span class="line">    <span class="keyword">var</span> token = wx.getStorageSync(<span class="string">"token"</span>);</span><br><span class="line">    <span class="keyword">if</span> (!token) &#123;</span><br><span class="line">      <span class="keyword">this</span>.getTokenFromServer();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  getTokenFromServer(callBack) &#123;</span><br><span class="line">    <span class="keyword">var</span> that = <span class="keyword">this</span>;</span><br><span class="line">    wx.login(&#123;</span><br><span class="line">      success: <span class="function"><span class="keyword">function</span>(<span class="params">res</span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">"code: "</span> + res.code);</span><br><span class="line">        wx.request(&#123;</span><br><span class="line">          url: that.tokenUrl,</span><br><span class="line">          method: <span class="string">"POST"</span>,</span><br><span class="line">          data: &#123;</span><br><span class="line">            code: res.code</span><br><span class="line">          &#125;,</span><br><span class="line">          success: <span class="function"><span class="keyword">function</span>(<span class="params">res</span>) </span>&#123;</span><br><span class="line">            <span class="built_in">console</span>.log(<span class="string">"token： "</span> + res.data.token);</span><br><span class="line">            wx.setStorageSync(<span class="string">"token"</span>, res.data.token);</span><br><span class="line">            callBack &amp;&amp; callBack(res.data.token);</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> &#123; Token &#125;;</span><br></pre></td></tr></table></figure><p><strong>【补充说明】</strong>：</p><p>(1) 需要调试时，将 XDEBUG 参数拼接到<code>this.tokenUrl</code>即可</p><p>(2) 如果没有输出 code, 需要关闭开发者工具后再重新启动，会自动调用该方法，并输出 code<br>[调用过生成的 token 已经被存储到浏览器的 Storage 中，便不会再调用 Token 请求接口，从而不产生 code]</p><h3 id="9-9-商品详情接口"><a class="markdownIt-Anchor" href="#9-9-商品详情接口"></a> 9-9 商品详情接口</h3><p>(1) 定义控制器方法 <code>getOne($id)</code></p><p>(2) 定义路由 <code>api/:version/product/:id</code></p><p>(3) 模型类实现[隐藏部分字段、设置数据表关联、实现数据库查询]</p><blockquote><p>Product =&gt; properties =&gt; ProductProperty =&gt; 商品属性值[品名、口味、产地、保质期]<br>Product =&gt; imgs =&gt; Image =&gt; 商品主图<br>ProductImage =&gt; imgs.imgUrl =&gt; Image =&gt; 商品详情图</p></blockquote><p>(4) 异常处理信息提示</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">[</span><br><span class="line">    <span class="string">'msg'</span>       =&gt; <span class="string">'当前产品无详情'</span>,</span><br><span class="line">    <span class="string">'errorCode'</span> =&gt; <span class="number">20001</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure><h3 id="9-10-1-路由变量规则"><a class="markdownIt-Anchor" href="#9-10-1-路由变量规则"></a> 9-10-1 路由变量规则</h3><p>1.路由匹配规则在项目中的应用。</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Route::get(<span class="string">'api/:version/product/recent'</span>, <span class="string">'api/:version.Product/getRecent'</span>);</span><br><span class="line">Route::get(<span class="string">'api/:version/product/:id'</span>, <span class="string">'api/:version.Product/getOne'</span>);</span><br></pre></td></tr></table></figure><p>2.存在的问题</p><p>目前调用接口都不存在问题，但是当将<code>:id</code>行放到<code>recent</code>行之前后，在调用<code>recent</code>路由时，则会因为优先匹配<code>:id</code>对应的路由，<br>此时则会因为参数校验不通过而报错。</p><p>3.解决之道：</p><p>对路由匹配规则进行限定，设置变量规则，对于<code>:id</code>行，限定只有当参数为数值时才匹配到当前行。即设置 <code>$id</code>的变量规则</p><p>变量规则：为变量用正则的方式指定变量规则，弥补了动态变量无法限制具体的类型问题，并且支持全局规则设置。</p><p>4.代码实现[设置变量规则]</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Route::get(<span class="string">'api/:version/product/:id'</span>, <span class="string">'api/:version.Product/getOne'</span>, [], [<span class="string">'id'</span>=&gt;<span class="string">'\d+'</span>]);</span><br></pre></td></tr></table></figure><h3 id="9-10-2-路由分组"><a class="markdownIt-Anchor" href="#9-10-2-路由分组"></a> 9-10-2 路由分组</h3><p>对路由配置文件中，具有相同路由前缀的路由归为同一路由组，例如：</p><p>对于几个对应产品信息的路由，</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Route::get(<span class="string">'api/:version/product/recent'</span>, <span class="string">'api/:version.Product/getRecent'</span>);</span><br><span class="line">Route::get(<span class="string">'api/:version/product/by_category'</span>, <span class="string">'api/:version.Product/getAllInCategory'</span>);</span><br><span class="line">Route::get(<span class="string">'api/:version/product/:id'</span>, <span class="string">'api/:version.Product/getOne'</span>);</span><br></pre></td></tr></table></figure><p>可以分组到产品组路由下，</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 闭包方式注册路由分组</span></span><br><span class="line">Route::group(<span class="string">'api/:version/product'</span>, <span class="function"><span class="keyword">function</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Route::get(<span class="string">'recent'</span>, <span class="string">'api/:version.Product/getRecent'</span>);</span><br><span class="line">    Route::get(<span class="string">'by_category'</span>, <span class="string">'api/:version.Product/getAllInCategory'</span>);</span><br><span class="line">    Route::get(<span class="string">':id'</span>, <span class="string">'api/:version.Product/getOne'</span>, [], [<span class="string">'id'</span> =&gt; <span class="string">'\d+'</span>]);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>或者：</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 数组方式注册路由分组</span></span><br><span class="line">Route::group(<span class="string">'api/:version/product'</span>, [</span><br><span class="line">    <span class="string">'recent'</span> =&gt; [<span class="string">'api/:version.Product/getRecent'</span>],</span><br><span class="line">    <span class="string">'by_category'</span> =&gt; [<span class="string">'api/:version.Product/getAllInCategory'</span>],</span><br><span class="line">    <span class="string">':id'</span> =&gt; [<span class="string">'api/:version.Product/getOne'</span>, [], [<span class="string">'id'</span> =&gt; <span class="string">'\d+'</span>]]</span><br><span class="line">],[<span class="string">'method'</span> =&gt; <span class="string">'get'</span>]);</span><br></pre></td></tr></table></figure><p>路由分组的方式定义路由，执行的效率会比一般形式高一点。</p><p>【注】路由分组的公共路由定义时，不能在末尾加<code>/</code>，否则会报控制器不存在的错误</p><h3 id="9-11-闭包函数构建查询器"><a class="markdownIt-Anchor" href="#9-11-闭包函数构建查询器"></a> 9-11 闭包函数构建查询器</h3><p>1.完成的商品详情的数据信息格式为：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">&quot;id&quot;: 11,</span><br><span class="line">&quot;name&quot;: &quot;贵妃笑 100克&quot;,</span><br><span class="line">&quot;price&quot;: &quot;0.01&quot;,</span><br><span class="line">&quot;stock&quot;: 994,</span><br><span class="line">&quot;main_img_url&quot;: &quot;http://mypro.com/static/images/product-dryfruit-a@6.png&quot;,</span><br><span class="line">&quot;summary&quot;: null,</span><br><span class="line">&quot;img_id&quot;: 39,</span><br><span class="line">&quot;imgs&quot;:[</span><br><span class="line">    &#123;</span><br><span class="line">        &quot;id&quot;: 4,</span><br><span class="line">        &quot;order&quot;: 1,</span><br><span class="line">        &quot;img_url&quot;:&#123;</span><br><span class="line">            &quot;url&quot;: &quot;http://mypro.com/static/images/detail-1@1-dryfruit.png&quot;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    &#123;</span><br><span class="line">        &quot;id&quot;: 5,</span><br><span class="line">        &quot;order&quot;: 2,</span><br><span class="line">        &quot;img_url&quot;:&#123;</span><br><span class="line">            &quot;url&quot;: &quot;http://mypro.com/static/images/detail-2@1-dryfruit.png&quot;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">],</span><br><span class="line">&quot;properties&quot;:[</span><br><span class="line">    &#123;</span><br><span class="line">        &quot;id&quot;: 1,</span><br><span class="line">        &quot;name&quot;: &quot;品名&quot;,</span><br><span class="line">        &quot;detail&quot;: &quot;杨梅&quot;</span><br><span class="line">    &#125;,</span><br><span class="line">]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>2.问题：其中<code>imgs</code>的值为每个商品下的所有图片介绍，所以所有图片之间一定存在一定的顺序，其中<code>imgs</code>数组下的数据中存在<code>order</code>排序字段，如何对<code>imgs</code>的数据通过<code>order</code>进行排序？</p><p>3.【答】：使用闭包函数构建查询器【相当于拼接 sql】。</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">$product = <span class="keyword">self</span>::with([</span><br><span class="line">        <span class="string">'imgs'</span> =&gt; <span class="function"><span class="keyword">function</span><span class="params">($query)</span> </span>&#123;</span><br><span class="line">            $query-&gt;with([<span class="string">'imgUrl'</span>])-&gt;order(<span class="string">'order asc'</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    ])</span><br><span class="line">    -&gt;with([<span class="string">'properties'</span>])</span><br><span class="line">    -&gt;find($id);</span><br></pre></td></tr></table></figure><p>4.思路分析：</p><p>（1）要对 imgs 下的数据进行处理，需要获取到每组数据，然后对<code>order</code>字段进行排序。【通过闭包函数获取到每组数据】</p><p>（2）除了要对每组数据进行按<code>order</code>排序，还需要处理<code>img_url</code>。【通过 with 链式操作处理<code>img_url</code>】</p><p>5.关于闭包函数的理解：</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">'imgs'</span> =&gt; <span class="function"><span class="keyword">function</span><span class="params">($query)</span> </span>&#123;</span><br><span class="line">    $query-&gt;with([<span class="string">'imgUrl'</span>])-&gt;order(<span class="string">'order asc'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对于数组<code>imgs</code>，通过闭包函数，获取到每组数据，其中<code>$query</code>即作为参数接收每组数据的值，然后再对每组数据的<code>img_url</code>通过 with 进行数据关联。</p><h3 id="9-12-用户收货地址"><a class="markdownIt-Anchor" href="#9-12-用户收货地址"></a> 9-12 用户收货地址</h3><p>1.需求说明：</p><p>用户收货地址接口信息需要进行身份验证，登录用户只能查看和操作自己的地址信息，未登录用户不能访问。</p><p>为简化操作，当前将用户和用户地址的关联关系设定为一对一。</p><p>2.思考点：</p><p>（1）对登录状态的判断：</p><p>当用户访问小程序时，调用<code>wx.login()</code>方法，并生成<code>code</code>,后台接口拿到 code 后生成 token，并用 token 以及配置的<code>app_id</code>和<code>app_secret</code>请求微信接口，并获取微信返回的<code>openid</code>等信息，存储到缓存中<br>[以 token 为键，uid|wxResult|scope 组成的 json 数据为值]</p><p>所以，创建或修改用户地址信息时，在处理地址信息和用户信息的关联时，使用的用户信息，应当是当前登录用户的信息，而不能是客户端传递的用户信息参数[可能传递有误，导致误操作到其他用户的地址信息]</p><p>实现一定程度上的接口保护。</p><p>（2）传入参数的检验</p><p>验证器校验往往只能验证某个字段或某些字段的合法性，而客户端可能传入的参数比需要的参数多，或者传入了<code>uid</code>或者<code>user_id</code>，导致更新时覆盖了其他用户的数据信息，对系统的安全性造成影响，<br>所以，在接收客户端传入参数时，需要进行多余字段的过滤。</p><p>（3）对手机号的验证</p><p>正则表达式的应用场景，正则模式<code>^1(3|4|5|6|7|8)[0-9]\d{8}$^</code></p><p>（4）<strong>通过模型关联，实现用户地址的新增和更新</strong>【新】</p><p>通过关联模型方法，创建数据</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 新增</span></span><br><span class="line">$user-&gt;address()-&gt;save($dataArray);</span><br></pre></td></tr></table></figure><p>通过关联模型属性，对当前属性对应的记录进行更新</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 更新</span></span><br><span class="line">$user-&gt;address-&gt;save($dataArray);</span><br></pre></td></tr></table></figure><p>（5）模型关联方法的选择：</p><p>模型关联方法的区分：</p><pre><code>有主键关联无主键 =》 belongsTo无主键关联有主键 =》 hasOne|hasMany</code></pre><p>（6）HTTP 状态码</p><p>200：操作成功，服务器已成功处理了请求。说明：<a href="https://blog.csdn.net/u014028956/article/details/47125403" target="_blank" rel="noopener">如果是对您的 robots.txt 文件显示此状态码，则表示 Googlebot 已成功检索到该文件</a></p><p>201：创建成功，表示服务器执行成功，并且创建了新的资源</p><p>设置接口调用成功后的状态码标识：</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">return</span> json(<span class="keyword">new</span> SuccessMessage(), <span class="number">201</span>);</span><br></pre></td></tr></table></figure><h3 id="9-12-1-通过令牌获取用户标识"><a class="markdownIt-Anchor" href="#9-12-1-通过令牌获取用户标识"></a> 9-12-1 通过令牌获取用户标识</h3><p>(1)定义控制器方法 <code>createOrUpdate()</code></p><p>(2)定义路由 <code>api/:version/address</code></p><p>(3)验证器验证用户输入数据 [<code>name</code>, <code>mobile</code>, <code>province</code>, <code>city</code>, <code>country</code>, <code>detail</code>]</p><p>(4)异常处理信息提示</p><p>当数据不合法时抛出异常，而当操作成功时，也需要返回相应的数据信息。当前项目将抛出的成功信息也放在异常处理类库下。</p><h3 id="9-12-2-面向对象的方式封装获取-uid-方法"><a class="markdownIt-Anchor" href="#9-12-2-面向对象的方式封装获取-uid-方法"></a> 9-12-2 面向对象的方式封装获取 uid 方法</h3><p>1.通过令牌 token 即可获取缓存中对应的用户信息，而缓存中的信息包括<code>uid</code> <code>scope</code> <code>wxResult</code>[<code>openid</code> <code>session_key</code>]</p><p>而在 http 请求时，token 保存在 header 头信息中，获取头信息中<code>token</code>的方法：</p><p><code>$token = Request::instance()-&gt;header('token');</code></p><p>2.通过 json 键值对的键，获取 cache 数据</p><p><code>Cache::get($token)</code></p><p>3.增强项目的扩展性，可将通过 token 获取变量的方法进行封装。</p><p>4.代码实现：</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="function"><span class="keyword">function</span> <span class="title">getCurrentTokenVar</span><span class="params">($key)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $token = Request::instance()-&gt;header(<span class="string">'token'</span>);</span><br><span class="line">    $vars  = Cache::get($token);</span><br><span class="line">    <span class="keyword">if</span> ( ! $vars) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> TokenException();</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (!is_array($vars)) &#123;</span><br><span class="line">            $vars = json_decode($vars, <span class="keyword">true</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">isset</span>($vars[$key])) &#123;</span><br><span class="line">            <span class="keyword">return</span> $vars[$key];</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="keyword">Exception</span>(<span class="string">'尝试获取的Token变量不存在'</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="function"><span class="keyword">function</span> <span class="title">getCurrentUid</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $uid = <span class="keyword">self</span>::getCurrentTokenVar(<span class="string">'uid'</span>);</span><br><span class="line">    <span class="keyword">return</span> $uid;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="9-12-3-模型新增和更新"><a class="markdownIt-Anchor" href="#9-12-3-模型新增和更新"></a> 9-12-3 模型新增和更新</h3><p>通过用户模型，进行面向对象方式的新增和更新</p><p>（1）user 模型定义 address()关联方法，获取到用户地址信息，当用户地址信息不存在时，也通过<strong>关联模型方法</strong>，保存地址信息</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 新增</span></span><br><span class="line">$user-&gt;address()-&gt;save($dataArray);</span><br></pre></td></tr></table></figure><p>（2）user 模型通过 address()关联方法关联 user_address 数据表中对应的用户地址信息，通过关联获取的数据仍然可以作为模型的属性值使用，<br>再通过<strong>关联模型属性</strong>，对当前属性对应的记录进行更新 [包含主键 id]</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 更新</span></span><br><span class="line">$user-&gt;address-&gt;save($dataArray);</span><br></pre></td></tr></table></figure><h3 id="9-12-4-参数过滤"><a class="markdownIt-Anchor" href="#9-12-4-参数过滤"></a> 9-12-4 参数过滤</h3><p>封装处理客户端传入的参数的方法，由于当前用户的信息是通过缓存获取的，为避免用户传入的参数造成错误修改，所以需要对客户端传入数据进行过滤，<br>如果携带用户 id 参数，则抛出异常，不再继续处理。除此之外，对于传入的无效、多余数据，进行过滤，仅接收验证器需要验证的字段信息。</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">getDataByRule</span><span class="params">($params)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">isset</span>($params[<span class="string">'uid'</span>]) || <span class="keyword">isset</span>($params[<span class="string">'user_id'</span>])) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> ParameterException([</span><br><span class="line">            <span class="string">'msg'</span> =&gt; <span class="string">'参数中包含非法的参数名user_id或者uid'</span></span><br><span class="line">        ]);</span><br><span class="line">    &#125;</span><br><span class="line">    $newArray = [];</span><br><span class="line">    <span class="keyword">foreach</span> (<span class="keyword">$this</span>-&gt;rule <span class="keyword">as</span> $key =&gt; $value) &#123;</span><br><span class="line">        $newArray[$key] = $params[$key];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> $newArray;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="9-12-5-接口测试"><a class="markdownIt-Anchor" href="#9-12-5-接口测试"></a> 9-12-5 接口测试</h3><p>1.需要的参数</p><ul><li><p>token: header 请求头 [通过微信小程序的开发者工具]</p></li><li><p>address 字段信息 [<code>name</code>, <code>mobile</code>, <code>province</code>, <code>city</code>, <code>country</code>, <code>detail</code>]</p></li></ul><p>2.返回的数据</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">"code"</span>: <span class="number">201</span>,</span><br><span class="line">  <span class="attr">"msg"</span>: <span class="string">"ok"</span>,</span><br><span class="line">  <span class="attr">"errorCode"</span>: <span class="number">0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>并且通过设置返回值为带状态码的 json 数据，<code>json(new SuccessMessage(), 201)</code>，可将 http 的状态码也设置为<code>201</code></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;ThinkPHP5 从入门到深入学习，结合实战项目深入理解 ThinkPHP5 的特性和使用方法。深入学习 api 开发，学习微信登录和令牌的相关知识，并理解微信登录流程，完成与微信开放 api 之间的数据交互，完善项目的相应功能。&lt;br&gt;
理解第三方登录授权的 code 和 token 交互过程。&lt;/p&gt;
    
    </summary>
    
      <category term="PHP" scheme="https://blog.i2arch.tech/categories/PHP/"/>
    
    
      <category term="thinkphp5" scheme="https://blog.i2arch.tech/tags/thinkphp5/"/>
    
      <category term="微信小程序" scheme="https://blog.i2arch.tech/tags/%E5%BE%AE%E4%BF%A1%E5%B0%8F%E7%A8%8B%E5%BA%8F/"/>
    
  </entry>
  
  <entry>
    <title>PHP性能优化</title>
    <link href="https://blog.i2arch.tech/2018/07/09/php-optimize/"/>
    <id>https://blog.i2arch.tech/2018/07/09/php-optimize/</id>
    <published>2018-07-09T00:57:55.000Z</published>
    <updated>2018-11-16T12:14:52.003Z</updated>
    
    <content type="html"><![CDATA[<p>PHP 运行环境的性能考虑在 php 深入学习中需要逐步强化意识，并着手实现，其中对于性能分析的相关工具也需要有一定的掌握，比如压力测试工具 Apache Benchmark，Opcode 代码分析工具 vld，PHP 性能分析工具 XHProf，另外，对于日常编写代码时，也需要考虑 PHP 自身的特性，进行扬长避短，使用 isset 而不用 array_key_exists 方法，以及尽可能规避 PHP 自带的魔术方法。对 PHP 的运行流程也需要有一个大致的了解，知道 Opcode 在 PHP 执行过程中的阶段。需要逐渐加深对 PHP 深层次的思考。</p><a id="more"></a><h1 id="php-性能优化"><a class="markdownIt-Anchor" href="#php-性能优化"></a> PHP 性能优化</h1><h2 id="一-语言级性能优化一"><a class="markdownIt-Anchor" href="#一-语言级性能优化一"></a> 一、语言级性能优化(一)</h2><p>PHP 性能问题的解决方向</p><pre><code>PHP语言级别的性能优化 =》 PHP周边问题的性能优化 =》 PHP语言自身分析、优化</code></pre><h3 id="1压力测试工具-apache-benchmark-ab"><a class="markdownIt-Anchor" href="#1压力测试工具-apache-benchmark-ab"></a> 1.压力测试工具 Apache Benchmark (ab)</h3><h4 id="1测试工具基本介绍"><a class="markdownIt-Anchor" href="#1测试工具基本介绍"></a> (1)测试工具基本介绍</h4><p>1). ab 是由 Apache 提供的压力测试软件，安装 apache 服务器时会自带该压力测试软件</p><p>2). 基本使用[Linux 平台]</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">./ab -n1000 -c100 http://www.baidu.com/</span><br><span class="line"></span><br><span class="line"><span class="comment"># -n 请求数</span></span><br><span class="line"><span class="comment"># -c 并发数</span></span><br><span class="line"><span class="comment"># url 目标压力测试地址</span></span><br></pre></td></tr></table></figure><p>3). 参考项</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 每秒接收请求数 =》 尽可能大</span></span><br><span class="line">Requests per second (mean)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 每一个请求的耗时情况 =》 尽可能小</span></span><br><span class="line">Time per request (mean, across all concurrent requests)</span><br></pre></td></tr></table></figure><h3 id="2php-自身能力"><a class="markdownIt-Anchor" href="#2php-自身能力"></a> 2.PHP 自身能力</h3><h4 id="1优化点少写代码多用-php-自身能力"><a class="markdownIt-Anchor" href="#1优化点少写代码多用-php-自身能力"></a> (1)优化点：少写代码，多用 PHP 自身能力</h4><p>1). 性能问题：自身代码冗余较多，可读性不佳，并且性能低。</p><p>2). 为什么性能低：PHP 代码需要编译解释为底层语言，这一过程每次请求都会处理一遍，开销大。</p><p>3). 好的方法：多实用 PHP 内置变量、常量、函数。</p><h4 id="2php-代码运行流程"><a class="markdownIt-Anchor" href="#2php-代码运行流程"></a> (2)PHP 代码运行流程</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">.php文件 =&gt;[zend引擎 Scanner]=&gt; zend exprs =&gt; [Parser] =&gt; Opcodes(要被执行的代码) =&gt; [Exec] =&gt; Output</span><br></pre></td></tr></table></figure><p>【补充】目前很多 php 的缓存服务使用的都是 opcode，节省了扫描和解析的过程，提升速度。</p><h4 id="3php-内置函数之间的性能测试"><a class="markdownIt-Anchor" href="#3php-内置函数之间的性能测试"></a> (3)PHP 内置函数之间的性能测试</h4><p>1). <code>array_key_exists()</code> vs <code>isset()</code></p><p>php 执行效率上： isset &gt; array_key_exists</p><blockquote><p>【插曲】：在接触的项目中，大多数情况下确实使用的也是 isset()，但是记得某次看到同事写的代码中有 array_key_exists()方法时，自己查看了手册，确认了这个方法的使用方法后，还特地将 isset 换成 array_key_exists 方法进行编写代码，粗浅的认为 array_key_exists()方法要比 isset()方法高级，学习了性能分析之后，顿时觉得之前的认识很是浅陋，也意识到项目中之所有 isset()更为常见是合理的。</p></blockquote><h2 id="二-语言级性能优化二"><a class="markdownIt-Anchor" href="#二-语言级性能优化二"></a> 二、语言级性能优化(二)</h2><h3 id="1优化点减少-php-魔法函数的使用"><a class="markdownIt-Anchor" href="#1优化点减少-php-魔法函数的使用"></a> 1.优化点：减少 PHP 魔法函数的使用</h3><h4 id="1-情况描述"><a class="markdownIt-Anchor" href="#1-情况描述"></a> (1). 情况描述：</h4><p>PHP 提供的魔法函数，性能不佳</p><h4 id="2-为什么性能低"><a class="markdownIt-Anchor" href="#2-为什么性能低"></a> (2). 为什么性能低：</h4><p>为了给 PHP 程序员省事，PHP 语言为你做了很多</p><h4 id="3-好的方法"><a class="markdownIt-Anchor" href="#3-好的方法"></a> (3). 好的方法：</h4><p>尽可能规避使用 PHP 魔法函数</p><p>【补充】：命令行模式查看 php 文件执行耗时</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># time命令</span></span><br><span class="line">time php test.php</span><br><span class="line"></span><br><span class="line"><span class="comment"># 输出结果</span></span><br><span class="line">real</span><br><span class="line">user =&gt; 主要参考的耗时</span><br><span class="line">sys</span><br></pre></td></tr></table></figure><h3 id="2优化点-产生额外开销的错误抑制符"><a class="markdownIt-Anchor" href="#2优化点-产生额外开销的错误抑制符"></a> 2.优化点： 产生额外开销的错误抑制符 <code>@</code></h3><h4 id="1情况描述"><a class="markdownIt-Anchor" href="#1情况描述"></a> (1).情况描述：</h4><p>PHP 提供的错误已支付只是为了方便懒人</p><h4 id="2-的实际逻辑"><a class="markdownIt-Anchor" href="#2-的实际逻辑"></a> (2). <code>@</code> 的实际逻辑：</h4><p>在代码开始前、结束后，增加 Opcode，忽略报错<br>vld - PHP Opcode 查看扩展</p><h4 id="3错误抑制符的性能测试"><a class="markdownIt-Anchor" href="#3错误抑制符的性能测试"></a> (3).错误抑制符的性能测试</h4><p>1)测试文件<code>at.php</code></p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//at.php</span></span><br><span class="line">file_get_contens(<span class="string">'xxx'</span>); <span class="comment">// xxx文件不存在</span></span><br></pre></td></tr></table></figure><p>2)使用 vld 扩展执行<code>at.php</code>，查看执行过程的完整 Opcode</p><p><code>php -dvld.active=1 -dvld.execute=0 at.php</code></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 不加@错误抑制符时Opcode的执行情况</span></span><br><span class="line">SEND_VAL</span><br><span class="line">DO_FCALL</span><br><span class="line">RETURN</span><br><span class="line"></span><br><span class="line"><span class="comment"># 加@错误抑制符时Opcode的执行情况</span></span><br><span class="line">BEGIN_SILENCE</span><br><span class="line">SEND_VAL</span><br><span class="line">DO_FCALL</span><br><span class="line">END_SILENCE</span><br><span class="line">RETURN</span><br></pre></td></tr></table></figure><h4 id="4好的建议尽量不要使用错误抑制符"><a class="markdownIt-Anchor" href="#4好的建议尽量不要使用错误抑制符"></a> (4).好的建议：尽量不要使用@错误抑制符</h4><p>可以使用 try throw 方式进行错误控制</p><h3 id="3优化点-合理使用内存"><a class="markdownIt-Anchor" href="#3优化点-合理使用内存"></a> 3.优化点： 合理使用内存</h3><h4 id="1情况描述-2"><a class="markdownIt-Anchor" href="#1情况描述-2"></a> (1)情况描述：</h4><p>PHP 有内存回收机制保底，但也要小心使用内存</p><h4 id="2好的建议"><a class="markdownIt-Anchor" href="#2好的建议"></a> (2)好的建议：</h4><p>利用 unset()及时释放不使用的内存[注：unset()出现注销不掉的情况]</p><h3 id="4优化点-尽量少的使用正则表达式"><a class="markdownIt-Anchor" href="#4优化点-尽量少的使用正则表达式"></a> 4.优化点： 尽量少的使用正则表达式</h3><h4 id="1-情况描述-2"><a class="markdownIt-Anchor" href="#1-情况描述-2"></a> (1) 情况描述：</h4><p>正则表达式的回溯开销比较大，“没有金刚钻别揽瓷器活”</p><h4 id="2-好的建议"><a class="markdownIt-Anchor" href="#2-好的建议"></a> (2) 好的建议：</h4><p>利用字符串处理函数，实现相同逻辑</p><h3 id="5优化点避免在循环内做运算"><a class="markdownIt-Anchor" href="#5优化点避免在循环内做运算"></a> 5.优化点：避免在循环内做运算</h3><h4 id="1-情况描述-3"><a class="markdownIt-Anchor" href="#1-情况描述-3"></a> (1) 情况描述：</h4><p>循环内的计算式将会被重复计算</p><h4 id="2-代码示例"><a class="markdownIt-Anchor" href="#2-代码示例"></a> (2) 代码示例：</h4><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">$str = <span class="string">"hello world"</span>;</span><br><span class="line"><span class="keyword">for</span> ($i=<span class="number">0</span>; $i &lt; strlen($str); $i++) &#123;</span><br><span class="line">    <span class="comment"># code...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 其中strlen()方法会在每次循环时计算一次</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 进行优化</span></span><br><span class="line">$str = <span class="string">"hello world"</span>;</span><br><span class="line">$strlen = strlen($str);</span><br><span class="line"><span class="keyword">for</span> ($i=<span class="number">0</span>; $i &lt; $strlen; $i++) &#123;</span><br><span class="line">    <span class="comment"># code...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="6优化点-减少计算密集型业务"><a class="markdownIt-Anchor" href="#6优化点-减少计算密集型业务"></a> 6.优化点： 减少计算密集型业务</h3><h4 id="1-情况描述-4"><a class="markdownIt-Anchor" href="#1-情况描述-4"></a> (1) 情况描述：</h4><p>PHP 不适合密集型运算的场景[大批量日志分析、大批量数据处理]</p><h4 id="2-为什么"><a class="markdownIt-Anchor" href="#2-为什么"></a> (2) 为什么？</h4><p>PHP 语言特性决定了 PHP 不适合做大数据量运算。[需要解析成 C 语言进行运算，C 语言可能几行代码就实现的计算，php 可能需要很多行代码才能实现]</p><h4 id="3-php-适用场景"><a class="markdownIt-Anchor" href="#3-php-适用场景"></a> (3) PHP 适用场景：</h4><p>适合衔接 Webserver 与后端服务、UI 呈现[纽带]</p><h3 id="7优化点-务必适用带引号字符串做键值"><a class="markdownIt-Anchor" href="#7优化点-务必适用带引号字符串做键值"></a> 7.优化点： 务必适用带引号字符串做键值</h3><h4 id="1-情况描述-5"><a class="markdownIt-Anchor" href="#1-情况描述-5"></a> (1) 情况描述：</h4><p>PHP 会将没有引号的键值当做常量，产生查找常量的开销</p><p>【补充】：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 将后台运行的任务放到前台终端运行</span></span><br><span class="line"><span class="built_in">fg</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 相关命令： jobs  bg  fg</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 将任务号为1的任务从后台执行转换到其那台执行。</span></span><br><span class="line"><span class="built_in">fg</span> 1</span><br><span class="line"></span><br><span class="line"><span class="comment"># 执行上述命令后，命令行窗口将显示如下信息</span></span><br><span class="line">find / -name password</span><br></pre></td></tr></table></figure><h4 id="2-程序说明"><a class="markdownIt-Anchor" href="#2-程序说明"></a> (2) 程序说明</h4><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">define(<span class="string">'key'</span>, <span class="string">'imooc'</span>);</span><br><span class="line"></span><br><span class="line">$array = <span class="keyword">array</span>(</span><br><span class="line">    <span class="string">'key'</span> =&gt; <span class="string">'hello world!'</span>,</span><br><span class="line">    <span class="string">'imooc'</span> =&gt; <span class="string">'http://www.imooc.com/'</span></span><br><span class="line">);</span><br><span class="line"><span class="keyword">echo</span> $array[<span class="string">"key"</span>] . <span class="string">'\n'</span>; <span class="comment">// 输出 hello world</span></span><br><span class="line"><span class="keyword">echo</span> $array[key] . <span class="string">'\n'</span>; <span class="comment">// 输出 http://www.imooc.com/</span></span><br></pre></td></tr></table></figure><p>【说明】：</p><p>当时用<code>$array[key]</code>时，程序也把 key 作为常量去查找，当查找到时，获取到常量的值；当没查找到时，再到数组内部，将其作为键 key 字符串进行解析</p><h4 id="3-好的建议"><a class="markdownIt-Anchor" href="#3-好的建议"></a> (3) 好的建议：</h4><p>严格使用引号作为键值</p><h2 id="三-php-周边问题的分析与阐述"><a class="markdownIt-Anchor" href="#三-php-周边问题的分析与阐述"></a> 三、PHP 周边问题的分析与阐述</h2><h3 id="1php-周边范围"><a class="markdownIt-Anchor" href="#1php-周边范围"></a> 1.PHP 周边范围：</h3><p>Linux 运行环境</p><p>文件存储[磁盘]</p><p>数据库[mysql]</p><p>缓存[硬件的内存、php 缓存技术：memcache redis]</p><p>网络</p><h3 id="2php-周边对-php-程序的影响分析"><a class="markdownIt-Anchor" href="#2php-周边对-php-程序的影响分析"></a> 2.PHP 周边对 PHP 程序的影响分析</h3><h4 id="1-连接数据库操作"><a class="markdownIt-Anchor" href="#1-连接数据库操作"></a> (1) 连接数据库操作</h4><p>1） 同一台服务器 =&gt; 数据库优化 决定时间性能</p><p>2） 分布式服务器 =&gt; 数据库优化 + 网络速度 决定时间性能</p><h4 id="2-减少文件类操作"><a class="markdownIt-Anchor" href="#2-减少文件类操作"></a> (2) 减少文件类操作</h4><p>1） 常见 PHP 场景的开销次序：</p><p>读写磁盘、 读写数据库、读写内存、读写网络数据</p><p>2） 时间开销：</p><pre><code>    读写内存 &lt;&lt;(远小于) 读写数据库[基于文件系统，操作本地磁盘] &lt;(小于) 读写磁盘 &lt; 读写网络数据    数据库会使用内存作为缓存，将其热数据先缓存在内存中，异步地写入到数据库 =》 数据库介于内存和磁盘之间    网络数据：通过socket发起，socket使用的是本地的文件句柄，磁盘操作。受网络延迟影响，延迟大时远远小于读写磁盘，延迟小时和读写磁盘差不多。</code></pre><p>3） 总结：</p><p>尽可能多的使用读写数据库、读写内存，尽量规避操作磁盘和操作网络数据。</p><h4 id="3-优化网络请求"><a class="markdownIt-Anchor" href="#3-优化网络请求"></a> (3) 优化网络请求</h4><p>网络请求的坑：</p><p>1.对方接口的不确定因素 2.网络稳定性</p><p>如何优化网络请求？</p><p>1.设置超时时间<br>a) 连接超时<br>b) 读超时<br>c) 写超时</p><p>2.将串行请求并行化<br>a) 使用 curl<em>multi</em>*() =&gt; 最简单，但是并不是最好用<br>b) 使用 swoole 扩展 =&gt; 效果更好</p><h4 id="4-压缩-php-接口输出"><a class="markdownIt-Anchor" href="#4-压缩-php-接口输出"></a> (4) 压缩 PHP 接口输出</h4><p>1） 如何压缩？</p><p>使用 Gzip 即可。</p><p>2） 利与弊：<br>利： 利于我们的数据输出，Client 端能更快获取数据<br>弊： 额外的 CPU 开销</p><h4 id="5-php-缓存复用"><a class="markdownIt-Anchor" href="#5-php-缓存复用"></a> (5) PHP 缓存复用</h4><p>1）什么情况下做输出内容的缓存？</p><p>多次请求，内容不变的情况。[模板缓存]</p><pre><code>      |---Cached----/\     \/              |    .php &lt;-------&gt; Cache -&gt; No Cache                    /\          |                     |---------\/</code></pre><h4 id="6-重叠时间窗口思想"><a class="markdownIt-Anchor" href="#6-重叠时间窗口思想"></a> (6) 重叠时间窗口思想</h4><p>串行：</p><pre><code>process1[客户端]  process2[web server]  process3[php]  process4[mysql或缓存]</code></pre><p>重叠时间窗口：</p><pre><code>process1    process2        process3            process4</code></pre><p>使用前提：后一个任务不强依赖于前一个任务的输出或返回。</p><h4 id="7-php-旁路处理方案"><a class="markdownIt-Anchor" href="#7-php-旁路处理方案"></a> (7) PHP 旁路处理方案</h4><pre><code>x.php                         x.php  |                             |process1                      process1  |                             |---------process2process2                      process3       |  |                             |&lt;------------process3                      process4  |process4</code></pre><p>使用前提：后一个任务不强依赖于前一个任务的输出或返回。</p><h2 id="四-具体的性能分析"><a class="markdownIt-Anchor" href="#四-具体的性能分析"></a> 四、具体的性能分析</h2><h3 id="1-借助-xhprof-工具分析-php-性能"><a class="markdownIt-Anchor" href="#1-借助-xhprof-工具分析-php-性能"></a> 1. 借助 xhprof 工具分析 PHP 性能</h3><p>工具： XHProf [源自 Facebook 的 PHP 性能分析工具]</p><p>实践： 分析 Wordpress 程序，做优化</p><h4 id="1准备工作"><a class="markdownIt-Anchor" href="#1准备工作"></a> (1)准备工作</h4><p>1） 检查 xhprof 工具是否安装成功</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">php --ri xhprof</span><br><span class="line"></span><br><span class="line">//输出</span><br><span class="line">xhprof</span><br><span class="line">xhprof =&gt; 0.9.2</span><br><span class="line">CPU num =&gt; 1</span><br></pre></td></tr></table></figure><p>2） 在 wordpress 代码中[index.php 文件]</p><pre><code>1.在起始位置添加`xhprof_enable()`进行开启xhprof2.在执行结束位置添加`xhprof_disable()`返回性能分析数据3.添加xhprof的两个lib文件</code></pre><p>wordpress/index.php 代码</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">xhprof_enable();</span><br><span class="line"></span><br><span class="line">define(<span class="string">'WP_USE_THEMES'</span>, <span class="keyword">true</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">require</span>(dirname(<span class="keyword">__FILE__</span>) . <span class="string">'/wp-blog-header.php'</span>);</span><br><span class="line"></span><br><span class="line">$data = xhprof_disable();</span><br><span class="line"></span><br><span class="line"><span class="keyword">include_once</span> <span class="string">"/var/www/html/xhprof_lib/utils/xhprof_lib.php"</span>;</span><br><span class="line"><span class="keyword">include_once</span> <span class="string">"/var/www/html/xhprof_lib/utils/xhprof_runs.php"</span>;</span><br><span class="line"></span><br><span class="line">$objXhprofRun = <span class="keyword">new</span> XHProfRuns_Default();</span><br><span class="line">$run_id = $objXhprofRun-&gt;save_run($data, <span class="string">"test"</span>);</span><br><span class="line">var_dump($run_id);</span><br></pre></td></tr></table></figure><p>3） wordpress 同级目录下的 xhp 目录</p><pre><code>callgraph.phpcss/docs/index.phpjquery/js/typeahead.php</code></pre><p>4）浏览器访问 xhp/index.php</p><pre><code>查看列表形式的性能分析 =》 View Full Callgraph =》 查看图形流程分析[查看耗时最长的文件和方法]=&gt;MO::import_from_reader| MO::make_entry</code></pre><p>5）查找最需要优化的文件</p><p><code>grep 'import_from_reader' ./ -r</code></p><p>6）主要耗时点：<code>MO::make_entry()</code> – 执行的是多语言支持的功能</p><p>优化方式：直接注释掉调用的相关代码</p><p>【补充】：<code>MO</code>和<code>PO</code>都是 PHP 处理多语言程序的语言包</p><p>7）优化结果：</p><p>让程序不再是只有一个或一处相互关联的高消耗代码点。</p><h3 id="2-php-性能分析工具扩展"><a class="markdownIt-Anchor" href="#2-php-性能分析工具扩展"></a> 2. PHP 性能分析工具扩展</h3><p>XHProf – PHP 性能分析工具</p><p>ab – 压力测试</p><p>vld – opcode 代码分析</p><h2 id="五-php-性能瓶颈终极办法"><a class="markdownIt-Anchor" href="#五-php-性能瓶颈终极办法"></a> 五、PHP 性能瓶颈终极办法</h2><h3 id="1opcode-cache-php-扩展apc"><a class="markdownIt-Anchor" href="#1opcode-cache-php-扩展apc"></a> 1.Opcode Cache: PHP 扩展<code>APC</code></h3><p>官方地址：<a href="http://pecl.php.net/package-search.php?pkg_name=apc&amp;bool=AND&amp;submit=Search" target="_blank" rel="noopener">前往</a></p><p>已不再更新。</p><p>【补充】：<code>pecl.php.net</code>是 php 扩展的官方维护站点。了解并使用<a href="http://pecl.php.net/packages.php?catpid=3&amp;catname=Caching" target="_blank" rel="noopener">类似 APC 的 opcode 缓存扩展</a>？</p><p>yac - 通过共享内存来缓存用户数据，用于代替 APC 和本地 memcached 的方案。</p><h3 id="2扩展实现通过-php-扩展代替原-php-代码中高频逻辑"><a class="markdownIt-Anchor" href="#2扩展实现通过-php-扩展代替原-php-代码中高频逻辑"></a> 2.扩展实现：通过 PHP 扩展代替原 PHP 代码中高频逻辑</h3><h3 id="3runtime-优化-hhvm"><a class="markdownIt-Anchor" href="#3runtime-优化-hhvm"></a> 3.Runtime 优化： HHVM</h3>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;PHP 运行环境的性能考虑在 php 深入学习中需要逐步强化意识，并着手实现，其中对于性能分析的相关工具也需要有一定的掌握，比如压力测试工具 Apache Benchmark，Opcode 代码分析工具 vld，PHP 性能分析工具 XHProf，另外，对于日常编写代码时，也需要考虑 PHP 自身的特性，进行扬长避短，使用 isset 而不用 array_key_exists 方法，以及尽可能规避 PHP 自带的魔术方法。对 PHP 的运行流程也需要有一个大致的了解，知道 Opcode 在 PHP 执行过程中的阶段。需要逐渐加深对 PHP 深层次的思考。&lt;/p&gt;
    
    </summary>
    
      <category term="PHP" scheme="https://blog.i2arch.tech/categories/PHP/"/>
    
    
      <category term="optimize" scheme="https://blog.i2arch.tech/tags/optimize/"/>
    
  </entry>
  
  <entry>
    <title>【Linux】网站上线</title>
    <link href="https://blog.i2arch.tech/2018/07/07/web-goserver/"/>
    <id>https://blog.i2arch.tech/2018/07/07/web-goserver/</id>
    <published>2018-07-07T04:00:00.000Z</published>
    <updated>2018-11-16T12:14:52.151Z</updated>
    
    <content type="html"><![CDATA[<p>作为后端程序员，对网站上线的操作需要有一定的了解，对于一些没有专门运维人员的公司，运维上线的操作就需要后端程序员来执行。<br>需要学习域名解析、本地文件和服务器文件传递<code>scp</code>的相关操作。</p><a id="more"></a><h2 id="一-上传网站到服务器"><a class="markdownIt-Anchor" href="#一-上传网站到服务器"></a> 一、上传网站到服务器</h2><h3 id="1将本地文件上传到远程服务器scp"><a class="markdownIt-Anchor" href="#1将本地文件上传到远程服务器scp"></a> 1.将本地文件上传到远程服务器：<strong>scp</strong></h3><h4 id="1-具体用法"><a class="markdownIt-Anchor" href="#1-具体用法"></a> (1) 具体用法：</h4><blockquote><p>scp -r {本地目录的文件} {服务器用户名@远程服务器 ip:{远程服务器的文件目录}}</p></blockquote><p><code>scp -r ./demo/* root@47.94.255.230:/root/www</code></p><h3 id="2centos-系统管理命令"><a class="markdownIt-Anchor" href="#2centos-系统管理命令"></a> 2.CentOS 系统管理命令：</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 使用ssh登录远程服务器</span></span><br><span class="line">ssh &#123;user&#125;@&#123;ipv4&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装scp命令</span></span><br><span class="line">yum install openssh-client</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看进程</span></span><br><span class="line">ps -ef | grep nginx</span><br><span class="line"></span><br><span class="line"><span class="comment"># 验证nginx配置文件语法</span></span><br><span class="line">nginx -t</span><br><span class="line"></span><br><span class="line"><span class="comment"># 关闭nginx服务进程</span></span><br><span class="line">nginx -s stop</span><br><span class="line"></span><br><span class="line"><span class="comment"># 开启nginx服务进程</span></span><br><span class="line">nginx -s reload</span><br></pre></td></tr></table></figure><h2 id="二-域名解析"><a class="markdownIt-Anchor" href="#二-域名解析"></a> 二、域名解析</h2><h3 id="1解析设置"><a class="markdownIt-Anchor" href="#1解析设置"></a> 1.解析设置</h3><table><thead><tr><th style="text-align:center">记录类型</th><th style="text-align:center">主机记录</th><th style="text-align:center">解析线路(jsp)</th><th style="text-align:center">记录值</th><th style="text-align:center">TTL 值</th></tr></thead><tbody><tr><td style="text-align:center">A</td><td style="text-align:center">www</td><td style="text-align:center">默认</td><td style="text-align:center">{ipv4}</td><td style="text-align:center">10 分钟</td></tr><tr><td style="text-align:center">A</td><td style="text-align:center">@</td><td style="text-align:center">默认</td><td style="text-align:center">{ipv4}</td><td style="text-align:center">10 分钟</td></tr></tbody></table><h3 id="2说明"><a class="markdownIt-Anchor" href="#2说明"></a> 2.说明</h3><p>(1) <code>www</code>表示对有<code>www</code>前缀的完整域名进行解析</p><p>(2) <code>@</code>表示对没有<code>www</code>前缀的省略域名进行解析</p><p>(3) <code>CNAME</code>表示需要将域名重定向到另一个域名[使用 github 搭建博客绑定独立域名时需要用到]</p><h2 id="三-http-协议在访问域名时的工作流程"><a class="markdownIt-Anchor" href="#三-http-协议在访问域名时的工作流程"></a> 三、HTTP 协议在访问域名时的工作流程</h2><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">输入网址(imooc.com)</span><br><span class="line">        ||</span><br><span class="line">        \/</span><br><span class="line">DNS解析，寻找对应服务器地址</span><br><span class="line">        ||</span><br><span class="line">        \/</span><br><span class="line">进行第一次握手（HTTP会话）</span><br><span class="line">        ||</span><br><span class="line">        \/</span><br><span class="line">建立文档树，加载资源</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;作为后端程序员，对网站上线的操作需要有一定的了解，对于一些没有专门运维人员的公司，运维上线的操作就需要后端程序员来执行。&lt;br&gt;
需要学习域名解析、本地文件和服务器文件传递&lt;code&gt;scp&lt;/code&gt;的相关操作。&lt;/p&gt;
    
    </summary>
    
      <category term="Linux" scheme="https://blog.i2arch.tech/categories/Linux/"/>
    
    
      <category term="上线" scheme="https://blog.i2arch.tech/tags/%E4%B8%8A%E7%BA%BF/"/>
    
  </entry>
  
  <entry>
    <title>【Linux】系统学习Crontab定时任务</title>
    <link href="https://blog.i2arch.tech/2018/07/06/linux-crontab/"/>
    <id>https://blog.i2arch.tech/2018/07/06/linux-crontab/</id>
    <published>2018-07-06T04:00:00.000Z</published>
    <updated>2018-11-16T12:14:51.923Z</updated>
    
    <content type="html"><![CDATA[<p>crontab 是一个用于设置周期性执行任务的工具。目前服务器端的运行环境大多数为 Linux，在日常的运营和维护中会有很多需要定期执行的操作，其中有些操作是可以机械的定期执行的操作，所以我们可以使用 crontab 定时服务来设置定时任务，从而减少手动操作的任务，帮助提高工作效率。</p><a id="more"></a><h2 id="一-cron-定时任务"><a class="markdownIt-Anchor" href="#一-cron-定时任务"></a> 一、cron 定时任务</h2><h3 id="1安装-crond-服务和-crontab-工具"><a class="markdownIt-Anchor" href="#1安装-crond-服务和-crontab-工具"></a> 1.安装 crond 服务和 crontab 工具</h3><h4 id="1相关命令"><a class="markdownIt-Anchor" href="#1相关命令"></a> (1)相关命令</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 清除yum缓存</span></span><br><span class="line">yum clean all</span><br><span class="line"></span><br><span class="line"><span class="comment"># 更新系统的安装包到最新版本</span></span><br><span class="line">yum update</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装cron服务和crontab工具[-y表示yes,没有的话需要手动输入yes]</span></span><br><span class="line">yum install -y cronie crontabs</span><br></pre></td></tr></table></figure><h4 id="2验证-crond-服务"><a class="markdownIt-Anchor" href="#2验证-crond-服务"></a> (2)验证 crond 服务</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 检查cond服务是否安装及启动：</span></span><br><span class="line">yum list cronie &amp;&amp; systemctl status crond</span><br><span class="line"></span><br><span class="line"><span class="comment"># 检查crontab工具是否安装：</span></span><br><span class="line">yum list crontabs &amp;&amp; <span class="built_in">which</span> crontab &amp;&amp; crontab -l</span><br></pre></td></tr></table></figure><h2 id="二-crontab-架构"><a class="markdownIt-Anchor" href="#二-crontab-架构"></a> 二、crontab 架构</h2><h3 id="1执行步骤"><a class="markdownIt-Anchor" href="#1执行步骤"></a> 1.执行步骤</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1.新建|编辑定时任务</span></span><br><span class="line">crontab -e</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2.查看定时任务列表[当前用户(root)保存的计划任务]</span></span><br><span class="line">crontab -l</span><br><span class="line"><span class="comment">#或</span></span><br><span class="line">cat /var/spool/cron/root</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3.重启crond进程</span></span><br><span class="line">systemctl restart crond</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4.查看crond状态</span></span><br><span class="line">systemctl status crond</span><br></pre></td></tr></table></figure><h3 id="2contab-配置文件格式"><a class="markdownIt-Anchor" href="#2contab-配置文件格式"></a> 2.contab 配置文件格式</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">*     *     *    *    *   [username]   my <span class="built_in">command</span></span><br><span class="line">分    时    日   月   周   [执行用户名]  要运行的命令</span><br><span class="line">0-59 0-23 1-31 1-12  0-6</span><br></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">* 取值范围内的所有数字</span><br><span class="line">/ 每</span><br><span class="line">- 某个区间</span><br><span class="line">, 几个数的集合</span><br></pre></td></tr></table></figure><h3 id="3crontab-配置文件"><a class="markdownIt-Anchor" href="#3crontab-配置文件"></a> 3.crontab 配置文件</h3><h4 id="1系统配置文件"><a class="markdownIt-Anchor" href="#1系统配置文件"></a> (1)系统配置文件</h4><p><code>/etc/crontab</code></p><h4 id="2系统用户-crontab-配置文件保存目录crontab-e-所编辑的文件"><a class="markdownIt-Anchor" href="#2系统用户-crontab-配置文件保存目录crontab-e-所编辑的文件"></a> (2)系统用户 crontab 配置文件保存目录[crontab -e 所编辑的文件]</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">/var/spllo/cron/[目录]</span><br><span class="line">root:/var/spppl/cron/root[文件]</span><br><span class="line">user01:/var/spppl/cron/user01[文件]</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 创建用户</span></span><br><span class="line">useradd user01</span><br><span class="line"><span class="comment"># 使用指定用户登陆系统</span></span><br><span class="line">su - user01</span><br></pre></td></tr></table></figure><h3 id="4crontab-环境变量"><a class="markdownIt-Anchor" href="#4crontab-环境变量"></a> 4.Crontab 环境变量</h3><h4 id="1添加-path-到etccrontab"><a class="markdownIt-Anchor" href="#1添加-path-到etccrontab"></a> (1)添加 PATH 到/etc/crontab</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">PATH=/sbin:/bin:/usr/sbin:/usr/bin:/usr/<span class="built_in">local</span>/jdk1.8.0_111/bin</span><br><span class="line"><span class="comment"># 可以直接在定时任务文件使用java程序</span></span><br></pre></td></tr></table></figure><h4 id="2在执行具体任务前引入系统用户环境变量"><a class="markdownIt-Anchor" href="#2在执行具体任务前引入系统用户环境变量"></a> (2)在执行具体任务前引入系统/用户环境变量</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># [系统级别环境变量]</span></span><br><span class="line">30 2 * * * <span class="built_in">source</span> /etc/profile;sh /root/test.sh</span><br><span class="line"></span><br><span class="line"><span class="comment"># [用户级别环境变量]</span></span><br><span class="line">30 2 * * * <span class="built_in">source</span> ~/.bash_profile;sh /root/test.sh</span><br></pre></td></tr></table></figure><h4 id="3操作步骤"><a class="markdownIt-Anchor" href="#3操作步骤"></a> (3)操作步骤</h4><h5 id="1添加环境变量"><a class="markdownIt-Anchor" href="#1添加环境变量"></a> 1)添加环境变量</h5><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># /etc/profile 或者 /root/.bash_profile</span></span><br><span class="line"><span class="attr">PATH</span>=<span class="variable">$PATH</span>:/usr/local/jdk1.<span class="number">8.0</span>_111/bin</span><br><span class="line">export PATH</span><br></pre></td></tr></table></figure><h5 id="2完成执行脚本"><a class="markdownIt-Anchor" href="#2完成执行脚本"></a> 2)完成执行脚本</h5><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># /root/test.sh</span></span><br><span class="line">java -version 2&gt; /root/script.out</span><br></pre></td></tr></table></figure><h5 id="3设置定时任务"><a class="markdownIt-Anchor" href="#3设置定时任务"></a> 3)设置定时任务</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 先使/etc/profile生效[添加环境变量]，后使用sh执行脚本文件</span></span><br><span class="line">* * * * * <span class="built_in">source</span> /etc/profile;sh /root/test.sh</span><br><span class="line">* * * * * <span class="built_in">source</span> /root/.bash_profile;sh /root/test.sh</span><br></pre></td></tr></table></figure><h2 id="三-实战"><a class="markdownIt-Anchor" href="#三-实战"></a> 三、实战</h2><h3 id="1crontab-日志"><a class="markdownIt-Anchor" href="#1crontab-日志"></a> 1.crontab 日志</h3><h4 id="1cron-日志保存在varlogcron文件中"><a class="markdownIt-Anchor" href="#1cron-日志保存在varlogcron文件中"></a> (1)cron 日志保存在<code>/var/log/cron</code>文件中</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看最近的两条cron日志</span></span><br><span class="line">tail -n 2 /var/<span class="built_in">log</span>/cron</span><br></pre></td></tr></table></figure><h3 id="2清理系统日志"><a class="markdownIt-Anchor" href="#2清理系统日志"></a> 2.清理系统日志</h3><h4 id="1系统日志的存放位置-varlogmessages"><a class="markdownIt-Anchor" href="#1系统日志的存放位置-varlogmessages"></a> (1)系统日志的存放位置： <code>/var/log/messages</code></h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"># /var/log目录下日志[平时主要用到的日志文件]</span><br><span class="line">cron -- 定时任务日志</span><br><span class="line">secure -- 相关ssh服务日志</span><br><span class="line">messages -- 系统总日志</span><br><span class="line">firewalld -- 系统防火墙日志</span><br><span class="line">lastlog -- 登录日志</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看当前目录下所有文件的大小</span></span><br><span class="line">du -sh .</span><br><span class="line"></span><br><span class="line"><span class="comment"># 清空系统日志 [/dev/null代表一个空文件]</span></span><br><span class="line">* 1 * * * cat /dev/null &gt; /var/<span class="built_in">log</span>/messages</span><br></pre></td></tr></table></figure><h4 id="2同时清理多个日志文件使用脚本文件"><a class="markdownIt-Anchor" href="#2同时清理多个日志文件使用脚本文件"></a> (2)同时清理多个日志文件[使用脚本文件]</h4><h5 id="1-文件rootlog_cleansh"><a class="markdownIt-Anchor" href="#1-文件rootlog_cleansh"></a> 1). 文件：<code>/root/log_clean.sh</code></h5><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash">!/bin/sh</span></span><br><span class="line"></span><br><span class="line">cat /dev/null &gt; /var/log/messages</span><br><span class="line">cat /dev/null &gt; /var/log/secure</span><br></pre></td></tr></table></figure><h5 id="2-定时任务"><a class="markdownIt-Anchor" href="#2-定时任务"></a> 2). 定时任务</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 定时执行日志清理脚本</span></span><br><span class="line">* * * * * sh /root/log_clean.sh</span><br></pre></td></tr></table></figure><h3 id="3crontab-备份-source-code"><a class="markdownIt-Anchor" href="#3crontab-备份-source-code"></a> 3.crontab 备份 source code</h3><h4 id="1准备工作"><a class="markdownIt-Anchor" href="#1准备工作"></a> (1)准备工作</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">backup -- 备份文件存放目录</span><br><span class="line">script -- 脚本文件目录</span><br><span class="line">www --- 网站根目录</span><br></pre></td></tr></table></figure><h4 id="2编写执行备份的脚本文件datascriptwww_backupsh"><a class="markdownIt-Anchor" href="#2编写执行备份的脚本文件datascriptwww_backupsh"></a> (2)编写执行备份的脚本文件[/data/script/www_backup.sh]</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash">!/bin/bash</span></span><br><span class="line"></span><br><span class="line">basedir = /data/backup</span><br><span class="line"></span><br><span class="line">www_src = $basedir/www_src/$(date +%F_%H%M)</span><br><span class="line"></span><br><span class="line">[ ! -d "$www_src" ]  &amp;&amp; mkdir -p $www_src</span><br><span class="line"></span><br><span class="line">cd /data</span><br><span class="line">tar -jpcf $www_src/www.tar.bz2 www</span><br></pre></td></tr></table></figure><h4 id="3脚本解释说明"><a class="markdownIt-Anchor" href="#3脚本解释说明"></a> (3)脚本解释说明</h4><h5 id="1-basedir-databackup"><a class="markdownIt-Anchor" href="#1-basedir-databackup"></a> 1). <code>basedir = /data/backup</code></h5><p>定义一个变量，名称为<code>basedir</code>，值为<code>/data/backup</code></p><h5 id="2-date-f_hm"><a class="markdownIt-Anchor" href="#2-date-f_hm"></a> 2). <code>$(date +%F_%H%M)</code></h5><p>获取系统的日期时间，并拼上日期(%F),再拼上小时(%H)和分钟(%M) =》 [2018-03-16_0715]</p><h5 id="3-www_src-basedirwww_srcdate-f_hm"><a class="markdownIt-Anchor" href="#3-www_src-basedirwww_srcdate-f_hm"></a> 3). <code>www_src = $basedir/www_src/$(date +%F_%H%M)</code></h5><p>使用变量 basedir 的值，再拼接出多层目录，赋值给 www_src[值为/data/backup/www_src/{当前日期时间的目录(动态)}]</p><h5 id="4-d-www_src"><a class="markdownIt-Anchor" href="#4-d-www_src"></a> 4). <code>[ ! -d &quot;$www_src&quot; ]</code></h5><p>相当于 if 的判断,判断$www_src 是否不是一个目录</p><h5 id="5-tar-jpcf-www_srcwwwtarbz2-www"><a class="markdownIt-Anchor" href="#5-tar-jpcf-www_srcwwwtarbz2-www"></a> 5). <code>tar -jpcf $www_src/www.tar.bz2 www</code></h5><p>打包/data/www 目录为 www.tar.bz2，并放置到$www_src 目录下[/data/backup/www_src/{当前日期时间的目录(动态)}/]</p><h4 id="4创建定时任务"><a class="markdownIt-Anchor" href="#4创建定时任务"></a> (4)创建定时任务</h4><p><code>* * * * * sh /data/script/www_backup.sh</code></p><h4 id="5重启定时任务使其生效"><a class="markdownIt-Anchor" href="#5重启定时任务使其生效"></a> (5)重启定时任务使其生效</h4><p><code>systemctl restart crond</code></p><h4 id="6查看备份文件大小被备份的原目录是-wordpress-系统"><a class="markdownIt-Anchor" href="#6查看备份文件大小被备份的原目录是-wordpress-系统"></a> (6)查看备份文件大小[被备份的原目录是 wordpress 系统]</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># /data/backup/www_src/2018-03-16_0715/</span></span><br><span class="line">8.4M    www.tar.bz2</span><br></pre></td></tr></table></figure><h3 id="4crontab-在-iptables-上的应用"><a class="markdownIt-Anchor" href="#4crontab-在-iptables-上的应用"></a> 4.crontab 在 iptables 上的应用</h3><h4 id="1应用场景"><a class="markdownIt-Anchor" href="#1应用场景"></a> (1)应用场景：</h4><p>DDOS 攻击或匿名暴力破解导致系统无法正常访问</p><h4 id="2解决之道"><a class="markdownIt-Anchor" href="#2解决之道"></a> (2)解决之道：</h4><p>使用 crontab 集成 iptables，实时监控系统的网络状态，及时将可疑的 ip 地址加入到网络黑名单</p><h4 id="3iptables-简介"><a class="markdownIt-Anchor" href="#3iptables-简介"></a> (3)iptables 简介：</h4><p>iptables 作为 Linux 下的内核防火墙，能够通过添加相应的规则，检测、修改、重定向、转发和丢弃 ip 数据包，从而过滤网络数据，实现保护系统网络的功能</p><p>CentOS7 默认使用 firewalld 服务维护内核防火墙，我们需要禁用 firewalld 服务，并安装 iptables 作为系统默认防火墙。</p><h4 id="4准备工作"><a class="markdownIt-Anchor" href="#4准备工作"></a> (4)准备工作</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看当前firewalld进程状态</span></span><br><span class="line">systemctl status firewalld</span><br><span class="line"></span><br><span class="line"><span class="comment"># 关闭并禁用firewalld</span></span><br><span class="line">systemctl <span class="built_in">disable</span> firewalld</span><br><span class="line">systemctl stop firewalld</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装iptables服务</span></span><br><span class="line">yum install iptables-services</span><br><span class="line">[yes]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 开启iptables服务</span></span><br><span class="line">systemctl <span class="built_in">enable</span> iptables</span><br><span class="line"></span><br><span class="line"><span class="comment"># 启动iptables服务</span></span><br><span class="line">systemctl start iptables</span><br><span class="line"></span><br><span class="line"><span class="comment"># 验证iptables是否安装成功</span></span><br><span class="line">iptables -V</span><br></pre></td></tr></table></figure><h4 id="5设置网络访问黑名单文件datascriptblacklisttxt"><a class="markdownIt-Anchor" href="#5设置网络访问黑名单文件datascriptblacklisttxt"></a> (5)设置网络访问黑名单文件<a href="%E6%AF%8F%E4%B8%80%E8%A1%8C%E4%B8%BA%E4%B8%80%E4%B8%AAip%E5%9C%B0%E5%9D%80">/data/script/blacklist.txt</a></h4><h4 id="6脚本实现datascriptfirewallsh"><a class="markdownIt-Anchor" href="#6脚本实现datascriptfirewallsh"></a> (6)脚本实现[/data/script/firewall.sh]</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash">!/bin/bash</span></span><br><span class="line"></span><br><span class="line">iptables -F</span><br><span class="line"></span><br><span class="line">list = /data/script/blacklist.txt</span><br><span class="line"></span><br><span class="line">for line in `cat $list`;do</span><br><span class="line">  iptables -I INPUT -s$line -j DROP</span><br><span class="line">  echo "$line is dropped into blacklist"</span><br><span class="line">done</span><br></pre></td></tr></table></figure><h4 id="7脚本解释说明"><a class="markdownIt-Anchor" href="#7脚本解释说明"></a> (7)脚本解释说明</h4><h5 id="1-iptables-f"><a class="markdownIt-Anchor" href="#1-iptables-f"></a> 1). <code>iptables -F</code></h5><p>清空 iptables 的所有记录</p><h5 id="2-for-line-in-cat-listdo"><a class="markdownIt-Anchor" href="#2-for-line-in-cat-listdo"></a> 2). for line in <code>cat $list</code>;do</h5><p>对/data/script/blacklist.txt 循环输出每一行</p><h5 id="3-iptables-i-input-sline-j-drop"><a class="markdownIt-Anchor" href="#3-iptables-i-input-sline-j-drop"></a> 3). <code>iptables -I INPUT -s$line -j DROP</code></h5><p>添加一个 input 链<br>-s 相当于 source，将每一行的数据，传递给 iptables<br>-j DROP 相当于一个 drop 操作，将当前主机接收到的数据包进行一个丢弃操作<br>=》将 blacklist.txt 中的 ip 地址做一个拒绝访问的操作，使黑名单中的 ip 地址无法访问主机</p><h5 id="4-echo-line-is-dropped-into-blacklist"><a class="markdownIt-Anchor" href="#4-echo-line-is-dropped-into-blacklist"></a> 4). <code>echo &quot;$line is dropped into blacklist&quot;</code></h5><p>对每一行的 ip 地址拒绝访问后，输出一条信息提示</p><h4 id="8创建定时任务"><a class="markdownIt-Anchor" href="#8创建定时任务"></a> (8)创建定时任务</h4><p><code>* * * * * source /etc/profile; sh /data/script/firewall.sh</code></p><h4 id="9查看-iptables-拒绝访问主机的-ip-列表"><a class="markdownIt-Anchor" href="#9查看-iptables-拒绝访问主机的-ip-列表"></a> (9)查看 iptables 拒绝访问主机的 ip 列表</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">iptables -nvL</span><br></pre></td></tr></table></figure><h3 id="5crontab-在-jenkins-上的扩展"><a class="markdownIt-Anchor" href="#5crontab-在-jenkins-上的扩展"></a> 5.crontab 在 Jenkins 上的扩展</h3><h4 id="1jenkins-介绍"><a class="markdownIt-Anchor" href="#1jenkins-介绍"></a> (1)Jenkins 介绍</h4><pre><code>java编写的开源、持续、集成工具，最大优势就是将开发人员和运维人员完美的结合在一起。</code></pre><h4 id="2使用前提"><a class="markdownIt-Anchor" href="#2使用前提"></a> (2)使用前提</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 安装</span></span><br><span class="line">yum install jenkins</span><br><span class="line">[y]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看系统java路径</span></span><br><span class="line"><span class="built_in">which</span> java</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看系统java版本</span></span><br><span class="line">java -version</span><br></pre></td></tr></table></figure><blockquote><p>[jenkins 要求的最低版本要求配置&gt;1.8.0_111]</p></blockquote><h4 id="3编辑-jenkins-的启动配置文件etcinitdjenkins"><a class="markdownIt-Anchor" href="#3编辑-jenkins-的启动配置文件etcinitdjenkins"></a> (3)编辑 jenkins 的启动配置文件[/etc/init.d/jenkins]</h4><p>[=&gt;搜索 candidates 关键词]<br>默认调用的 java 目录是/usr/bin/java 和/usr/lib/jvm/***/bin/java</p><p>添加 java 的路径<br><code>/usr/local/jdk1.8.0_111/bin/java</code></p><h4 id="4相关命令"><a class="markdownIt-Anchor" href="#4相关命令"></a> (4)相关命令</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#启动jenkins服务</span></span><br><span class="line">systemctl start jenkins</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看jenkins服务是否正常启动，并查看jenkins服务是否打开8080端口监听服务</span></span><br><span class="line">lsof -i:8080</span><br></pre></td></tr></table></figure><h4 id="5在-web-网页中进行配置并使用-jenkins"><a class="markdownIt-Anchor" href="#5在-web-网页中进行配置并使用-jenkins"></a> (5)在 web 网页中进行配置并使用 jenkins</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1.访问jenkins的后台地址</span></span><br><span class="line">10.110.16.5:8080</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2.登录密码保存在系统的目录下</span></span><br><span class="line">/var/lib/jenkins/secrets/initialAdminPassword文件中</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3.自定义jenkins=》安装插件</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 4.创建第一个管理员用户</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 5.创建新任务</span></span><br><span class="line">1)构建自由风格的软件项目</span><br><span class="line">2)构建触发器 =&gt; Build Periodically =&gt; 日程表[相当于crontab定时任务的时间表单] =&gt; * * * * *</span><br><span class="line">3)构建 =&gt; 增加构建步骤 =&gt; Execute shell =&gt; Command[执行定时任务时执行的操作] =&gt; <span class="built_in">echo</span> <span class="string">"this is a test build"</span></span><br></pre></td></tr></table></figure><p>【学习参考】：<a href="https://www.imooc.com/learn/1009" target="_blank" rel="noopener">慕课网-Crontab 不知疲倦的时间表</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;crontab 是一个用于设置周期性执行任务的工具。目前服务器端的运行环境大多数为 Linux，在日常的运营和维护中会有很多需要定期执行的操作，其中有些操作是可以机械的定期执行的操作，所以我们可以使用 crontab 定时服务来设置定时任务，从而减少手动操作的任务，帮助提高工作效率。&lt;/p&gt;
    
    </summary>
    
      <category term="Linux" scheme="https://blog.i2arch.tech/categories/Linux/"/>
    
    
      <category term="crontab" scheme="https://blog.i2arch.tech/tags/crontab/"/>
    
  </entry>
  
  <entry>
    <title>【实战】Tp5+小程序(二)--接口编写</title>
    <link href="https://blog.i2arch.tech/2018/07/05/php-practice-wx-tp5-2/"/>
    <id>https://blog.i2arch.tech/2018/07/05/php-practice-wx-tp5-2/</id>
    <published>2018-07-05T08:26:58.000Z</published>
    <updated>2018-11-16T12:14:52.026Z</updated>
    
    <content type="html"><![CDATA[<p>ThinkPHP5 从入门到深入学习，结合实战项目深入理解 ThinkPHP5 的特性和使用方法。编写完成简单的基于 RESTFul 接口，实现相应功能，掌握控制器、模型、异常处理、数据校验的使用。</p><a id="more"></a><h2 id="8-1-banner-相关表分析数据表关系分析"><a class="markdownIt-Anchor" href="#8-1-banner-相关表分析数据表关系分析"></a> 8-1 Banner 相关表分析（数据表关系分析）</h2><ul><li>banner 位的数据表<code>banner</code></li></ul><p><code>banner(id, name, description, delete_time, update_time)</code></p><ul><li>每个 banner 位图片的数据表<code>banner_item</code></li></ul><p><code>banner_item(id, img_id, key_word, type, delete_time, banner_id, update_time)</code></p><ul><li>图片表<code>image</code></li></ul><p><code>image(id, url, from, delete_time, update_time)</code></p><pre><code>banner &lt;=&gt; banner_item 一对多关系image &lt;=&gt; banner_item 一对一关系</code></pre><h2 id="8-2-模型关联定义关联与查询关联"><a class="markdownIt-Anchor" href="#8-2-模型关联定义关联与查询关联"></a> 8-2 模型关联–定义关联与查询关联</h2><blockquote><p>model/Banner.php</p></blockquote><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 创建关联方法</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">items</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 参数1：关联模型的模型名</span></span><br><span class="line">    <span class="comment">// 参数2：关联模型的外键</span></span><br><span class="line">    <span class="comment">// 参数3：当前模型的主键</span></span><br><span class="line">    <span class="comment">// hasMany：表示是一对多的关系</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;hasMany(<span class="string">'BannerItem'</span>, <span class="string">'banner_id'</span>, <span class="string">'id'</span>);</span><br><span class="line">    <span class="comment">// 【需要创建BannerItem模型类文件】</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>controller/Banner.php</p></blockquote><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//with()方法，设置关联模型</span></span><br><span class="line">$banner = BannerModel::with(<span class="string">'items'</span>)-&gt;find($id);</span><br><span class="line"><span class="comment">//等价于 $banner = model('banner')-&gt;with('items')-&gt;find($id);</span></span><br></pre></td></tr></table></figure><p>执行结果时会自动附件关联信息。</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">"id"</span>: <span class="number">1</span>,</span><br><span class="line">  <span class="attr">"name"</span>: <span class="string">"首页置顶"</span>,</span><br><span class="line">  <span class="attr">"description"</span>: <span class="string">"首页轮播图"</span>,</span><br><span class="line">  <span class="attr">"delete_time"</span>: <span class="literal">null</span>,</span><br><span class="line">  <span class="attr">"update_time"</span>: <span class="string">"1970-01-01 08:00:00"</span>,</span><br><span class="line">  <span class="attr">"items"</span>: [</span><br><span class="line">    &#123;</span><br><span class="line">      <span class="attr">"id"</span>: <span class="number">1</span>,</span><br><span class="line">      <span class="attr">"img_id"</span>: <span class="number">65</span>,</span><br><span class="line">      <span class="attr">"key_word"</span>: <span class="string">"6"</span>,</span><br><span class="line">      <span class="attr">"type"</span>: <span class="number">1</span>,</span><br><span class="line">      <span class="attr">"delete_time"</span>: <span class="literal">null</span>,</span><br><span class="line">      <span class="attr">"banner_id"</span>: <span class="number">1</span>,</span><br><span class="line">      <span class="attr">"update_time"</span>: <span class="string">"1970-01-01 08:00:00"</span></span><br><span class="line">    &#125;</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="8-3-模型关联-嵌套关联查询"><a class="markdownIt-Anchor" href="#8-3-模型关联-嵌套关联查询"></a> 8-3 模型关联 --嵌套关联查询</h2><p>1.多个关联表</p><pre><code>with(['items','item2'])</code></pre><p>2.命令行创建模型（自动完成模板）</p><pre><code>php think make:model api/Image</code></pre><p>3.banner 嵌套 items，现在需要给 items 嵌套 img 相关信息</p><p>多层嵌套使用方法：</p><pre><code>with(['items', 'items.img'])</code></pre><p>4.具体实现：</p><ul><li><code>model/BannerItem.php</code></li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">img</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// BannerItem和Image是一对一的关系，使用的方法是belongsTo</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;belongsTo(<span class="string">'Image'</span>, <span class="string">'img_id'</span>, <span class="string">'id'</span>);</span><br><span class="line">    <span class="comment">//【需要创建Image模型类文件】</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>也可以在<code>model/Image.php</code>中定义,实现的效果是一样的。</p><ul><li><code>controller/Banner.php</code></li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 多层嵌套的使用</span></span><br><span class="line">$banner = BannerModel::with([<span class="string">'items'</span>, <span class="string">'items.img'</span>])-&gt;find($id);</span><br></pre></td></tr></table></figure><p>5.实现效果如下：</p><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">"id"</span>: <span class="number">1</span>,</span><br><span class="line">  <span class="attr">"name"</span>: <span class="string">"首页置顶"</span>,</span><br><span class="line">  <span class="attr">"description"</span>: <span class="string">"首页轮播图"</span>,</span><br><span class="line">  <span class="attr">"delete_time"</span>: <span class="literal">null</span>,</span><br><span class="line">  <span class="attr">"update_time"</span>: <span class="string">"1970-01-01 08:00:00"</span>,</span><br><span class="line">  <span class="attr">"items"</span>: [</span><br><span class="line">    &#123;</span><br><span class="line">      <span class="attr">"id"</span>: <span class="number">1</span>,</span><br><span class="line">      <span class="attr">"img_id"</span>: <span class="number">65</span>,</span><br><span class="line">      <span class="attr">"key_word"</span>: <span class="string">"6"</span>,</span><br><span class="line">      <span class="attr">"type"</span>: <span class="number">1</span>,</span><br><span class="line">      <span class="attr">"delete_time"</span>: <span class="literal">null</span>,</span><br><span class="line">      <span class="attr">"banner_id"</span>: <span class="number">1</span>,</span><br><span class="line">      <span class="attr">"update_time"</span>: <span class="string">"1970-01-01 08:00:00"</span>,</span><br><span class="line">      <span class="attr">"img"</span>: &#123;</span><br><span class="line">        <span class="attr">"id"</span>: <span class="number">65</span>,</span><br><span class="line">        <span class="attr">"url"</span>: <span class="string">"/banner-4a.png"</span>,</span><br><span class="line">        <span class="attr">"from"</span>: <span class="number">1</span>,</span><br><span class="line">        <span class="attr">"delete_time"</span>: <span class="literal">null</span>,</span><br><span class="line">        <span class="attr">"update_time"</span>: <span class="string">"1970-01-01 08:00:00"</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    &#123;...&#125;</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="8-4-隐藏模型字段"><a class="markdownIt-Anchor" href="#8-4-隐藏模型字段"></a> 8-4 隐藏模型字段</h2><ul><li>方法 1：将对象转化为数组<code>toArray()</code>，再将该字段 unset</li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">$banner = $banner-&gt;toArray();</span><br><span class="line"><span class="keyword">unset</span>($banner[<span class="string">'delete_time'</span>]);</span><br></pre></td></tr></table></figure><ul><li>方法 2：使用对象的 <code>hidden()</code> 方法</li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$banner-&gt;hidden([<span class="string">'update_time'</span>, <span class="string">'delete_time'</span>]);</span><br></pre></td></tr></table></figure><ul><li>方法 3：只显示指定字段<code>visible()</code></li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$banner-&gt;visible([<span class="string">'id'</span>, <span class="string">'name'</span>]);</span><br></pre></td></tr></table></figure><h2 id="8-5-在模型内部隐藏字段"><a class="markdownIt-Anchor" href="#8-5-在模型内部隐藏字段"></a> 8-5 在模型内部隐藏字段</h2><p>1.对嵌套的数据字段隐藏</p><p>最好的办法：在相应的模型类中定义相应的属性。</p><ul><li>想要隐藏 <code>banner</code> 的字段信息</li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// model/Banner.php</span></span><br><span class="line"><span class="comment">// 隐藏的字段</span></span><br><span class="line"><span class="keyword">protected</span> $hidden = [<span class="string">'id'</span>];</span><br><span class="line"><span class="comment">// 只显示的字段</span></span><br><span class="line"><span class="keyword">protected</span> $visibale = [<span class="string">'name'</span>,<span class="string">'update_time'</span>];</span><br></pre></td></tr></table></figure><ul><li>想要隐藏 <code>banner.items</code> 下的字段信息：</li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// model/BannerItem.php</span></span><br><span class="line"><span class="keyword">protected</span> $hidden = [<span class="string">'id'</span>];</span><br></pre></td></tr></table></figure><ul><li>想要隐藏 <code>banner.items.img</code> 的字段信息</li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// model/Image.php</span></span><br><span class="line"><span class="keyword">protected</span> $hidden = [<span class="string">'from'</span>];</span><br></pre></td></tr></table></figure><h2 id="8-6-图片资源-url-配置"><a class="markdownIt-Anchor" href="#8-6-图片资源-url-配置"></a> 8-6 图片资源 URL 配置</h2><p>1.数据库存放的图片 url 是相对地址，所以获取的数据也是相对地址，不能直接获取到图片的具体资源位置。</p><pre><code>具体路径 = 服务器域名+路径配置+相对地址</code></pre><p>2.定义自己项目相关的配置 =》 自定义配置文件</p><p>TP5 扩展配置目录 =》自动加载该目录下的配置文件</p><p>默认位置：<code>application/extra</code></p><p>3.定义配置项:</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// application/extra/setting.php</span></span><br><span class="line"><span class="keyword">return</span> [</span><br><span class="line">  <span class="string">'img_prefix'</span> =&gt; <span class="string">'http://mypro.com/static/images'</span></span><br><span class="line">];</span><br></pre></td></tr></table></figure><p>4.tp5 中只有 public 目录是对外公开，可以访问的，所以图片资源应当放在 public 目录下</p><p>5.读取配置文件：</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 配置文件自动被加载，直接读取配置项即可</span></span><br><span class="line">config(<span class="string">'setting.img_prefix'</span>);</span><br></pre></td></tr></table></figure><p>6.<strong>【注】：如果自定义了<code>CONF_PATH</code>目录，则自动加载的配置文件目录应该在<code>config/extra</code>目录下</strong></p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// public/index.php</span></span><br><span class="line"><span class="comment">// 自定义CONF_PATH目录</span></span><br><span class="line">define(<span class="string">'CONF_PATH'</span>, <span class="keyword">__DIR__</span> . <span class="string">'/../config/'</span>);</span><br></pre></td></tr></table></figure><h2 id="8-7-模型读取器的巧妙应用"><a class="markdownIt-Anchor" href="#8-7-模型读取器的巧妙应用"></a> 8-7 模型读取器的巧妙应用</h2><p>1.读取器的命名：<code>get+字段名+Attr</code></p><p>如对 url 处理则定义为<code>getUrlAttr</code></p><p>2.读取器的特性：</p><ul><li><p>模型具有的性质</p></li><li><p>使用模型时自动调用的方法（访问该属性时调用）</p></li><li><p>AOP 思想的一个实现</p><p>3.接收器参数说明：</p><pre><code>参数1：需要处理的字段的值参数2：当前记录的完整信息(包括隐藏未显示的字段)</code></pre><p>4.使用方法：</p></li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义读取器（框架自动调用）</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">getUrlAttr</span><span class="params">($value)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// $value为获取到的url值。</span></span><br><span class="line">    $prefix = config(<span class="string">'setting.img_prefix'</span>);</span><br><span class="line">    <span class="keyword">return</span> $prefix.$value;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>url 字段被自动拼接成：<code>&quot;url&quot;: &quot;http://mypro.com/static/images/banner-4a.png&quot;</code>形式</p><p>5.根据业务逻辑进行调整</p><p>image 数据表中的<code>from</code>字段标识当前图片的来源。</p><pre><code>from=1 =》 图片来自当前项目，存储的是 相对路径from=2 =》 图片来自网络，存储的是 绝对路径</code></pre><p>即：当 from=1 时，才需要对 url 进行相关操作。</p><p>此时需要访问到<code>from</code>的值，要用到第二个参数。</p><p>6.调整代码实现</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义读取器（框架自动调用）</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">getUrlAttr</span><span class="params">($value, $data)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// $value 获取到的url值。</span></span><br><span class="line">    <span class="comment">// $data 当前记录的完整信息(包括隐藏未显示的字段)</span></span><br><span class="line"></span><br><span class="line">    $finalUrl = $value;</span><br><span class="line">    <span class="keyword">if</span> ($data[<span class="string">'from'</span>] == <span class="number">1</span>) &#123;</span><br><span class="line">        $prefix = config(<span class="string">'setting.img_prefix'</span>);</span><br><span class="line">        $finalUrl = $prefix . $value;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> $finalUrl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过关联模型访问 Image 模型并获取 url 字段信息时调用该方法。</p><h2 id="8-8-自定义模型基类"><a class="markdownIt-Anchor" href="#8-8-自定义模型基类"></a> 8-8 自定义模型基类</h2><p>1.对于多个模型处理 url 字段时，为增强代码的复用性，可将该处理方法封装到模型类基类<code>model/BaseModel.php</code>中。</p><p>2.其他的模型类不再直接继承<code>model</code>类，而是直接继承<code>BaseModel</code>类。</p><p>3.又考虑到当前使用的 url 表示的是 img 路径，而其他数据表中的 url 可能并非 img 路径，所以需要再次调整。将<code>getUrlAttr</code>功能的具体实现进行拆分。</p><p>(1) <code>model/BaseModel.php</code>，定义成一个普通的方法</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">prefixImgUrl</span><span class="params">($value, $data)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $finalUrl = $value;</span><br><span class="line">    <span class="keyword">if</span> ($data[<span class="string">'from'</span>] == <span class="number">1</span>) &#123;</span><br><span class="line">        $prefix = config(<span class="string">'setting.img_prefix'</span>);</span><br><span class="line">        $finalUrl = $prefix . $value;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> $finalUrl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>(2) <code>model/Image.php</code>，读取器中调用基类的方法。</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">getUrlAttr</span><span class="params">($value, $data)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;prefixImgUrl($value, $data);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>(3)分析：将业务逻辑的具体实现集中到一起，简化业务变动时的频繁修改。提高了项目的<strong>扩展性</strong>。</p><h2 id="8-9-定义-api-版本号"><a class="markdownIt-Anchor" href="#8-9-定义-api-版本号"></a> 8-9 定义 API 版本号</h2><p>1.为什么要实现多版本？</p><p>由于业务调整，实现的功能需要进行变更，（处理同一个问题需要使用不同解决方式），并且之前的功能还需要兼容，此时如果通过判断条件进行判断，再执行相应的功能会使得代码冗余，违背代码的<strong>开闭原则</strong>。应该将代码分离出来，每一个版本做一个单独的代码模块。</p><blockquote><p>开闭原则：对扩展是开放的，对修改是封闭的。（以扩展的形式修改代码）</p></blockquote><p>2.如何实现多版本？</p><ul><li>目录设置:</li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">application</span><br><span class="line">    |__ api</span><br><span class="line">        |__ controller</span><br><span class="line">            |__ v1</span><br><span class="line">                |__ Banner.php</span><br><span class="line">            |__ v2</span><br><span class="line">                |__ Banner.php</span><br></pre></td></tr></table></figure><ul><li>路由设置：</li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 动态参数 :version 动态访问相应版本</span></span><br><span class="line">Route::get(<span class="string">'api/:version/banner/:id'</span>, <span class="string">'api/:version.Banner/getBanner'</span>);</span><br></pre></td></tr></table></figure><h2 id="8-10-专题接口模型分析"><a class="markdownIt-Anchor" href="#8-10-专题接口模型分析"></a> 8-10 专题接口模型分析</h2><ul><li><p>theme 专题表<br><code>theme(id,name,description,topic_img_id,delete_time,head_img_id,update_time)</code></p><pre><code>`topic_img_id` 首页主题入口的img图片`head_img_id` 进入相应主题显示的head图片</code></pre></li><li><p>product 产品表<br><code>product(id,name,price,stock,delete_time,category_id,main_img_url,from,create_time,update_time,summary,img_id)</code></p><pre><code>`main_img_url``img_id`</code></pre></li><li><p>theme_product 专题-产品关联表<br><code>theme_product(theme_id,product_id)</code></p><pre><code>theme &lt;=&gt; product 多对多关系theme_product 多对多关系表中需要一个关联表连接两者关系</code></pre></li></ul><h2 id="8-11-一对一关系解析"><a class="markdownIt-Anchor" href="#8-11-一对一关系解析"></a> 8-11 一对一关系解析</h2><pre><code>theme &lt;=&gt; image 一对一关系</code></pre><p>1.一对一关系的表示方法（有主从关系）：</p><pre><code>hasOne()belongsTo()</code></pre><p>外键存储在其中一张表里，所以需要使用<code>hasOne</code>和<code>belongsTo</code>来区分。</p><pre><code>有外键的表`belongsTo`无外键的表无外键的表`hasOne`有外键的表</code></pre><p>theme – (topic_img_id, head_img_id) – 表中有外键 (对应 image 表中的 id 主键)<br>=》 theme topicImg belongsTo image</p><p>image – 表中没有外键<br>=》 image hasOne theme</p><h2 id="8-12-theme-接口验证与重构"><a class="markdownIt-Anchor" href="#8-12-theme-接口验证与重构"></a> 8-12 Theme 接口验证与重构</h2><p>1.Theme 接口实现的不同方法对比：</p><p>（1）客户端只负责调用接口，由接口确定需要返回的主题 theme 的 id 号（2）由客户端传入具体需要的主题 Theme 的 id 号（前端有更大的灵活性）</p><p>2.方法实现</p><p>步骤：</p><p>(1)定义控制器方法名</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/controller/v1/Theme.php</span></span><br><span class="line">getSimpleList();</span><br></pre></td></tr></table></figure><p>(2)路由文件定义路由</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// config/route.php</span></span><br><span class="line">Route::get(<span class="string">'api/:version/theme'</span>, <span class="string">'api/:version.Theme/getSimpleList'</span>);</span><br></pre></td></tr></table></figure><p>(3)控制器方法具体实现业务功能（一） — 参数要求</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 获取需要展示的主题theme</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Location</span> api/controller/v1/Theme.php</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> string $ids</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> string $theme</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure><p>(4)验证器验证</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/validate/IDCollection.php</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 1.验证规则</span></span><br><span class="line"><span class="keyword">protected</span> $rule = [</span><br><span class="line">    <span class="string">'ids'</span> =&gt; <span class="string">'require|checkIDs'</span></span><br><span class="line">];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2.验证不通过的提示信息</span></span><br><span class="line"><span class="keyword">protected</span> $message = [</span><br><span class="line">    <span class="string">'ids'</span> =&gt; <span class="string">'ids必须是以逗号隔开的多个正整数'</span></span><br><span class="line">];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3.自定义验证方法(验证器)</span></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 验证ids</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> string $values = id1,id2,id3,...</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> bool false/true</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="function"><span class="keyword">function</span> <span class="title">checkIDs</span><span class="params">($values)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $ids = explode(<span class="string">','</span>, $values);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">empty</span>($ids)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">foreach</span> ($ids <span class="keyword">as</span> $id) &#123;</span><br><span class="line">        <span class="comment">// 每个id必须是正整数</span></span><br><span class="line">        $res = <span class="keyword">$this</span>-&gt;isPositiveInteger($id);</span><br><span class="line">        <span class="keyword">if</span> (!$res) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>3.<strong>扩展</strong>:</p><p>IDCollection 和 IDMustPositiveInt 都用到对 id 是正整数的验证，为提高代码的复用性，可以：</p><pre><code>（1）将isPositiveInteger提取到公共方法中（没有内聚性）（2）将方法重新定义到验证器基类中供所有验证器之类调用。（优化的选择）</code></pre><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/validate/BaseValidate.php</span></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 验证是否是正整数</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> int $value</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> boolean false/true</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> <span class="function"><span class="keyword">function</span> <span class="title">isPositiveInteger</span><span class="params">($value)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (is_numeric($value) &amp;&amp; is_int($value+<span class="number">0</span>) &amp;&amp; ($value +<span class="number">0</span>)&gt;<span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>4.调用验证器</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/controller/v1/Theme.php</span></span><br><span class="line">(<span class="keyword">new</span> IDCollection())-&gt;goCheck();</span><br></pre></td></tr></table></figure><p>5.测试 url</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">http://mypro.com/api/v1/theme?ids=0.1,2,3</span><br><span class="line">http://mypro.com/api/v1/theme?ids=1,s,3</span><br></pre></td></tr></table></figure><h2 id="8-13-完成-theme-简要信息接口"><a class="markdownIt-Anchor" href="#8-13-完成-theme-简要信息接口"></a> 8-13 完成 Theme 简要信息接口</h2><p>1.完成获取信息接口</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/controller/v1/Theme.php</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">getSimpleList</span><span class="params">($ids=<span class="string">''</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 验证用户传递的参数</span></span><br><span class="line">    (<span class="keyword">new</span> IDCollection())-&gt;goCheck();</span><br><span class="line"></span><br><span class="line">    $ids = explode(<span class="string">','</span>, $ids);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 关联Image表获取相应信息</span></span><br><span class="line">    $theme = model(<span class="string">'theme'</span>)-&gt;with([<span class="string">'topicImg'</span>, <span class="string">'headImg'</span>])-&gt;select($ids);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 无查询结果时，进行异常处理</span></span><br><span class="line">    <span class="keyword">if</span> (!$theme) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> ThemeMissException();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 对数组格式的返回数据进行json格式化</span></span><br><span class="line">    <span class="keyword">return</span> json($theme);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>2.完成异常处理类</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// application/lib/exception/ThemeMissException.php</span></span><br><span class="line"><span class="keyword">public</span> $code = <span class="number">404</span>;</span><br><span class="line"><span class="keyword">public</span> $msg = <span class="string">'请求的主题不存在'</span>;</span><br><span class="line"><span class="keyword">public</span> $errorCode = <span class="number">30000</span>;</span><br></pre></td></tr></table></figure><p>3.在相应的模型中隐藏部分字段</p><p>(1)隐藏 Theme 表的部分字段</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/model/v1/Theme.php</span></span><br><span class="line"><span class="keyword">protected</span> $hidden = [<span class="string">'delete_time'</span>, <span class="string">'update_time'</span>, <span class="string">'topic_img_id'</span>, <span class="string">'head_img_id'</span>];</span><br></pre></td></tr></table></figure><p>(2)隐藏 Image 表的部分字段(只显示部分字段)</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/model/v1/Image.php</span></span><br><span class="line"><span class="keyword">protected</span> $visible = [<span class="string">'url'</span>];</span><br></pre></td></tr></table></figure><p>4.补充说明：</p><p>对于复杂的业务处理，应该将相应的代码写到 Service 层(Model 层之上) – 特别是涉及到多个模型之间的关联的时候</p><h2 id="8-14-开启路由完整匹配"><a class="markdownIt-Anchor" href="#8-14-开启路由完整匹配"></a> 8-14 开启路由完整匹配</h2><p>1.功能需求说明</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">点击专题图片进入到专题后需要显示相应的产品图片、</span><br><span class="line"></span><br><span class="line">=》获取属于该专题的产品信息</span><br><span class="line"></span><br><span class="line">（一个产品可以属于一个专题，也可以属于多个专题； 一个专题会包含多个产品） ==》多对多关系[Theme &lt;=&gt; Product]</span><br><span class="line"></span><br><span class="line">多对多关系的数据表有一个中间关联表</span><br></pre></td></tr></table></figure><p>2.模型关联获取关联的数据</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/model/Theme.php</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">products</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 参数1： 对应数据表的模型名</span></span><br><span class="line">    <span class="comment">// 参数2： 关联表的模型名</span></span><br><span class="line">    <span class="comment">// 参数3： 关联表中的外键名(和参数1模型关联)</span></span><br><span class="line">    <span class="comment">// 参数4： 关联表的外键(关联当前模型)</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;belongsToMany(<span class="string">'Product'</span>, <span class="string">'theme_product'</span>, <span class="string">'product_id'</span>. <span class="string">'theme_id'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>3.编写控制器方法(定义方法名和需要接收的参数)</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/v1/controller/Theme.php</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">getProducts</span><span class="params">($id)</span></span>&#123;&#125;</span><br></pre></td></tr></table></figure><p>4.定义路由</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Route::get(<span class="string">'api/:version/theme/:id'</span>, <span class="string">'api/:version.Theme/getProducts/:id'</span>);</span><br></pre></td></tr></table></figure><p>【注意】：</p><p>默认情况下 TP5 的配置项是关闭路由完整匹配的，这种情况下访问当前路由接口时，由于先匹配到<code>api/:version/theme</code>路由，便不会再继续向下匹配路由，从而会调用该路由对应的接口。</p><p>==》<strong>解决办法</strong>：<code>开启路由完整匹配</code></p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// application/config.php默认配置文件路径</span></span><br><span class="line"><span class="comment">// 路由使用完整匹配（设置为true时开启）</span></span><br><span class="line"><span class="string">'route_complete_match'</span>   =&gt; <span class="keyword">false</span>,  <span class="comment">// =&gt;true</span></span><br></pre></td></tr></table></figure><h2 id="8-15-完成-theme-详情接口"><a class="markdownIt-Anchor" href="#8-15-完成-theme-详情接口"></a> 8-15 完成 Theme 详情接口</h2><h3 id="1参数校验"><a class="markdownIt-Anchor" href="#1参数校验"></a> 1.参数校验</h3><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/v1/controller/Theme.php</span></span><br><span class="line">(<span class="keyword">new</span> IDMustPositiveInt)-&gt;check();</span><br></pre></td></tr></table></figure><h3 id="2在模型中编写方法实现数据获取"><a class="markdownIt-Anchor" href="#2在模型中编写方法实现数据获取"></a> 2.在模型中编写方法实现数据获取</h3><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/model/Theme.php</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">getThemeWithProducts</span><span class="params">($id)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $theme = <span class="keyword">self</span>::with(<span class="string">'products,topicImg,headImg'</span>)-&gt;find($id);</span><br><span class="line">    <span class="keyword">return</span> $theme;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>【注】REST 是面向资源的请求方式，即将相关的数据全部返回给客户端，不管客户端目前需不需要用得上，但这种方式返回的资源应该有一个限度，</p><h3 id="3在控制器中调用"><a class="markdownIt-Anchor" href="#3在控制器中调用"></a> 3.在控制器中调用</h3><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/v1/controller/Theme.php</span></span><br><span class="line">$theme = model(<span class="string">'theme'</span>)-&gt;getThemeWithProducts($id);</span><br><span class="line"><span class="keyword">if</span>(!$theme) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> ThemeMissException();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> $theme;</span><br></pre></td></tr></table></figure><h3 id="4编写异常处理类"><a class="markdownIt-Anchor" href="#4编写异常处理类"></a> 4.编写异常处理类</h3><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/lib/exception/ThemeMissException.php</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ThemeMissException</span> <span class="keyword">extends</span> <span class="title">BaseException</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 覆盖父类的相应属性</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> $code = <span class="number">404</span>;</span><br><span class="line">    <span class="keyword">public</span> $msg = <span class="string">'请求的主题不存在'</span>;</span><br><span class="line">    <span class="keyword">public</span> $errorCode = <span class="number">30000</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="8-16-数据库字段冗余的合理利用"><a class="markdownIt-Anchor" href="#8-16-数据库字段冗余的合理利用"></a> 8-16 数据库字段冗余的合理利用</h2><p>多对多关系的数据表关联查询时会自动多一个<code>pivot</code>字段的信息，存储关联字段。但关联信息不是我们需要显示的信息，所以将该字段隐藏掉。</p><p><code>products</code>中<code>main_img_url</code>和<code>img_id</code>都是用来关联 image 表，记录图片信息。属于数据冗余。</p><p>但此处是数据冗余的合理应用范围，因为需要在多处使用到，并且数据量和业务并不是太复杂。</p><h2 id="8-17-rest-的合理利用"><a class="markdownIt-Anchor" href="#8-17-rest-的合理利用"></a> 8-17 REST 的合理利用</h2><p>1.数据冗余之后对数据的完整性和一致性的维护变得困难。</p><p>2.数据更新时需要对多处数据进行修改，否则就会出现数据不一致的现象。</p><p>3.完成方法编写(对 product 相关字段的 url 进行处理—添加前缀)</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// api/model/Product.php</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">getMainImgUrlAttr</span><span class="params">($value, $data)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;prefixImgUrl($value, $data);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>4.REST 设计原则</p><p>(1)REST 是基于资源的，凡是和业务相关的数据都应该返回，不管当前的业务是否需要使用相应的数据。</p><p>好处在于后期业务变更需要相应的数据的时候，可以直接调用即可，不用更改服务器的接口程序，可以用来保证客户端的稳定性。</p><p>(2)但也不能一味的将所有相关的数据返回，会消耗数据库的性能。</p><h2 id="8-18-最近新品接口编写"><a class="markdownIt-Anchor" href="#8-18-最近新品接口编写"></a> 8-18 最近新品接口编写</h2><p>1.TP5 框架自带时间更新操作,使用模型操作数据库时，当插入记录时，自动带上<code>create_time</code>; 更新操作时自动带上<code>updated_time</code>;删除时自动带上<code>delete_time</code></p><p>2.删除操作不是真实的物理删除，而是通过判断<code>delete_time</code>的值来确定该条记录的状态</p><p>3.实现步骤</p><p>(1)定义控制器方法 [方法名|传递参数]</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">getRecent</span><span class="params">($count=<span class="number">15</span>)</span></span>&#123;&#125;</span><br></pre></td></tr></table></figure><p>(2)定义路由</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Route::get(<span class="string">'api/:version/product/recent'</span>, <span class="string">'api/:version.Product/getRecent'</span>);</span><br></pre></td></tr></table></figure><p>(3)定义模型方法</p><ul><li>limit()方法的使用</li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">getMostRecent</span><span class="params">($count)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    $products = <span class="keyword">self</span>::limit($count)-&gt;order(<span class="string">'create_time desc'</span>)-&gt;select();</span><br><span class="line">    <span class="keyword">return</span> $products;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>(4)编写验证器</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//需要对传递的count值进行验证</span></span><br><span class="line"><span class="comment">// application/validate/Count.php</span></span><br><span class="line"><span class="keyword">protected</span> $rule = [</span><br><span class="line">    <span class="string">'count'</span> =&gt; <span class="string">'isPositiveInteger|between:1,15'</span></span><br><span class="line">];</span><br></pre></td></tr></table></figure><p>(5)完成控制器方法</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">getRecent</span><span class="params">($count=<span class="number">15</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    (<span class="keyword">new</span> Count())-&gt;goCheck();</span><br><span class="line">    $products = model(<span class="string">'product'</span>)-&gt;getMostRecent($count);</span><br><span class="line">    <span class="keyword">if</span> ($products) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> ProductMissException();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> $products;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>(6)完成异常处理类方法</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ProductMissException</span> <span class="keyword">extends</span> <span class="title">BaseException</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> $code = <span class="string">'404'</span>;</span><br><span class="line">    <span class="keyword">public</span> $msg = <span class="string">'请求的product不存在'</span>;</span><br><span class="line">    <span class="keyword">public</span> $errorCode = <span class="number">20000</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>[注]：<code>app_debug</code>设置为 true 时，在<code>ExceptionHandler.php</code>中会调用父类的<code>render()</code>方法，导致框架的异常处理类找不到程序中自定义的异常处理类，从而会有报错提示。</p><p>!!!出现 500 系统内部错误!</p><ul><li><p>原因=&gt;config.php 设置<code>default_return_type</code>的值为<code>html</code>, 而 Product 的 controller 中 return 的结果值为 array，导致系统内部错误。</p></li><li><p>解决=&gt;将<code>default_return_type</code>的值为<code>json</code>。或者将 Product 的 controller 中 return 的结果进行 json 格式化。</p></li></ul><h3 id="警告学会查看-log-日志信息提高错误排查能力"><a class="markdownIt-Anchor" href="#警告学会查看-log-日志信息提高错误排查能力"></a> <strong>【警告】学会查看 log 日志信息，提高错误排查能力！</strong></h3><h2 id="8-19-使用数据集还是数组"><a class="markdownIt-Anchor" href="#8-19-使用数据集还是数组"></a> 8-19 使用数据集还是数组？</h2><p>1.问题 1：验证方法中，<code>$rule</code>属性数组的键值对中， 值<code>'isPositiveInteger|between:1,15'</code>中<code>|</code>符两端不能有空格，否则会被视为验证错误。</p><p>2.问题 2：对某些当前不需要用到，但后期会用到的字段信息（特殊情况不用，大多数情况要用），既不能直接显示，也不能直接隐藏，如何处理？</p><p>=》 在<code>api/v1/Product/recent</code>接口中临时隐藏<code>summary</code>字段。</p><p>3.<strong>collection()方法</strong>：临时隐藏某个或某些字段</p><p>【使用方法】：</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 使用数据集，临时隐藏某些字段</span></span><br><span class="line">$productCollection = collection($products);</span><br><span class="line">$products = $productCollection-&gt;hidden([<span class="string">'summary'</span>]);</span><br></pre></td></tr></table></figure><p>4.一个 product 是一个对象，一组 product 也可是是一个对象(数据集)。</p><p>5.使用对象的方式，可读性好，内聚性好。</p><p>6.TP5 调用模型自动返回一个数据集的形式：<code>resultset_type</code> [database.php]</p><p>默认是<code>array</code>，设置成<code>collection</code>后，模型返回的数据自动就是<code>collection</code>形式，不需要再转换一次。</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 在database.php中配置之后，不需要手动转换为collection</span></span><br><span class="line">$products = $products-&gt;hidden([<span class="string">'summary'</span>]);</span><br></pre></td></tr></table></figure><p>【扩展】：</p><p>但是这样使用之后，控制器中调用模型返回数据后，返回的是对象，即使没有数据，也不是空，所以直接使用<code>!</code>判断是不能实现效果的。</p><p>=》解决方法：使用数据集对象的<code>isEmpty()</code>方法进行判空。</p><h2 id="8-20-分类列表接口"><a class="markdownIt-Anchor" href="#8-20-分类列表接口"></a> 8-20 分类列表接口</h2><p>1.模型类的<code>all</code>方法使用。</p><ul><li><p>参数 1：主键列表或者查询条件（闭包） <code>mixed</code></p></li><li><p>参数 2：关联预查询 <code>array | string</code></p></li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">$categories = model(<span class="string">'Category'</span>)-&gt;with(<span class="string">'img'</span>)-&gt;select();</span><br><span class="line"><span class="comment">// 等价于</span></span><br><span class="line">$categories = model(<span class="string">'Category'</span>)-&gt;all([], <span class="string">'img'</span>);</span><br></pre></td></tr></table></figure><h2 id="8-21-扩展接口粒度与接口分层"><a class="markdownIt-Anchor" href="#8-21-扩展接口粒度与接口分层"></a> 8-21 扩展：接口粒度与接口分层</h2><p>1.减少首页 http 请求(api)的次数，从而减轻服务器的压力</p><p>2.接口粒度： 太粗=》代码复用性不好，不够灵活；太细=》需要发送的请求太多，不方便</p><p>3.架构师 =》 Api 接口设计 =》 底层设计力度比较小、灵活性比较高的 api 接口；越往上粒度逐渐变粗。</p><p>4.如果确实调用的接口比较多，应该在 api 基础数据层上建立业务层，再在业务层调用基础数据层相关的接口，再进行封装。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;ThinkPHP5 从入门到深入学习，结合实战项目深入理解 ThinkPHP5 的特性和使用方法。编写完成简单的基于 RESTFul 接口，实现相应功能，掌握控制器、模型、异常处理、数据校验的使用。&lt;/p&gt;
    
    </summary>
    
      <category term="PHP" scheme="https://blog.i2arch.tech/categories/PHP/"/>
    
    
      <category term="thinkphp5" scheme="https://blog.i2arch.tech/tags/thinkphp5/"/>
    
      <category term="微信小程序" scheme="https://blog.i2arch.tech/tags/%E5%BE%AE%E4%BF%A1%E5%B0%8F%E7%A8%8B%E5%BA%8F/"/>
    
  </entry>
  
  <entry>
    <title>【PHP】第三方登录OAuth2.0</title>
    <link href="https://blog.i2arch.tech/2018/07/05/http-ouath/"/>
    <id>https://blog.i2arch.tech/2018/07/05/http-ouath/</id>
    <published>2018-07-05T04:00:00.000Z</published>
    <updated>2018-11-16T12:14:51.898Z</updated>
    
    <content type="html"><![CDATA[<p>对于网站应用程序，涉及到登录和第三方 api 接口时，都会接触到 Token 等概念，而这部分的逻辑原理则是来自于 OAuth 授权协议，<br>目前的 OAuth2.0 协议的安全性也是被广泛认可，到目前为止尚且没有发生严重的安全事故。学习 OAuth2.0 协议的工作原理，并了解 qq 登录的流程和实现方式。</p><a id="more"></a><h2 id="oauth20-协议工作原理"><a class="markdownIt-Anchor" href="#oauth20-协议工作原理"></a> OAuth2.0 协议工作原理</h2><p><img src="http://ww1.sinaimg.cn/large/005EgYNMgy1fsvhp6rqkvj30wz0g0wld.jpg" alt=""></p><h3 id="步骤一请求-oauth-登录页"><a class="markdownIt-Anchor" href="#步骤一请求-oauth-登录页"></a> 步骤一：请求 OAuth 登录页</h3><p>Request Token URL - 未授权的令牌请求服务地址<a href="client_id,redirect_uri">慕课网请求 QQ 登录页面时使用的带有特定参数的 URL</a></p><h3 id="步骤二用户使用第三方账号登录并授权"><a class="markdownIt-Anchor" href="#步骤二用户使用第三方账号登录并授权"></a> 步骤二：用户使用第三方账号登录并授权</h3><p>身份认证通过后，会跳转到第一步的 redirect_uri，并携带 code 参数</p><h3 id="步骤三返回登录结果"><a class="markdownIt-Anchor" href="#步骤三返回登录结果"></a> 步骤三：返回登录结果</h3><p>User Authorization URL - 用户授权的令牌请求服务地址<a href="client_id,client_secret,code">用户 QQ 登录授权之后需要请求的一个带有特定参数的 URL</a><br>code 有生命周期且只可使用一次的字符串</p><p>AccessToken - 用户通过第三方应用访问 OAuth 接口的令牌[通过慕课网把自己喜欢的课程分享到 QQ 空间]<br>Refresh Token</p><h3 id="accesstoken-和-refreshtoken-数据传输原理"><a class="markdownIt-Anchor" href="#accesstoken-和-refreshtoken-数据传输原理"></a> AccessToken 和 RefreshToken 数据传输原理</h3><p>[imooc]带有 AccessToken 参数的特定 URL=&gt;[post]=&gt;[QQ]open Authorization API=&gt;[xml/json]=&gt;[imooc]带有 AccessToken 参数的特定 URL</p><h3 id="accesstoken-和-refreshtoken-生命周期解析"><a class="markdownIt-Anchor" href="#accesstoken-和-refreshtoken-生命周期解析"></a> AccessToken 和 RefreshToken 生命周期解析</h3><p>AccessToken - 具有较长生命周期(10 天半个月甚至更长)<br>User Authorization URL 中指定参数 RefreshToken 进行重新获取 AccessToken</p><h2 id="qq-登录"><a class="markdownIt-Anchor" href="#qq-登录"></a> QQ 登录</h2><h3 id="1接入-qq-开放平台的前置条件"><a class="markdownIt-Anchor" href="#1接入-qq-开放平台的前置条件"></a> 1.接入 QQ 开放平台的前置条件</h3><ul><li><p>qq 号</p></li><li><p>公网可访问的 web 服务器</p></li><li><p>关于域名备案</p></li></ul><p><img src="http://ww1.sinaimg.cn/large/005EgYNMgy1fsvhwnt4zqj30ye0fqdnz.jpg" alt=""></p><p>(腾讯的用于域名验证，拿到 appid 等信息)</p><ul><li>关于服务器运行环境</li></ul><h3 id="2申请-appid-和-appkey"><a class="markdownIt-Anchor" href="#2申请-appid-和-appkey"></a> 2.申请 AppID 和 AppKey</h3><p><a href="https://connect.qq.com" target="_blank" rel="noopener">QQ 互联</a></p><p>网站地址[需要在该页面下的 index.html 文件中嵌入一行代码，然后进行验证]<br>回调地址[可以填写多个，英文半角分号;间隔，加 http(s)😕/头]</p><p>[每次修改配置后都需要重新验证网站地址]</p><h3 id="3添加测试回调地址"><a class="markdownIt-Anchor" href="#3添加测试回调地址"></a> 3.添加测试回调地址</h3><p>eg. <a href="http://test.open.mypro.com/callback.php" target="_blank" rel="noopener">http://test.open.mypro.com/callback.php</a></p><h3 id="4引入官方-sdk"><a class="markdownIt-Anchor" href="#4引入官方-sdk"></a> 4.引入官方 SDK</h3><p><a href="http://wiki.connect.qq.com/sdk%E4%B8%8B%E8%BD%BD" target="_blank" rel="noopener">下载</a></p><h3 id="5sdk-参数配置"><a class="markdownIt-Anchor" href="#5sdk-参数配置"></a> 5.SDK 参数配置</h3><ul><li><p>appid</p></li><li><p>appkey</p></li><li><p>callback</p></li><li><p>请求授权列表</p><pre><code>  get_user_info add_share list_album add_album ...</code></pre><p>[请求的权限会在授权登录页面显示需要请求的信息列表]</p></li><li><p>是否开通调试</p></li></ul><h3 id="6sdk-解读"><a class="markdownIt-Anchor" href="#6sdk-解读"></a> 6.SDK 解读</h3><ul><li><p>文档资料 -&gt; oauth 开发指引 -&gt; 开发功率_server-side</p></li><li><p>Server-side or Client-side</p></li></ul><p>核心类和重要方法(Connectx.x/class/*.class.php)</p><ul><li><p>Recorder.class.php[配置读写与 SESSION 存取]</p><ul><li>__construct()<ul><li>读入配置文件 json 串：$incFileContents = file(ROOT.“comm/inc.php”)</li><li>$incFileContents = $incFileContents[1];</li><li>解析成 php 对象：<span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>t</mi><mi>h</mi><mi>i</mi><mi>s</mi><mo>−</mo><mo>&gt;</mo><mi>i</mi><mi>n</mi><mi>c</mi><mo>=</mo><mi>j</mi><mi>s</mi><mi>o</mi><msub><mi>n</mi><mi>d</mi></msub><mi>e</mi><mi>c</mi><mi>o</mi><mi>d</mi><mi>e</mi><mo>(</mo></mrow><annotation encoding="application/x-tex">this-&gt;inc = json_decode(</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="strut" style="height:0.75em;"></span><span class="strut bottom" style="height:1em;vertical-align:-0.25em;"></span><span class="base"><span class="mord mathit">t</span><span class="mord mathit">h</span><span class="mord mathit">i</span><span class="mord mathit">s</span><span class="mord">−</span><span class="mord rule" style="margin-right:0.2777777777777778em;"></span><span class="mrel">&gt;</span><span class="mord rule" style="margin-right:0.2777777777777778em;"></span><span class="mord mathit">i</span><span class="mord mathit">n</span><span class="mord mathit">c</span><span class="mord rule" style="margin-right:0.2777777777777778em;"></span><span class="mrel">=</span><span class="mord rule" style="margin-right:0.2777777777777778em;"></span><span class="mord mathit" style="margin-right:0.05724em;">j</span><span class="mord mathit">s</span><span class="mord mathit">o</span><span class="mord"><span class="mord mathit">n</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.33610799999999996em;"><span style="top:-2.5500000000000003em;margin-left:0em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mathit mtight">d</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.15em;"></span></span></span></span></span><span class="mord mathit">e</span><span class="mord mathit">c</span><span class="mord mathit">o</span><span class="mord mathit">d</span><span class="mord mathit">e</span><span class="mopen">(</span></span></span></span>incFileContents);</li></ul></li><li>readInc($name)<ul><li>return <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>t</mi><mi>h</mi><mi>i</mi><mi>s</mi><mo>−</mo><mo>&gt;</mo><mi>i</mi><mi>n</mi><mi>c</mi><mo>−</mo><mo>&gt;</mo></mrow><annotation encoding="application/x-tex">this-&gt;inc-&gt;</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="strut" style="height:0.69444em;"></span><span class="strut bottom" style="height:0.77777em;vertical-align:-0.08333em;"></span><span class="base"><span class="mord mathit">t</span><span class="mord mathit">h</span><span class="mord mathit">i</span><span class="mord mathit">s</span><span class="mord">−</span><span class="mord rule" style="margin-right:0.2777777777777778em;"></span><span class="mrel">&gt;</span><span class="mord rule" style="margin-right:0.2777777777777778em;"></span><span class="mord mathit">i</span><span class="mord mathit">n</span><span class="mord mathit">c</span><span class="mord">−</span><span class="mord rule" style="margin-right:0.2777777777777778em;"></span><span class="mrel">&gt;</span></span></span></span>name;</li><li>//-&gt;readInc(‘appid’) 既读取配置文件的 appid</li></ul></li></ul></li><li><p>URL.class.php[基于 CURL 库的 get 与 post 请求]</p><ul><li>combineURL($baseURL, $keysArr)<ul><li>拼接：$combined = $baseURL.&quot;?&quot;;</li><li>拼接参数: foreach($keysArr as <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>k</mi><mi>e</mi><mi>y</mi><mo>=</mo><mo>&gt;</mo></mrow><annotation encoding="application/x-tex">key=&gt;</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="strut" style="height:0.69444em;"></span><span class="strut bottom" style="height:0.8888799999999999em;vertical-align:-0.19444em;"></span><span class="base"><span class="mord mathit" style="margin-right:0.03148em;">k</span><span class="mord mathit">e</span><span class="mord mathit" style="margin-right:0.03588em;">y</span><span class="mord rule" style="margin-right:0.2777777777777778em;"></span><span class="mrel">=</span><span class="mrel">&gt;</span></span></span></span>val) <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>v</mi><mi>a</mi><mi>l</mi><mi>u</mi><mi>e</mi><mi>A</mi><mi>r</mi><mi>r</mi><mo>[</mo><mo>]</mo><mo>=</mo><mi mathvariant="normal">&quot;</mi></mrow><annotation encoding="application/x-tex">valueArr[] = &quot;</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="strut" style="height:0.75em;"></span><span class="strut bottom" style="height:1em;vertical-align:-0.25em;"></span><span class="base"><span class="mord mathit" style="margin-right:0.03588em;">v</span><span class="mord mathit">a</span><span class="mord mathit" style="margin-right:0.01968em;">l</span><span class="mord mathit">u</span><span class="mord mathit">e</span><span class="mord mathit">A</span><span class="mord mathit" style="margin-right:0.02778em;">r</span><span class="mord mathit" style="margin-right:0.02778em;">r</span><span class="mopen">[</span><span class="mclose">]</span><span class="mord rule" style="margin-right:0.2777777777777778em;"></span><span class="mrel">=</span><span class="mord rule" style="margin-right:0.2777777777777778em;"></span><span class="mord">&quot;</span></span></span></span>key=$val&quot;;</li><li>使用&amp;拼接参数键值对：$keyStr = implode(&quot;&amp;&quot;, $valueArr);</li><li>生成形如http://xxx.com?a=b&amp;c=d…的链接</li></ul></li><li>get(<span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>u</mi><mi>r</mi><mi>l</mi><mo separator="true">,</mo></mrow><annotation encoding="application/x-tex">url,</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="strut" style="height:0.69444em;"></span><span class="strut bottom" style="height:0.8888799999999999em;vertical-align:-0.19444em;"></span><span class="base"><span class="mord mathit">u</span><span class="mord mathit" style="margin-right:0.02778em;">r</span><span class="mord mathit" style="margin-right:0.01968em;">l</span><span class="mpunct">,</span></span></span></span>keysArr) 发送 get 请求</li><li>post(<span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>u</mi><mi>r</mi><mi>l</mi><mo separator="true">,</mo></mrow><annotation encoding="application/x-tex">url,</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="strut" style="height:0.69444em;"></span><span class="strut bottom" style="height:0.8888799999999999em;vertical-align:-0.19444em;"></span><span class="base"><span class="mord mathit">u</span><span class="mord mathit" style="margin-right:0.02778em;">r</span><span class="mord mathit" style="margin-right:0.01968em;">l</span><span class="mpunct">,</span></span></span></span>keysArr,$flag = 0) 发送 post 请求</li></ul></li><li><p>Oauth.class.php[Oauth 相关 URL 动态拼接与 token 操作]</p><ul><li>qq_login() 拼接 QQ 登录页面 URL<ul><li>读取 appid: $appid = $this-&gt;recoder-&gt;readInc(“appid”);</li><li>读取回调地: $callback = $this-&gt;recoder-&gt;readInc(“callback”);</li><li>读取授权列表：$this-&gt;recorder-&gt;readInc(“scope”);</li><li>生成唯一随机串防 CSRF 攻击</li><li>原样返回参数：md5(uniqid(rand(), TRUE));</li><li>state 写入 session 中：<span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi>t</mi><mi>h</mi><mi>i</mi><mi>s</mi><mo>−</mo><mo>&gt;</mo><mi>r</mi><mi>e</mi><mi>c</mi><mi>o</mi><mi>r</mi><mi>d</mi><mi>e</mi><mi>r</mi><mo>−</mo><mo>&gt;</mo><mi>w</mi><mi>r</mi><mi>i</mi><mi>t</mi><mi>e</mi><msup><mo>(</mo><mo mathvariant="normal">′</mo></msup><mi>s</mi><mi>t</mi><mi>a</mi><mi>t</mi><msup><mi>e</mi><mo mathvariant="normal">′</mo></msup><mo separator="true">,</mo></mrow><annotation encoding="application/x-tex">this-&gt;recorder-&gt;write(&#x27;state&#x27;,</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="strut" style="height:0.751892em;"></span><span class="strut bottom" style="height:1.001892em;vertical-align:-0.25em;"></span><span class="base"><span class="mord mathit">t</span><span class="mord mathit">h</span><span class="mord mathit">i</span><span class="mord mathit">s</span><span class="mord">−</span><span class="mord rule" style="margin-right:0.2777777777777778em;"></span><span class="mrel">&gt;</span><span class="mord rule" style="margin-right:0.2777777777777778em;"></span><span class="mord mathit" style="margin-right:0.02778em;">r</span><span class="mord mathit">e</span><span class="mord mathit">c</span><span class="mord mathit">o</span><span class="mord mathit" style="margin-right:0.02778em;">r</span><span class="mord mathit">d</span><span class="mord mathit">e</span><span class="mord mathit" style="margin-right:0.02778em;">r</span><span class="mord">−</span><span class="mord rule" style="margin-right:0.2777777777777778em;"></span><span class="mrel">&gt;</span><span class="mord rule" style="margin-right:0.2777777777777778em;"></span><span class="mord mathit" style="margin-right:0.02691em;">w</span><span class="mord mathit" style="margin-right:0.02778em;">r</span><span class="mord mathit">i</span><span class="mord mathit">t</span><span class="mord mathit">e</span><span class="mopen"><span class="mopen">(</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.751892em;"><span style="top:-3.063em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span></span></span></span></span></span></span></span></span><span class="mord mathit">s</span><span class="mord mathit">t</span><span class="mord mathit">a</span><span class="mord mathit">t</span><span class="mord"><span class="mord mathit">e</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.751892em;"><span style="top:-3.063em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span></span></span></span></span></span></span></span></span><span class="mpunct">,</span></span></span></span>state);<br>=》拼接之后得到https://graph.qq.com/oauth2.0/authorize?response_type=code&amp;client_id=[APPID]&amp;redirect_uri=[REDIRECT_URI]&amp;scope=[SCOPE]&amp;state=[STATE]</li><li>跳转到生成的 url: header(“Location:$login_url”);</li></ul></li><li>qq_callback() QQ 登录完成后的回调处理</li></ul></li></ul><h3 id="7sdk-优化"><a class="markdownIt-Anchor" href="#7sdk-优化"></a> 7.SDK 优化</h3><ul><li>SDK 太老，很久无人维护<ul><li>调整文件及目录结构</li></ul></li><li>SDK 中的常量名太常见，可能和现有项目冲突<ul><li>批量替换 SDK 中常量名称为不常见名称</li></ul></li></ul><h3 id="8整合-sdk-到-web-项目中请求访问-qq-登录页面"><a class="markdownIt-Anchor" href="#8整合-sdk-到-web-项目中请求访问-qq-登录页面"></a> 8.整合 SDK 到 Web 项目中–请求访问 QQ 登录页面</h3><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">$oauth = <span class="keyword">new</span> Oauth();</span><br><span class="line">$oauth-&gt;qq_login();</span><br></pre></td></tr></table></figure><h3 id="9整合-sdk-到-web-项目中获取-code-和-accesstoken"><a class="markdownIt-Anchor" href="#9整合-sdk-到-web-项目中获取-code-和-accesstoken"></a> 9.整合 SDK 到 Web 项目中–获取 code 和 AccessToken</h3><ul><li><p>回调地址：<a href="http://test.open.mypro.com/callback.php" target="_blank" rel="noopener">http://test.open.mypro.com/callback.php</a></p></li><li><p>拿到返回的<code>code</code>，并请求 AccessToken</p></li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">$oauth = <span class="keyword">new</span> Oauth();</span><br><span class="line">$accessToken = $oauth-&gt;qq_callback();</span><br></pre></td></tr></table></figure><h3 id="9整合-sdk-到-web-项目中获取-openid"><a class="markdownIt-Anchor" href="#9整合-sdk-到-web-项目中获取-openid"></a> 9.整合 SDK 到 Web 项目中–获取 openID</h3><p>(1) 关于 openId</p><ul><li>QQ 用户在第三方站点的唯一标识</li><li>同一个 QQ 用户在不同站点使用 QQ 登录 openId 始终一样</li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$openid = $oauth-&gt;get_openid();</span><br></pre></td></tr></table></figure><p>(2)存储<code>accesstoken</code>和<code>openid</code>到<code>cookie</code>中</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 有效期时长可以读取session中的相应信息的有效期 [手动设置时需要将该时长小于实际有效期]</span></span><br><span class="line">setcookie(<span class="string">'qq_accesstoken'</span>, $accesstoken, time()+<span class="number">86400</span>);</span><br><span class="line">setcookie(<span class="string">'qq_openid'</span>, $openid, time()+<span class="number">86400</span>);</span><br></pre></td></tr></table></figure><h3 id="10api-调用示例"><a class="markdownIt-Anchor" href="#10api-调用示例"></a> 10.API 调用示例</h3><h4 id="调用get_user_info接口获取用户信息"><a class="markdownIt-Anchor" href="#调用get_user_info接口获取用户信息"></a> 调用<code>get_user_info</code>接口，获取用户信息</h4><p>(1)回调成功后，跳转到<code>index.php</code>文件</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">header(<span class="string">'Location: index.php'</span>);</span><br></pre></td></tr></table></figure><p>(2)判断当前登录状态[通过 cookie]</p><ul><li><p>未登录</p><ul><li>进行登录 [获取 AccessToken，获取 openid]</li></ul></li><li><p>已经登录</p><ul><li>调用接口，获取信息</li></ul></li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">$qc = <span class="keyword">new</span> QC($_COOKIE[<span class="string">'qq_accesstoken'</span>], $_COOKIE[<span class="string">'qq_openid'</span>]);</span><br><span class="line">$userInfo = $qc-&gt;get_user_info();</span><br></pre></td></tr></table></figure><h3 id="平台政策与注意事项"><a class="markdownIt-Anchor" href="#平台政策与注意事项"></a> 平台政策与注意事项</h3><ul><li><p>APPID 申请之后 3 个月未申请上线将被回收</p></li><li><p>申请上线需要使用官网提供的 QQ 登录按钮素材</p></li><li><p>遵守国家法律法规, 拒绝黄赌毒</p></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;对于网站应用程序，涉及到登录和第三方 api 接口时，都会接触到 Token 等概念，而这部分的逻辑原理则是来自于 OAuth 授权协议，&lt;br&gt;
目前的 OAuth2.0 协议的安全性也是被广泛认可，到目前为止尚且没有发生严重的安全事故。学习 OAuth2.0 协议的工作原理，并了解 qq 登录的流程和实现方式。&lt;/p&gt;
    
    </summary>
    
      <category term="PHP" scheme="https://blog.i2arch.tech/categories/PHP/"/>
    
    
      <category term="oauth" scheme="https://blog.i2arch.tech/tags/oauth/"/>
    
  </entry>
  
  <entry>
    <title>【实战】Tp5+小程序(一)--数据库访问与ORM</title>
    <link href="https://blog.i2arch.tech/2018/07/01/php-practice-wx-tp5-1/"/>
    <id>https://blog.i2arch.tech/2018/07/01/php-practice-wx-tp5-1/</id>
    <published>2018-07-01T08:26:58.000Z</published>
    <updated>2018-11-16T12:14:52.013Z</updated>
    
    <content type="html"><![CDATA[<p>ThinkPHP5 从入门到深入学习，结合实战项目深入理解 ThinkPHP5 的特性和使用方法，了解 ThinkPHP5 的数据库访问和 ORM 思想，学习使用查询构造器、日志以及初步了解异常处理机制。</p><a id="more"></a><h2 id="72-从一个错误了解-exception-的继承关系"><a class="markdownIt-Anchor" href="#72-从一个错误了解-exception-的继承关系"></a> 7.2 从一个错误了解 Exception 的继承关系</h2><p>1.将<code>Exception</code>修改为全局<code>Exception</code>基类，而不是<code>think\Exception</code></p><pre><code>`think\Exception =&gt; \Exception =&gt;  Throwable``HttpException =&gt; \RuntimeException =&gt;  \Exception =&gt;  Throwable`</code></pre><p><strong>当访问的控制器不存在、url 错误时，属于 HttpException 异常。</strong> 而原先定义的<code>render()</code>和<code>recordErrorLog()</code>方法要求接收的参数类型是<code>think\Exception</code>，由于<code>HttpException</code>不是继承于<code>think\Exception</code>，不能转化为<code>think\Exception</code>，所以会报错。</p><blockquote><p>解决：使用全局 Exception 基类后，既支持 HttpException，又支持 think\Exception。子类可以自动被转化为父类的类型。<br>Exception 基类是所有异常类的基类。</p></blockquote><p>2.<strong>补充</strong>：PHPStrom 快捷键：</p><ul><li>ctrl+alt+O =&gt; 快速删除没有 use 的类</li></ul><h2 id="73-tp5-数据库中间层架构解析"><a class="markdownIt-Anchor" href="#73-tp5-数据库中间层架构解析"></a> 7.3 TP5 数据库中间层架构解析</h2><p><img src="https://ws1.sinaimg.cn/large/005EgYNMgy1fucheex8nsj30g10cxt9l.jpg" alt="tp5数据库架构"></p><h2 id="74-查询构造器一"><a class="markdownIt-Anchor" href="#74-查询构造器一"></a> 7.4 查询构造器一</h2><p>1、为什么不使用原生的查询语句而使用查询构造器？</p><ul><li>简洁方便</li><li>对不同数据库的操作进行了封装，使用统一的数据库操作标准</li></ul><p>2、对查询构造器的理解</p><ul><li>查询构造器不仅仅是对数据库进行读操作，还包括数据库的写操作</li><li>查询构造器仅仅是语法，最终都是通过 Builder 翻译成 SQL 语句来执行</li></ul><h2 id="75-查询构造器二"><a class="markdownIt-Anchor" href="#75-查询构造器二"></a> 7.5 查询构造器二</h2><p>1.查询语句 = 链式方法 + 执行方法</p><p>2.链式方法：</p><ul><li>where</li><li>whereOr</li><li>field</li><li>…</li></ul><p>只会返回 Query 对象，不是查询结果</p><p>3.执行方法：</p><ul><li><p>find</p></li><li><p>select</p></li><li><p>update</p></li><li><p>delete</p></li><li><p>insert</p><p>4.<strong>在执行方法调用前，查询状态是保留的，直到调用执行方法后，状态才会被清除</strong></p></li></ul><h2 id="76-查询构造器三"><a class="markdownIt-Anchor" href="#76-查询构造器三"></a> 7.6 查询构造器三</h2><p>1.链式方法说明（where）：</p><blockquote><p>where(‘字段名’,‘表达式’,‘查询条件’)</p></blockquote><p>2.三种实现方式：</p><ul><li><p>表达式</p></li><li><p>数组法（不够灵活，且存在一定的安全问题）</p></li><li><p>闭包（最灵活）</p></li></ul><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 通过use来使用外部的数据</span></span><br><span class="line">where(<span class="function"><span class="keyword">function</span> <span class="params">($query)</span> <span class="title">use</span> <span class="params">($id)</span></span>&#123;</span><br><span class="line">    $query-&gt;where(<span class="string">'banner_id'</span>, <span class="string">'='</span>, $id)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><h2 id="77-开启-sql-日志记录"><a class="markdownIt-Anchor" href="#77-开启-sql-日志记录"></a> 7.7 开启 SQL 日志记录</h2><p>1.database.php =》 debug=true</p><p>2.config.php =》 app_debug=true</p><p>3.config.php =》 log =》 level =》sql</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">'log'</span>                    =&gt; [</span><br><span class="line">     日志记录方式，内置 file socket 支持扩展</span><br><span class="line">    <span class="string">'type'</span>  =&gt; <span class="string">'File'</span>,</span><br><span class="line">     日志保存目录</span><br><span class="line">    <span class="string">'path'</span>  =&gt; LOG_PATH,</span><br><span class="line">     日志记录级别</span><br><span class="line">    <span class="string">'level'</span> =&gt; [<span class="string">'error'</span>, <span class="string">'sql'</span>],</span><br><span class="line">],</span><br></pre></td></tr></table></figure><p>不在配置文件中开启日志</p><p><code>ExceptionHandler</code>=&gt;<code>recordErrorLog</code> 方法中开启日志仅仅在发生异常时写入。</p><p>所以对于全局的情况，将日志手动添加到入口文件中，对所有调用都开启 sql 日志</p><p>？？这样和在配置文件中开启日志有什么区别？</p><h2 id="7-8-orm-与模型"><a class="markdownIt-Anchor" href="#7-8-orm-与模型"></a> 7-8 ORM 与模型</h2><p>1.ORM 理解：</p><p>ORM = <code>Object Relationship Map</code></p><p>对象关系映射:将每张数据表看作是一个对象</p><p>2.模型（TP5 模型）–ORM 实现的具体机制<br>=&gt; <code>业务的集合= 数据库查询+业务逻辑+...</code></p><p>【注】模型与数据表不是一一对应的关系，简单的业务逻辑看上去是一张表对应一个模型，但复杂的业务逻辑（需要分层）可能是横跨多个表。</p><p>模型不仅仅只是 model 这一层，复杂的业务还可以继续划分，tp5 中有 <code>model</code>(数据层，细)，<code>service</code>(服务层，粗)，<code>logic</code>(逻辑层)</p><h2 id="7-9-初识模型"><a class="markdownIt-Anchor" href="#7-9-初识模型"></a> 7-9 初识模型</h2><p>1.<code>model/Banner.php</code> 继承 Model 类之后，就成为了模型，就可以使用模型类已经封装好的方法(<code>get</code>)，而不用自定义方法(<code>getBannerById</code>)。</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">$banner = BannerModel::getBannerById($id);</span><br><span class="line"> <span class="comment">// =&gt; 返回数组</span></span><br><span class="line"> <span class="comment">//等价于</span></span><br><span class="line">$banner = BannerModel::get($id);</span><br><span class="line"><span class="comment">// =&gt; 返回对象，便于处理查询结果</span></span><br></pre></td></tr></table></figure><p>2.tp5 自动将对象序列化，序列化的格式根据配置文件中的配置项<code>default_return_type</code>转化为相应的格式。</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// config.php</span></span><br><span class="line"><span class="string">'default_return_type'</span> =&gt; <span class="string">'html'</span></span><br></pre></td></tr></table></figure><p>3.自定义模型方法(<code>getBannerById</code>)调用的是指定的数据表，而模型类自动的方法(<code>get</code>)调用的是模型类名对应的数据表。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;ThinkPHP5 从入门到深入学习，结合实战项目深入理解 ThinkPHP5 的特性和使用方法，了解 ThinkPHP5 的数据库访问和 ORM 思想，学习使用查询构造器、日志以及初步了解异常处理机制。&lt;/p&gt;
    
    </summary>
    
      <category term="PHP" scheme="https://blog.i2arch.tech/categories/PHP/"/>
    
    
      <category term="thinkphp5" scheme="https://blog.i2arch.tech/tags/thinkphp5/"/>
    
      <category term="微信小程序" scheme="https://blog.i2arch.tech/tags/%E5%BE%AE%E4%BF%A1%E5%B0%8F%E7%A8%8B%E5%BA%8F/"/>
    
  </entry>
  
  <entry>
    <title>【转】Windows系统下的高效工具</title>
    <link href="https://blog.i2arch.tech/2018/06/29/windows-tool/"/>
    <id>https://blog.i2arch.tech/2018/06/29/windows-tool/</id>
    <published>2018-06-29T10:50:47.000Z</published>
    <updated>2018-11-16T12:14:52.157Z</updated>
    
    <content type="html"><![CDATA[<p>正所谓：工欲善其事，必先利其器<br>花点时间来折腾一下还是非常有必要。</p><p>Mac 之所以 <strong>高效</strong>，实际上是藉其自带特色功能以及原生的命令行支持<br>（当然也有它脑残的一面，例如：自带的窗口管理基本没带；新建文件只能使用命令 <code>touch</code> ······）</p><a id="more"></a><h1 id="高效特性"><a class="markdownIt-Anchor" href="#高效特性"></a> ※ 高效特性</h1><p>下面列出 Mac 相对高效的特性：</p><ul><li>多工作区支持（实际上 Ubuntu 等都有此功能）</li><li>原生支持 Unix like 命令环境，且自带众多的运行环境及命令行工具</li><li>强大的 Spotlight 全局搜索框</li><li>强大的第三方神级软件：Homebrew / Alfred / iTerm2 …</li><li>······</li></ul><p>我们来把上述特性一一加装在 Windows 上。</p><p>P.S 首选 免费 / 开源 的，且在同类软件中脱颖而出的</p><h1 id="实现方案"><a class="markdownIt-Anchor" href="#实现方案"></a> ※ 实现方案</h1><h2 id="多工作区多桌面支持dexpot"><a class="markdownIt-Anchor" href="#多工作区多桌面支持dexpot"></a> 多工作区（多桌面）支持：Dexpot</h2><p>没有 Mac 的好用，但绝对够用了<br>商业软件推荐：AltDesk（不推荐使用 MS 自家的）</p><h2 id="命令行环境babuncygwin-oh-my-zsh-git-众多运行环境"><a class="markdownIt-Anchor" href="#命令行环境babuncygwin-oh-my-zsh-git-众多运行环境"></a> 命令行环境：Babun（Cygwin + Oh-my-zsh + git + 众多运行环境）</h2><p>Babun 自带包管理工具 pact，极其方便<br>[P.S] Babun 使用 Ctrl + C 中断 node 进程，实际上 node 依旧还在运行，见 issue</p><p>因此最终建议使用超级好用的 Git Bash</p><h2 id="全局搜索框-快速启动器woxalfred-for-windows开源"><a class="markdownIt-Anchor" href="#全局搜索框-快速启动器woxalfred-for-windows开源"></a> 全局搜索框 / 快速启动器：Wox（Alfred for Windows，开源）</h2><p>而且还捎带上文件搜索神器 <code>Everything</code>，因此除了没有工作流外，基本满足开发需求。<br>该项的选择非常多， <code>Launchy</code>、<code>ALTRun</code>、<code>FARR</code>，以及大神们最喜欢用的 win + R</p><h2 id="其余效率相关"><a class="markdownIt-Anchor" href="#其余效率相关"></a> 其余效率相关</h2><ul><li><p>护眼神器：f.lux。不用多介绍了吧</p></li><li><p>增强型文件管理器：Total Commander 及其 教程。不过我用的是稍微简单一点的 Directory Opus</p></li><li><p>触发角与手势：WGestures</p></li></ul><p>可媲美 Mac 的体验，甚至还有所加强！设置滑到边角就锁屏，懒得按键盘 win + L 了有木有！还可以配置写命令，国产开源的良心之作！快点去人家的 Github 上面点个星星吧亲！</p><ul><li><p>历史剪贴板：<strong>Ditto</strong>，媲美 Mac 上的 ClipMenu，但又有所增强（带历史搜索功能）</p></li><li><p>简约又强大的卸载器：GeekUninstaller，可让你避免全家桶套餐</p></li></ul><p>WAMP 集成环境：Mac 下自带 Apache 与 PHP，但我相信 WampServer 的使用会更方便</p><p>快捷键：实际上这才是最重要的，详请知乎</p><p>转自：<a href="https://github.com/kenberkeley/make-windows-development-mac-like" target="_blank" rel="noopener">github</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;正所谓：工欲善其事，必先利其器&lt;br&gt;
花点时间来折腾一下还是非常有必要。&lt;/p&gt;
&lt;p&gt;Mac 之所以 &lt;strong&gt;高效&lt;/strong&gt;，实际上是藉其自带特色功能以及原生的命令行支持&lt;br&gt;
（当然也有它脑残的一面，例如：自带的窗口管理基本没带；新建文件只能使用命令 &lt;code&gt;touch&lt;/code&gt; ······）&lt;/p&gt;
    
    </summary>
    
      <category term="Tool" scheme="https://blog.i2arch.tech/categories/Tool/"/>
    
    
      <category term="tool" scheme="https://blog.i2arch.tech/tags/tool/"/>
    
  </entry>
  
</feed>
